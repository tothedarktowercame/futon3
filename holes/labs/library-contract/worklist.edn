{:schema :wm/worklist-v1
 :title "library-contract: populate the pattern library's rationale layer so cascades can be formed from it"
 :ledger-of "futon2/holes/problems/P-organise-the-library.md and P-validated-R5.md; standard futon3/README-flexiarg.md §5a"
 :method "futon2/holes/TN-validation-method.md -- same discipline as wm-contract: one row per invocation, author != reviewer, decisions recorded with arms and run where runnable"
 :classes #{:C :V :E :W :D :J}
 :statuses #{:open :blocked :done-unreviewed :done :needs-joe}
 :items
 [{:id :L1 :class :C :status :done :owner :any
   :reviewed-by "codex"
   :reviewed-at "2026-09-01T19:21:47Z"
   :review "Read a3bff43 in full and confirmed its author differs from the reviewer. Reproduced clj-kondo exit 0 (0 errors, 0 warnings), check-parens exit 0 (OK), library_graph_lint_test exit 0 (6 tests, 26 assertions), spider_runner_test exit 0 (3 tests, 11 assertions), and the C88-pinned aif section lint exit 0 (1247 files; edges :why 86, :how 27, :see-also 136; 4 edge-refusals, 0 warrant-refusals, 0 failures). Confirmed all 5 reclassified refused edges have no surviving directive in their source pattern. No .flexiarg file was changed by the evidence commit, so it authored no spider @why, post-hoc edge, or similarity-inferred edge."
   :covers-key :none
   :evidence
   {:sha "a3bff43"
    :schema "library/.spider/attestation-schema.edn -- :warrant-refused {:by :at :note} beside :state :proposed; :state :refused unchanged"
    :lint ["checks/library_graph_lint.clj:169 valid-warrant-refusal?"
           "checks/library_graph_lint.clj:195 refusal-act-failures (:warrant-refusal-requires-proposed-state)"
           "checks/library_graph_lint.clj:180 non-keyword attestation key is malformed"
           "summary counts :edge-refusals and :warrant-refusals separately"]
    :tests ["test/library_graph_lint_test.clj:159 edge-refusal-and-warrant-refusal-are-different-acts (4 planted cases: warrant-refusal keeps the directive and passes; edge-refusal with the directive still present fails :refused-edge-still-present and without it passes; both acts on one record fails; a warrant-refusal without a note is malformed)"
            "test/library_graph_lint_test.clj:185 non-keyword-attestation-key-is-malformed"]
    :reclassification "library/.spider/refusal-reclassification-2026-09-01.edn -- all 5 prior :refused records are edge-refusals by act (no surviving directive in any of the 5 source patterns); grounds quoted per record; aif rows at :607 and :646 give a warrant-shaped ground, recorded not revised"
    :repair "library/writing-coherence/attestations.edn:95 row :reason held unescaped quotes around \"meta-lede\", which split the EDN string and left `meta-lede` as a symbol key; escaped, and the linter now rejects the class"
    :not-touched "library/sidecar/attestations.edn rows 0 and 1 (:refused-reason, :state :proposed) left for L3's second read"
    :gates {:clj-kondo "exit 0, errors 0 warnings 0 (the one :info at library_graph_lint.clj:234 pre-dates this change)"
            :check-parens "exit 0, OK"
            :library-graph-lint-test "exit 0, 6 tests / 26 assertions / 0 failures"
            :spider-runner-test "exit 0, 3 tests / 11 assertions / 0 failures"
            :section-lint "aif pass? true, :edge-refusals 4; writing-coherence 4 failures and sidecar 2 failures are :evidence-excerpt-mismatch from the pinned fixture holding aif's 56 records only -- identical counts measured on the pre-change linter, so not a regression"}}
   :statement "Vocabulary gap found at the wave-2 gate: the lint's :refused means the EDGE leaves the pattern file; refusing a WARRANT is a different act (:proposed plus a review note). One word for two acts (P-organise-the-library.md, WAVE 2 GATED 12:10Z)."
   :acceptance "checks/library_graph_lint.clj and the attestation schema distinguish edge-refusal (edge leaves the file) from warrant-refusal (edge stays :proposed, warrant recorded as rejected with a note). A test exercises both acts on planted records. Existing 5 :refused records re-classified with pointers, or shown to all be true edge-refusals. clj-kondo, check-parens, and test/library_graph_lint_test.clj green."}
  {:id :L2 :class :C :status :open :owner :any
   :review-finding "checks/library_graph_lint.clj:278 authoring-turn? does not establish that the evidence record's author authored the named file; it only finds any authoring verb within 120 characters of any .flexiarg path. Reproducer: an evidence body containing `Alice wrote library/aif/example.flexiarg; I independently reviewed how it uses the cited pattern.` returns true, so rung-two-warrant? can discard genuine external corroboration. Add a counter-fixture where the record author describes somebody else's authored pattern file, make v3 leave it non-reflection, and then recompute the C88 audit. Review gates otherwise reproduced: clj-kondo exit 0 (0 errors, 0 warnings), check-parens exit 0 (OK), library_graph_lint_test exit 0 (7 tests, 35 assertions), spider_runner_test exit 0 (5 tests, 23 assertions), system-coherence pinned section lint exit 0 (1247 files; :why 86, :how 27, :see-also 136; 0 failures), and worklist_check exit 0. Evidence commit changes no .flexiarg files, so it adds no spider-authored @why, post-hoc, or similarity-inferred edges."
   :covers-key :none
   :evidence
   {:sha "299f47b"
    :rule ["checks/library_graph_lint.clj:12 reflection-rule-version 3"
           "checks/library_graph_lint.clj:278 authoring-turn? -- an authoring verb within 120 chars of a *.flexiarg path"
           "checks/library_graph_lint.clj:300 reflection-record? adds the class"
           "checks/library_graph_lint.clj:312 reflection-rule -- :authoring-turn and :authoring-verb-window ride in the cache header beside :basis, where v2's clauses live"]
    :rung-2-gate ["checks/spider_runner.clj:210 rung-two-warrant? -- rung 2 had no reflection check at all, which is how the rejected record reached the owner; at least one cited record must be non-reflection"
                  "checks/spider_runner.clj:252 the throw (:reflection-only-rung-two) in validate-output"
                  "checks/spider_runner.clj:160 the seat prompt states the rule"]
    :fixtures ["test/fixtures/library-graph/reflection-v3-fixtures.edn :authoring-turn -- the wave-2 rejected case (system-coherence e-7a50b862, claude-5 announcing the file it had just written and listing its references)"
               "test/fixtures/library-graph/reflection-v3-fixtures.edn :external-description -- the counter-fixture, abridged from wave-1 aif record e-f5a7638c: says \"flexiarg\" and \"authored\", names five patterns, announces no pattern file"]
    :tests ["test/spider_runner_test.clj:81 authoring-turn-is-reflection-under-v3-and-not-under-v2 -- asserts all three v2 clauses are false on the rejected record and v3 is true, and that v3 leaves the counter-fixture alone"
            "test/spider_runner_test.clj:97 rung-two-warrant-refuses-an-authoring-turn (stubbed fetch, no live store)"
            "test/library_graph_lint_test.clj:238 wave-1-attestations-re-audited-under-reflection-rule-v3 -- recomputes the audit file's numbers from the C88-pinned fixture"]
    :wave-1-re-audit
    {:record "library/aif/attestations-v3-reflection-audit.edn"
     :basis "C88 pin -- test/fixtures/library-graph/evidence-records.edn, 56 records, sha 15effe91…"
     :records-audited 56 :attestations-audited 62
     :reflection-under-v2 49 :authoring-turns 1 :reflection-under-v3 49
     :newly-reflection 0 :changed-verdicts 0
     :note "the one authoring turn (e-e8e638c6) is a zai-2 spider turn quoting the same claude-5 announcement, already reflection under v2 by its author; the expected 0 holds"}
    :cache-pin "evidence-cache-path now takes the rule version from the corpus pin, not the constant, so bumping to v3 does not orphan a v2-pinned index: system-coherence's basis 191738 still resolves to /tmp/futon3-spider-evidence-occurrences-v3-191738-d8f61810a34eb73a.edn and lints pass? true"
    :not-touched "library/system-coherence/attestations.edn -- the wave-2 record's :review-note still says \"v3 candidate\"; rewording it is a wave-2 record edit no acceptance asked for"
    :gates {:clj-kondo "exit 0, errors 0 warnings 0 over the four touched files (the one :info at library_graph_lint.clj:247 spider-self-text? pre-dates this change -- reproduced on HEAD's copy)"
            :check-parens "exit 0, OK (emacs -Q --batch -l futon4/dev/check-parens.el, arxana-check-parens-cli)"
            :library-graph-lint-test "exit 0, 7 tests / 35 assertions / 0 failures"
            :spider-runner-test "exit 0, 5 tests / 23 assertions / 0 failures"
            :section-lint "system-coherence pass? true, 0 failures, under the v2-pinned cache (no live rebuild)"}}
   :statement "Reflection-class rule v2 misses an author describing their own references at authoring time: wave 2 rejected one warrant on exactly this (the pattern's author describing its own references). A v3 candidate is named in the record but not written."
   :acceptance "Rule v3 stated in the cache header beside the basis (where v2 lives), with the wave-2 rejected case as a fixture that v3 catches and v2 misses; a counter-fixture v3 must NOT catch (a genuine external description). Test in the spider/lint suite. Wave-1 attestations re-audited under v3 with the count of changed verdicts reported (expected 0 per the wave-1 audit; state the number)."}
  {:id :L3 :class :V :status :open :owner :any
   :covers-key :none
   :statement "Two sidecar attestation holds from wave 2 (a tag-list warrant) are queued for a second read and nobody has read them."
   :acceptance "Each hold read against its evidence excerpt and either attested, sent back with a note, or warrant-refused (using L1's vocabulary if landed, a review note otherwise). The read recorded in the section's attestations.edn with reader and date. Pointers to both records."}
  {:id :L4 :class :E :status :open :owner :any
   :covers-key :none
   :statement "The one-edge price test, the stated next step of NOTE-the-chain-layer-exists-and-is-empty.md:105-112: nothing yet says whether a derived check over an @how edge is cheap or expensive, and that price decides whether Q(o|pi) is reachable by populating and deriving."
   :acceptance "Take ONE attested @how edge. Write the check (core.logic rule or unit test) that says whether the named method actually carries out the pattern. Record what it cost: wall time, lines, and what had to be invented that the edge did not supply. Verdict stated as cheap or expensive WITH the numbers, in a note beside the NOTE. No generalisation beyond the one edge."}
  {:id :L5 :class :W :status :open :owner :any
   :covers-key :none
   :loop-mode :one-slice-per-invocation
   :statement "Wave 3: the 0%-tagged families -- or3, nomad, mmca, memory, math-formalization-CA, math-formalization-CV, math-informal-CT, math-informal-CA -- rung 2 only, per P-organise-the-library's wave plan. One section per invocation."
   :progress []
   :acceptance "Per slice: run the spider for ONE listed section against the pinned evidence basis (C88 pin discipline), gate with library_graph_lint, record the section's before/after edges-by-kind and attestation states in library/.spider/ledger.edn, commit. Append the section and sha to :progress. The row is done when all eight sections have a wave-3 ledger entry. No @why authored by the spider: propose only (LD0)."}
  {:id :LD1 :class :D :status :open :owner :any
   :covers-key :none
   :statement "README-flexiarg §5a asks for an inline mark distinguishing a post-hoc @why (a trace, a hypothesis) from an author's own @why (a causal claim) and does not fix the syntax. Not determined by theory: a decision with arms, recorded in decisions.edn (interim arm :why-posthoc adopted so work can proceed; Joe can veto)."
   :acceptance "decisions.edn entry :posthoc-why-syntax carries the three arms with costs (parser impact, greppability, collision with existing @why consumers such as checks/playout_snatch.clj parse-why), the interim choice, and what evidence would change it. README-flexiarg §5a gains the interim form with a pointer to the decision. The lint recognises the interim form and counts it separately from authored @why. Tests."}
  {:id :LJ1 :class :J :status :needs-joe :owner :joe
   :covers-key :none
   :bar {:why-not-runnable "Confirm-vs-rewrite of a problem statement has no machine-behaviour difference: the statement is the criterion runs are judged against, so no recorded field separates the arms."
         :a "The S1 problem field of P-validated-R5.md is marked [Joe: confirm/rewrite] by its own author; no source, code, or prior ruling determines what the operator means his problem statement to be."
         :b "No experiment can decide a problem statement: it is the criterion runs are judged against, so the arms (confirm vs rewrite) have no machine-behaviour difference a recorded field could separate."}
   :statement "P-validated-R5 §1 confirm/rewrite. The gate before any §5 packet is dispatched: Joe confirms the S1 problem statement (G computed over single actions from a channel-range C; 'policy' a borrowed name for a cascade; a cascade in code a bag of pattern ids; nothing states what G over a cascade IS)."
   :acceptance "Joe says 'confirmed' or supplies the rewrite; the confirmation or new text lands in P-validated-R5.md §1 with a date; L6 unblocks."}
  {:id :L6 :class :C :status :blocked :owner :any :depends-on [:LJ1]
   :covers-key :none
   :statement "Kernel + compose on library/snatch first (P-validated-R5 §5 step 3, owner codex-22 by that record): the generic find/organise path, currently sorry in DarkTower/WarMachine/Holes.lean:264,308 and hardcoded to library/snatch in checks/playout_snatch.clj:228-260 and find_snatch.clj."
   :acceptance "As P-validated-R5 §5 step 3 states it, against the pinned find-snatch.edn fixture; laws F1-F4/O1-O4 as narrowed by the 2026-08-31 scope amendment. Not started before LJ1."}]}
