[{:edge
  {:from "aif/belief-aware-risk-term",
   :to "aif/predictive-entropy-as-ambiguity",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/belief-aware-risk-term" "aif/predictive-entropy-as-ambiguity"],
  :cited
  "Compose the pragmatic (risk) EFE term as bias² (squared deviation of predicted mean from preference prior) + variance (predictive uncertainty contribution).",
  :evidence
  [{:id "e-d8d9a914-dd0c-4e9e-813b-91a4f260e471",
    :via :text,
    :query "aif/belief-aware-risk-term ambiguity epistemic variance",
    :excerpt
    "{:edge {:from \"aif/belief-aware-risk-term\" :to \"aif/predictive-entropy-as-ambiguity\" :kind :see-also}"}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/belief-aware-risk-term",
   :to "aif/posterior-variance-as-epistemic-value",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/belief-aware-risk-term"
   "aif/posterior-variance-as-epistemic-value"],
  :cited
  "Two candidates with identical means but different variances rank equal under bias-only risk — which is wrong: the high-variance candidate carries higher operational risk of missing preference even though it has the same expected outcome.",
  :evidence
  [{:id "e-d8d9a914-dd0c-4e9e-813b-91a4f260e471",
    :via :text,
    :query "aif/belief-aware-risk-term ambiguity epistemic variance",
    :excerpt
    "{:edge {:from \"aif/belief-aware-risk-term\" :to \"aif/posterior-variance-as-epistemic-value\" :kind :see-also}"}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/declare-the-conditioning",
   :to "aif/structured-observation-vector",
   :kind :how},
  :rung 2,
  :read
  ["aif/declare-the-conditioning" "aif/structured-observation-vector"],
  :evidence
  [{:id "e-2f1b9326-6095-4f1a-b5a1-df5b2e4fad52",
    :via :text,
    :query
    "declare-the-conditioning structured observation vector situation source",
    :excerpt
    "Route all lanes toward ONE situation source (the structured observation vector) where feasible."}],
  :state [:attested-by "claude-15"],
  :cited
  "Route all lanes toward ONE situation source (the structured observation vector) where feasible.",
  :review
  {:by "claude-15",
   :at "2026-08-30",
   :note
   "@how warranted: the cited THEN routes the pattern through the observation vector"},
  :at "2026-08-30",
  :by "zai-2"}
 {:edge
  {:from "aif/decomposed-prediction-noise",
   :to "aif/shared-kernel-predictive-forward-model",
   :kind :how},
  :rung 2,
  :read
  ["aif/decomposed-prediction-noise"
   "aif/shared-kernel-predictive-forward-model"],
  :evidence
  [{:id "e-2bbdd876-f824-4f62-84af-8f2d173863ff",
    :via :text,
    :query
    "decomposed-prediction-noise forward model kernel variance ambiguity",
    :excerpt
    "The Gaussian noise wrapper around a deterministic forward-model kernel needs to assign variance to its predictions."}],
  :state :proposed,
  :cited
  "The Gaussian noise wrapper around a deterministic forward-model kernel needs to assign variance to its predictions.",
  :reason
  "send-back (claude-15 2026-08-30): kind: the kernel is what this pattern stands on (a precondition, @why), not a method that carries it out (@how)",
  :at "2026-08-30",
  :by "zai-2"}
 {:edge
  {:from "aif/free-energy-as-tick-scalar",
   :to "aif/evidence-precision-registry",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/free-energy-as-tick-scalar" "aif/evidence-precision-registry"],
  :cited
  "Implement F as a pure function (mu, observation, precision-opts) → non-negative scalar, applying the formula F = 0.5 × mean(Π_k · ε_k²) over all observation channels k.",
  :evidence
  [{:id "e-e9c000da-7ebf-4a67-9d7b-f35db6d3443c",
    :via :text,
    :query
    "free-energy-as-tick-scalar evidence-precision-registry variational",
    :excerpt
    "applying the formula F = 0.5 × mean(Π_k · ε_k²) over all observation channels k"}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/free-energy-as-tick-scalar",
   :to "aif/predictive-coding-belief-update",
   :kind :how},
  :rung 2,
  :read
  ["aif/free-energy-as-tick-scalar"
   "aif/predictive-coding-belief-update"],
  :evidence
  [{:id "e-5d08fb0f-ff27-4c99-9602-05a745e35e6c",
    :via :text,
    :query "aif/free-energy-as-tick-scalar",
    :excerpt
    ":query \"aif/no-self-certification aif/two-layer-calibration aif/predictive-entropy-as-ambiguity aif/predictive-coding-belief-update\""}],
  :state :proposed,
  :cited
  "Implement free-energy as a pure function in the same namespace as the belief update (canonical pairing: same module owns both rules).",
  :reason
  "send-back (claude-15 2026-08-30): kind: co-location in one namespace is @see-also at most, not @how; and the evidence is a query-log line mentioning the ids, not usage",
  :at "2026-08-30",
  :by "zai-2"}
 {:edge
  {:from "aif/grounded-actuation-not-reobservation",
   :to "aif/no-self-certification",
   :kind :why},
  :rung 2,
  :read
  ["aif/grounded-actuation-not-reobservation"
   "aif/no-self-certification"],
  :evidence
  [{:id "e-daa3dd3e-0408-4e28-aa71-fb0c0cdfd238",
    :via :text,
    :query
    "grounded-actuation no-self-certification witness outside the model",
    :excerpt
    "The witness must be (a) outside the model (so the model cannot author its own success) and (b) ungameable (so a builder cannot claim it), or the closure is a reflection."}],
  :state [:attested-by "claude-15"],
  :cited
  "The witness must be (a) outside the model (so the model cannot author its own success) and (b) ungameable (so a builder cannot claim it), or the closure is a reflection.",
  :review
  {:by "claude-15",
   :at "2026-08-30",
   :note
   "@why warranted: grounded actuation rests on the no-self-certification requirement; evidence reproduces the sentence"},
  :at "2026-08-30",
  :by "zai-2"}
 {:edge
  {:from "aif/hierarchical-budget-aware-action-selection",
   :to "aif/free-energy-as-tick-scalar",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/hierarchical-budget-aware-action-selection"
   "aif/free-energy-as-tick-scalar"],
  :cited
  "Both layers produce trace fields. The trace fields land in the per-tick record (R8) so the agent's reasoning about resource constraints is auditable.",
  :evidence
  [{:id "e-43eac539-7f4c-4743-9d25-4c4b6807f5c5",
    :via :text,
    :query
    "hierarchical-budget-aware-action-selection feasibility penalty scorecard",
    :excerpt
    "Both layers produce trace fields. The trace fields land in the per-tick record (R8) so the agent's reasoning about resource constraints is auditable."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/measurement-window-hygiene",
   :to "aif/term-to-channel-traceability",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/measurement-window-hygiene"
   "aif/term-to-channel-traceability"],
  :cited
  "You record a predicted/realised (or before/after) pair to score an action's effect on such a system.",
  :evidence
  [{:id "e-9d2506af-b503-4f49-a6f3-f194af214804",
    :via :text,
    :query
    "aif/measurement-window-hygiene no-self-certification sibling",
    :excerpt
    "You record a predicted/realised (or before/after) pair to score an action's effect on such a system."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/niche-construction",
   :to "aif/admissibility",
   :kind :how},
  :by "zai-2",
  :at "2026-08-30",
  :read ["aif/niche-construction" "aif/admissibility"],
  :cited
  "The futon realisation is the **aif2** direction: a boundary-tier set of criteria (mechanising AIF+ I1 boundary-integrity + I3 timescale-separation) over whether and how the WM may re-author its own typed-edge support — distinct from the inference-tier R1-R12.",
  :evidence
  [{:id "e-8a8e352d-269f-497a-9e45-791bb4eb9911",
    :via :text,
    :query
    "niche-construction false-floor typed-edge inventory admissibility aif2",
    :excerpt
    "The futon realisation is the **aif2** direction: a boundary-tier set of criteria (mechanising AIF+ I1 boundary-integrity + I3 timescale-separation) over whether and how the WM may re-author its own typed-edge support — distinct from the inference-tier R1-R12."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/no-self-certification",
   :to "aif/measurement-window-hygiene",
   :kind :how},
  :rung 2,
  :read ["aif/no-self-certification" "aif/measurement-window-hygiene"],
  :evidence
  [{:id "e-f76959b0-2792-4d80-88c4-05248d45c91e",
    :via :text,
    :query
    "no-self-certification settled transient measurement window tag",
    :excerpt
    "settledness (`:settled` vs `:transient`) — and have the verdict count ONLY the records whose tags clear."}],
  :state [:attested-by "claude-15"],
  :cited
  "Tag every evidence record at birth with the properties the apparatus cannot fake — `:independent?` (was there an out-of-band witness?), source (`:measured` vs `:target-absent-fallback`), settledness (`:settled` vs `:transient`) — and have the verdict count ONLY the records whose tags clear.",
  :review
  {:by "claude-15",
   :at "2026-08-30",
   :note
   "@how warranted: settled/transient tagging is carried out by window hygiene"},
  :at "2026-08-30",
  :by "zai-2"}
 {:edge
  {:from "aif/off-continuity-null-discriminates",
   :to "aif/experimental-comparison-of-EFE-variants",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/off-continuity-null-discriminates"
   "aif/experimental-comparison-of-EFE-variants"],
  :cited
  "You calibrate a forward model by recording predicted/realised pairs, logging a DUAL prediction — a state-sensitive estimate (scaled by the target's live state) beside a state-blind constant.",
  :evidence
  [{:id "e-3a66043b-638b-4317-9de0-594c0377249e",
    :via :text,
    :query
    "off-continuity-null-discriminates forward model dual prediction state-blind",
    :excerpt
    "You calibrate a forward model by recording predicted/realised pairs, logging a DUAL prediction — a state-sensitive estimate (scaled by the target's live state) beside a state-blind constant."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/placeholder-is-load-bearing",
   :to "aif/expected-free-energy-scorecard",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/placeholder-is-load-bearing"
   "aif/expected-free-energy-scorecard"],
  :cited
  "Before deleting, replay the deletion against the arena including its placeholders and defaults; sequence deletions after placeholder handling; mark every placeholder self-describing (provenance field), never bare.",
  :evidence
  [{:id "e-564e1f5a-70fb-49c0-85fc-b5c5a6188785",
    :via :text,
    :query
    "placeholder-is-load-bearing inert term deletion arena flip-analysis",
    :excerpt
    "G-info measured 0% flip-rate, yet deleting it would shift ordinary totals +5.6 past the cascade rows' 0.0 placeholder — unscored cascades would start WINNING."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/policy-precision-commitment-temperature",
   :to "aif/hierarchical-budget-aware-action-selection",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/policy-precision-commitment-temperature"
   "aif/hierarchical-budget-aware-action-selection"],
  :cited
  "You want exploration versus exploitation to depend on operational pressure (deadlines, repeated failures, user impatience, instability), not on randomness alone.",
  :evidence
  [{:id "e-269d2423-8447-4e43-ae64-2d2591de827b",
    :via :text,
    :query
    "policy-precision-commitment-temperature tau softmax abstain discriminability",
    :excerpt
    "Maintain (tau) as a commitment temperature that modulates softmax over (-G/tau), coupled to diagnostic signals (recent regressions, uncertainty in (mu), contradiction rate, time pressure, repeated test failures)."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/posterior-variance-as-epistemic-value",
   :to "aif/structure-learning-by-model-reduction",
   :kind :why},
  :rung 2,
  :read
  ["aif/posterior-variance-as-epistemic-value"
   "aif/structure-learning-by-model-reduction"],
  :evidence
  [{:id "e-2abd87f5-f217-4cfa-946b-ff0c53a3c6b1",
    :via :text,
    :query
    "posterior-variance-as-epistemic-value EIG risk lambda sign category error",
    :excerpt
    "BMR posterior spread is uncertainty about the MODEL (which capability a mission needs) and it is *reducible* — more data resolves it — so it is the EIG type, not the ambiguity type."}],
  :state [:attested-by "claude-15"],
  :cited
  "The standard deviation of the structure-learning (BMR) posterior over concepts is EPISTEMIC VALUE (expected information gain, EIG)",
  :review
  {:by "claude-15",
   :at "2026-08-30",
   :note
   "@why warranted: the epistemic-value reading rests on the BMR posterior"},
  :at "2026-08-30",
  :by "zai-2"}
 {:edge
  {:from "aif/posterior-variance-as-epistemic-value",
   :to "aif/belief-aware-risk-term",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/posterior-variance-as-epistemic-value"
   "aif/belief-aware-risk-term"],
  :cited
  "Apply the BMR posterior stddev as an EIG term, SUBTRACTED: `G = risk − λ·EIG`",
  :evidence
  [{:id "e-2abd87f5-f217-4cfa-946b-ff0c53a3c6b1",
    :via :text,
    :query
    "posterior-variance-as-epistemic-value EIG risk lambda sign category error",
    :excerpt
    "Apply the BMR posterior stddev as an EIG term, SUBTRACTED: `G = risk − λ·EIG`, where `EIG` = the aggregated per-concept posterior stddev (endpoint-count; `{concept → stddev}` the source, absent-concept → 0)."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/predictive-coding-belief-update",
   :to "aif/predictive-entropy-as-ambiguity",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/predictive-coding-belief-update"
   "aif/predictive-entropy-as-ambiguity"],
  :cited
  "Variance with no sensor-variance floor decays toward zero under any informative-observation history, killing the predictive-entropy term in EFE for later ticks.",
  :evidence
  [{:id "e-e46c043f-0199-4e89-938b-55881814cb07",
    :via :text,
    :query
    "predictive-coding-belief-update variance floor predictive-entropy ambiguity EFE",
    :excerpt
    "the variance rule keeps belief uncertainty in a meaningful regime where ambiguity terms in EFE stay informative."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/predictive-coding-belief-update",
   :to "aif/evidence-precision-registry",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/predictive-coding-belief-update"
   "aif/evidence-precision-registry"],
  :cited
  "Mean: μ ← μ + α · Π · ε, where ε = observation − μ, Π is per-channel precision, α is learning rate.",
  :evidence
  [{:id "e-e46c043f-0199-4e89-938b-55881814cb07",
    :via :text,
    :query
    "predictive-coding-belief-update variance floor predictive-entropy ambiguity EFE",
    :excerpt
    "Mean: μ ← μ + α · Π · ε, where ε = observation − μ, Π is per-channel precision, α is learning rate."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/predictive-entropy-as-ambiguity",
   :to "aif/decomposed-prediction-noise",
   :kind :how},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/predictive-entropy-as-ambiguity"
   "aif/decomposed-prediction-noise"],
  :cited
  "Verify your forward model returns predicted variance per observation channel. If not, the entropy isn't computable.",
  :evidence
  [{:id "e-cae646c4-2fda-4f7c-a6c3-95919793718c",
    :via :text,
    :query
    "predictive-entropy-as-ambiguity diagonal Gaussian variance per channel forward model",
    :excerpt
    "For diagonal-covariance (independent channels), `det(Σ) = Π_k σ²_k`."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/scheduled-observer-entrypoint",
   :to "aif/grounded-actuation-not-reobservation",
   :kind :how},
  :rung 2,
  :read
  ["aif/scheduled-observer-entrypoint"
   "aif/grounded-actuation-not-reobservation"],
  :evidence
  [{:id "e-284653d1-ed6a-4cb9-bd13-c0c5ac1c19a4",
    :via :text,
    :query
    "scheduled-observer-entrypoint no-op trap liveness grounded actuation R16",
    :excerpt
    "Couple R10 to R16: a scheduled tick counts as \"live\" only if it can (and does) move the substrate dial via grounded actuation."}],
  :state [:attested-by "claude-15"],
  :cited
  "Couple R10 to R16: a scheduled tick counts as \"live\" only if it can (and does) move the substrate dial via grounded actuation.",
  :review
  {:by "claude-15",
   :at "2026-08-30",
   :note
   "@how warranted: liveness is carried out by grounded actuation (R10 coupled to R16)"},
  :at "2026-08-30",
  :by "zai-2"}
 {:edge
  {:from "aif/shared-kernel-predictive-forward-model",
   :to "aif/expected-free-energy-scorecard",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/shared-kernel-predictive-forward-model"
   "aif/expected-free-energy-scorecard"],
  :cited
  "An AIF agent that scores candidate actions via Expected Free Energy needs a predictive forward model — a function that takes a candidate action and returns the predicted next-state distribution.",
  :evidence
  [{:id "e-f44b210f-1946-4293-97f0-8c3e87478a11",
    :via :text,
    :query
    "shared-kernel-predictive-forward-model deterministic core wrapper bit-equivalence drift",
    :excerpt
    "Separation makes the call surface for downstream EFE consumers clear."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/status-gated-belief-update",
   :to "aif/predictive-coding-belief-update",
   :kind :how},
  :rung 2,
  :read
  ["aif/status-gated-belief-update"
   "aif/predictive-coding-belief-update"],
  :evidence
  [{:id "e-eded2ea1-6fac-457e-b858-63becbc6a08f",
    :via :text,
    :query
    "status-gated belief update multi-instance paused truncated observation",
    :excerpt
    "update-mu stays a pure function over (μ, observation, opts); the gate decides whether to call it."}],
  :state [:attested-by "claude-15"],
  :cited
  "Implement the gate as a case-dispatch in the per-tick post-processing step, *not* inside update-mu itself. update-mu stays a pure function over (μ, observation, opts); the gate decides whether to call it.",
  :review
  {:by "claude-15",
   :at "2026-08-30",
   :note
   "@how warranted: the gate is carried out by calling update-mu"},
  :at "2026-08-30",
  :by "zai-2"}
 {:edge
  {:from "aif/structure-learning-by-model-reduction",
   :to "aif/posterior-variance-as-epistemic-value",
   :kind :how},
  :rung 2,
  :read
  ["aif/structure-learning-by-model-reduction"
   "aif/posterior-variance-as-epistemic-value"],
  :evidence
  [{:id "e-332ec78e-f6b8-4b77-a6e0-bb9f3b787ed2",
    :via :text,
    :query
    "structure-learning-by-model-reduction BMR Dirichlet Bayes factor prune merge",
    :excerpt "The surviving reduced structure IS the concept."}],
  :state :refused,
  :cited
  "Implement Structure Learning as Bayesian Model Reduction over the Dirichlet concentration parameters.",
  :reason
  "the cited text (implement BMR over Dirichlet parameters) does not mention the target; the edge reverses #14, which already records that posterior-variance rests on BMR",
  :review {:by "claude-15", :at "2026-08-30"},
  :at "2026-08-30",
  :by "zai-2"}
 {:edge
  {:from "aif/structured-observation-vector",
   :to "aif/predictive-coding-belief-update",
   :kind :how},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/structured-observation-vector"
   "aif/predictive-coding-belief-update"],
  :cited
  "A stable observation vector is the prerequisite for meaningful precision control and policy scoring.",
  :evidence
  [{:id "e-70024776-1c31-4a14-9466-2ecbe1bbf2cd",
    :via :text,
    :query
    "structured-observation-vector typed normalized feature map session state",
    :excerpt
    "Maintain an explicit (o) as a typed, normalized feature map derived from session state (for example: test status, compile status, diff size, failing spec count, user-stated constraints, time since last anchor, contradiction flags)."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/temporal-depth-beyond-greedy",
   :to "aif/hierarchical-budget-aware-action-selection",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/temporal-depth-beyond-greedy"
   "aif/hierarchical-budget-aware-action-selection"],
  :cited
  "Extend G(π) evaluation to a horizon H > 1 with a temporal discount, AND introduce a two-timescale hierarchy: a slow (strategic) loop that updates priors/preferences and a fast (tactical) loop that acts against them.",
  :evidence
  [{:id "e-23c2c9a2-f889-432c-a88d-95484148ad25",
    :via :text,
    :query
    "temporal-depth-beyond-greedy horizon slow loop fast loop hierarchy",
    :excerpt
    "introduce a two-timescale hierarchy: a slow (strategic) loop that updates priors/preferences and a fast (tactical) loop that acts against them"}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/term-to-channel-traceability",
   :to "aif/evidence-precision-registry",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/term-to-channel-traceability"
   "aif/evidence-precision-registry"],
  :cited
  "Require every G term contribution to declare the observation keys it consumed, the precision channels applied, and the intermediate values used to compute that term, and persist this mapping alongside the score.",
  :evidence
  [{:id "e-451eb212-766f-4d11-b39b-c2374b3aeeea",
    :via :text,
    :query
    "term-to-channel-traceability declare observation keys precision channels provenance",
    :excerpt
    "Require every G term contribution to declare the observation keys it consumed, the precision channels applied, and the intermediate values used to compute that term, and persist this mapping alongside the score."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/two-layer-calibration",
   :to "aif/off-continuity-null-discriminates",
   :kind :how},
  :rung 2,
  :read
  ["aif/two-layer-calibration"
   "aif/off-continuity-null-discriminates"],
  :evidence
  [{:id "e-382a8293-8bbd-449f-886d-8317aa0d5d00",
    :via :text,
    :query
    "two-layer-calibration dynamics-consistency witnessed outcome L1 L2",
    :excerpt
    "L1 dynamics live — the state-sensitive model is 2.55× better than the constant counterfactual at predicting no-move"}],
  :state [:attested-by "claude-15"],
  :cited
  "**L1 = G-vs-G dynamics**: cheap, every cycle, labelled never-value-evidence; it earns its keep as a consistency/sanity layer.",
  :review
  {:by "claude-15",
   :at "2026-08-30",
   :note
   "@how warranted: the null-discriminates check is how L1 earns its keep; evidence records the 2.55x live result"},
  :at "2026-08-30",
  :by "zai-2"}
 {:edge
  {:from "aif/valuation-reads-the-paperwork",
   :to "aif/term-to-channel-traceability",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/valuation-reads-the-paperwork"
   "aif/term-to-channel-traceability"],
  :cited
  "Persist, next to every score, the identity of the text it conditioned on (content hash or verbatim snapshot + date).",
  :evidence
  [{:id "e-2114660b-a9f1-457a-8b1e-47787844f1e7",
    :via :text,
    :query
    "valuation-reads-the-paperwork content hash living documents drift prose",
    :excerpt
    "Treat cross-edit comparisons as cross-version comparisons."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/belief-state-operational-hypotheses",
   :to "writing-coherence/plain-language-thesis",
   :kind :see-also},
  :rung 1,
  :read
  ["aif/belief-state-operational-hypotheses"
   "writing-coherence/plain-language-thesis"],
  :evidence
  [{:id "e-6d22077c-fdec-4524-8c81-2e63efb2b8db",
    :via :tag,
    :query
    "exact export occurrence: aif/belief-state-operational-hypotheses",
    :excerpt
    "aif/belief-state-operational-hypotheses Belief State as Operational Hypotheses futon3a embeddings id title score rank retrieval-source retrieval-method writing-coherence/plain-language-thesis State Top-Level Claims in Plain Language"}],
  :state :refused,
  :cited
  "If the belief state is just the conversation, the agent cannot cleanly update, compare, or falsify its own assumptions.",
  :reason
  "refused (claude-15 2026-08-30): the sole evidence is a context-retrieval event — an embeddings ranking that lists both ids. Co-occurrence in a similarity list is not a stated relation (P-validated-R5 §3e F3: nearness generates hypotheses, it is not proof); rung 1 by exact occurrence, but not a warrant",
  :at "2026-08-30",
  :by "zai-2"}
 {:edge
  {:from "aif/evidence-precision-registry",
   :to "exotic/full-lift-registry",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/evidence-precision-registry" "exotic/full-lift-registry"],
  :cited
  "Represent (Pi_o) as a per-channel precision registry keyed to observation features (and/or evidence sources), and apply it wherever prediction error, scoring, or gating occurs.",
  :evidence
  [{:id "e-ec575c01-804f-4b54-84d1-f5dbf042c03b",
    :via :tag,
    :query "exact export occurrence: aif/evidence-precision-registry",
    :excerpt
    "Also flagged three likely registry/credit prior-art patterns (`aif/candidate-pattern-action-space`, `aif/evidence-precision-registry`, `exotic/full-lift-registry`) that DERIVE car 1 must read before defining the registry, so we don't reinvent (I-4)."}],
  :rung 1,
  :state [:attested-by "claude-15"]}
 {:edge
  {:from "aif/expected-free-energy-scorecard",
   :to "ants/baseline-cyber-ant",
   :kind :see-also},
  :rung 1,
  :read
  ["aif/expected-free-energy-scorecard" "ants/baseline-cyber-ant"],
  :evidence
  [{:id "e-e6c950cc-0c79-496c-ae05-f58762631037",
    :via :tag,
    :query
    "exact export occurrence: aif/expected-free-energy-scorecard",
    :excerpt
    "id title score aif/expected-free-energy-scorecard Expected Free Energy as Multi-Term Scorecard id title score ants/baseline-cyber-ant Baseline Cyber-AIF Ant"}],
  :state :refused,
  :cited
  "A single opaque score encourages post-hoc rationalization and makes tuning indistinguishable from storytelling.",
  :reason
  "refused (claude-15 2026-08-30): the sole evidence is a context-retrieval event — an embeddings ranking that lists both ids. Co-occurrence in a similarity list is not a stated relation (P-validated-R5 §3e F3: nearness generates hypotheses, it is not proof); rung 1 by exact occurrence, but not a warrant",
  :at "2026-08-30",
  :by "zai-2"}
 {:edge
  {:from "aif/admissibility",
   :to "war-machine/advanceability",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read ["aif/admissibility" "war-machine/advanceability"],
  :cited
  "Un-registered `:pursue`, goal-extending `:decompose`, and unagreed operator-exit advances are **inadmissible** — refused (or NAG'd), never scored.",
  :evidence
  [{:id "e-f5a7638c-40f0-4ca2-8ac5-f91c077ed117",
    :via :tag,
    :query "exact export occurrence: aif/admissibility",
    :excerpt
    "(`aif/niche-construction`, `orchestration/consent-gate`, `aif/admissibility`, `war-machine/advanceability`, `orchestration/pattern-warranted-choice-point`)"}],
  :rung 1,
  :state :proposed}
 {:edge
  {:from "aif/belief-aware-risk-term",
   :to "aif/expected-free-energy-scorecard",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/belief-aware-risk-term" "aif/expected-free-energy-scorecard"],
  :cited
  "Compose the pragmatic (risk) EFE term as bias² (squared deviation of predicted mean from preference prior) + variance (predictive uncertainty contribution). Both terms in the same units; both pulling the agent toward predictable closeness to preference.",
  :evidence
  [{:id "e-e96402c5-0bb5-4a2a-83bc-8b259e608806",
    :via :text,
    :query "aif/belief-aware-risk-term hunger precision coupling",
    :excerpt
    "Compose the pragmatic (risk) EFE term as bias² (squared deviation of predicted mean from preference prior) + variance (predictive uncertainty contribution). Both terms in the same units; both pulling the agent toward predictable closeness to preference."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/candidate-pattern-action-space",
   :to "aif/evidence-precision-registry",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/candidate-pattern-action-space"
   "aif/evidence-precision-registry"],
  :cited
  "Define (a) as a constructed candidate set of pattern IDs produced by retrieval plus gating rules, with bounded size and explicit reasons for inclusion and exclusion.",
  :evidence
  [{:id "e-ec575c01-804f-4b54-84d1-f5dbf042c03b",
    :via :tag,
    :query
    "exact export occurrence: aif/candidate-pattern-action-space",
    :excerpt
    "Also flagged three likely registry/credit prior-art patterns (`aif/candidate-pattern-action-space`, `aif/evidence-precision-registry`, `exotic/full-lift-registry`) that DERIVE car 1 must read before defining the registry, so we don't reinvent (I-4)."}],
  :rung 1,
  :state :proposed}
 {:edge
  {:from "aif/currency-before-merge",
   :to "aif/expected-free-energy-scorecard",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/currency-before-merge" "aif/expected-free-energy-scorecard"],
  :cited
  "A system accumulates valuation lanes over time (an EFE blend, a wholeness score, a rollout ΔG, heuristic bonuses), and selection wants one number to rank by.",
  :evidence
  [{:id "e-3fb820aa-3e22-4a67-a514-7ce2478ce63e",
    :via :text,
    :query
    "currency-before-merge exchange rate lanes expected-free-energy-scorecard weights",
    :excerpt
    "A system accumulates valuation lanes over time (an EFE blend, a wholeness score, a rollout ΔG, heuristic bonuses), and selection wants one number to rank by."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/declare-the-conditioning",
   :to "aif/term-to-channel-traceability",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/declare-the-conditioning" "aif/term-to-channel-traceability"],
  :cited
  "Give every valuation surface a three-axis declaration: SENSE (which formal object it claims), SELECTION-SEMANTICS (extremum / amortized sample / bound — over what menu), and CONDITIONING (what situation-slice it consumed).",
  :evidence
  [{:id "e-83b1f41f-204f-4274-b45d-5d2503c39e63",
    :via :text,
    :query "aif/declare-the-conditioning term-to-channel traceability",
    :excerpt
    "Every valuation surface must declare what slice of the situation it consumed; G(candidate) is a lambda — only G(candidate | situation) is a value."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/decomposed-prediction-noise",
   :to "aif/belief-aware-risk-term",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/decomposed-prediction-noise" "aif/belief-aware-risk-term"],
  :cited
  "Practice: emit the decomposition keys alongside the total variance in the trace record. Consumers (validation harness, UI panels, EFE term computations) can read whichever component they need.",
  :evidence
  [{:id "e-c6ac0857-5a23-4c22-866c-2775fb358c6e",
    :via :text,
    :query
    "Decomposed Prediction Noise as Named Variance Contributions k_coverage",
    :excerpt
    "Consumers (validation harness, UI panels, EFE term computations) can read whichever component they need."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/evidence-precision-registry",
   :to "aif/candidate-pattern-action-space",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/evidence-precision-registry"
   "aif/candidate-pattern-action-space"],
  :cited
  "Represent (Pi_o) as a per-channel precision registry keyed to observation features (and/or evidence sources), and apply it wherever prediction error, scoring, or gating occurs.",
  :evidence
  [{:id "e-ec575c01-804f-4b54-84d1-f5dbf042c03b",
    :via :tag,
    :query "exact export occurrence: aif/evidence-precision-registry",
    :excerpt
    "Also flagged three likely registry/credit prior-art patterns (`aif/candidate-pattern-action-space`, `aif/evidence-precision-registry`, `exotic/full-lift-registry`) that DERIVE car 1 must read before defining the registry, so we don't reinvent (I-4)."}],
  :rung 1,
  :state :proposed}
 {:edge
  {:from "aif/expected-free-energy-scorecard",
   :to "structure/mana-allostasis",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/expected-free-energy-scorecard" "structure/mana-allostasis"],
  :cited
  "Compute G(a) as a weighted sum of named terms (risk, ambiguity, information gain, constraint violation, cost, coordination pressure), and store the term breakdown for each candidate.",
  :evidence
  [{:id "e-991c1415-3a8b-4889-a268-5586ba5525f4",
    :via :tag,
    :query
    "exact export occurrence: aif/expected-free-energy-scorecard",
    :excerpt
    "- a pattern cross-reference table tying the v0 design to `structure/hinge-point`, `structure/cook-ting`, `peripherals/canonical-typed-event-vs-side-channel`, `aif/expected-free-energy-scorecard`, and `structure/mana-allostasis`"}],
  :rung 1,
  :state :proposed}
 {:edge
  {:from "aif/experimental-comparison-of-EFE-variants",
   :to "aif/predictive-entropy-as-ambiguity",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/experimental-comparison-of-EFE-variants"
   "aif/predictive-entropy-as-ambiguity"],
  :cited
  "Some EFE terms (notably pattern-EFE-style terms, info-gain composites, multi-source ambiguity terms) have multiple plausible operationalisations.",
  :evidence
  [{:id "e-bc58f837-0de6-4d09-9e64-cbf931592556",
    :via :text,
    :query
    "experimental-comparison-of-EFE-variants term variant ambiguity predictive entropy",
    :excerpt
    "Some EFE terms (notably pattern-EFE-style terms, info-gain composites, multi-source ambiguity terms) have multiple plausible operationalisations."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/free-energy-as-tick-scalar",
   :to "aif/expected-free-energy-scorecard",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/free-energy-as-tick-scalar"
   "aif/expected-free-energy-scorecard"],
  :cited
  "AIF computes two distinct quantities. **G** (Expected Free Energy) is per-candidate-action, drives policy. **F** (Variational Free Energy) is per-tick, summarises how surprised the agent currently is by its observations.",
  :evidence
  [{:id "e-e9c000da-7ebf-4a67-9d7b-f35db6d3443c",
    :via :text,
    :query
    "free-energy-as-tick-scalar G-per-candidate is not a substitute",
    :excerpt
    "And G-per-candidate is not a substitute: G is about the future; F is about the present."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/grounded-actuation-not-reobservation",
   :to "aif/scheduled-observer-entrypoint",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/grounded-actuation-not-reobservation"
   "aif/scheduled-observer-entrypoint"],
  :cited
  "That witness feeds the next decision — the deliberator re-ranks on the substrate dial. Gate \"closed = ✓\" on a real dial move (1/2 → 2/2), NEVER on the enactor merely firing.",
  :evidence
  [{:id "e-d0b7eb83-bbbd-4bfb-ade4-71b5d370e28d",
    :via :text,
    :query
    "grounded-actuation re-observe-your-own-construction mirror not an act",
    :excerpt
    "Gate \"closed = ✓\" on a real dial move (1/2 → 2/2), NEVER on the enactor merely firing."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/hierarchical-and-temporal-depth",
   :to "aif/policy-precision-commitment-temperature",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/hierarchical-and-temporal-depth"
   "aif/policy-precision-commitment-temperature"],
  :cited
  "(3) longer policies use a named temporal discount of their own, rather than overloading the commitment dial (R14) to stand in for time preference.",
  :evidence
  [{:id "e-537dec82-822e-44a0-857d-40694bbf02e8",
    :via :text,
    :query "overloading the commitment dial R14 temporal discount",
    :excerpt
    "the commitment dial doing duty as a temporal discount, a tactical outcome read as if it were strategic evidence — collapses the levels back into one"}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/hierarchical-budget-aware-action-selection",
   :to "budgeted-action-selection/mana-gated-work",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/hierarchical-budget-aware-action-selection"
   "budgeted-action-selection/mana-gated-work"],
  :cited
  "A multi-agent (or multi-band, or hierarchical) AIF setup gives each agent or sub-agent local autonomy: each scores candidate actions and selects via softmax. But the candidates often consume shared resources — setup budget for capability activation, engagement-budget for per-instance participation, attention budget across the agent's responsibility area.",
  :evidence
  [{:id "e-06df1277-45cc-45be-98c6-6d20b2d640d1",
    :via :text,
    :query
    "hierarchical-budget resolve-conflicts feasibility-penalty band priority",
    :excerpt
    "each scores candidate actions and selects via softmax. But the candidates often consume shared resources — setup budget for capability activation, engagement-budget for per-instance participation, attention budget across the agent's responsibility area."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/interoceptive-tripwires",
   :to "aif/structured-observation-vector",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/interoceptive-tripwires" "aif/structured-observation-vector"],
  :cited
  "The loop's observation channel is task-level only: it observes the world it acts on (job outcomes, verdicts, witnesses, QA), while its own machinery is a hidden state with no likelihood mapping.",
  :evidence
  [{:id "e-ef28b7b2-cb6d-4a1c-8c08-12b36a08a796",
    :via :text,
    :query "interoceptive-tripwires freeze record park summon",
    :excerpt
    "The loop's observation channel is task-level only: it observes the world it acts on (job outcomes, verdicts, witnesses, QA), while its own machinery is a hidden state with no likelihood mapping."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/niche-construction",
   :to "ants/baseline-cyber-ant",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read ["aif/niche-construction" "ants/baseline-cyber-ant"],
  :cited
  "An active-inference agent (e.g. the futon War Machine) reads a substrate of typed vertices and edges, computes T / ∇T / ΔT or analogues, and ranks actions.",
  :evidence
  [{:id "e-e1980870-c223-4ec8-a1a8-cc06762698c3",
    :via :tag,
    :query "exact export occurrence: aif/niche-construction",
    :excerpt
    "Strong matches already — `ants/baseline-cyber-ant` (the parliament line literally), the `aif/` free-energy patterns, `aif/niche-construction` + `musn/expensive-move-consent` (INV-G)"}],
  :rung 1,
  :state :proposed}
 {:edge
  {:from "aif/no-self-certification",
   :to "aif/interoceptive-tripwires",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read ["aif/no-self-certification" "aif/interoceptive-tripwires"],
  :cited
  "The only thing that breaks it is a referent the maker had no hand in: an external witness, a settled re-read of a world it does not control, a tag it cannot forge.",
  :evidence
  [{:id "e-75ef042f-5000-4e48-aec1-f70fd23e12a1",
    :via :text,
    :query
    "no-self-certification untagged never counts verdict manufactured",
    :excerpt
    "Enforce this STRUCTURALLY — with tags the apparatus cannot fake (independence, measured-vs-fallback, settled-vs-transient) and a verdict that counts only those tags — not by vigilance."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/off-continuity-null-discriminates",
   :to "aif/measurement-window-hygiene",
   :kind :how},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/off-continuity-null-discriminates"
   "aif/measurement-window-hygiene"],
  :cited
  "Mask-in still requires the null be CLEAN: settled, two distinct-as-of scans agreeing within ε ([[measurement-window-hygiene]]); a confounded null discriminates nothing.",
  :evidence
  [{:id "e-24708ed2-3074-4253-b9a5-17ac92c33989",
    :via :text,
    :query
    "off-continuity null discriminates clean-target wall dual prediction",
    :excerpt
    "Mask-in still requires the null be CLEAN: settled, two distinct-as-of scans agreeing within ε ([[measurement-window-hygiene]]); a confounded null discriminates nothing."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/placeholder-is-load-bearing",
   :to "aif/term-to-channel-traceability",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/placeholder-is-load-bearing"
   "aif/term-to-channel-traceability"],
  :cited
  "Before deleting, replay the deletion against the arena including its placeholders and defaults; sequence deletions after placeholder handling; mark every placeholder self-describing (provenance field), never bare.",
  :evidence
  [{:id "e-9ed973f1-edbe-4073-a7c4-25aabbec4a60",
    :via :text,
    :query "placeholder-is-load-bearing honest map flattering counter",
    :excerpt
    "mark every placeholder self-describing (provenance field), never bare."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/policy-precision-commitment-temperature",
   :to "aif/expected-free-energy-scorecard",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/policy-precision-commitment-temperature"
   "aif/expected-free-energy-scorecard"],
  :cited
  "Maintain (tau) as a commitment temperature that modulates softmax over (-G/tau), coupled to diagnostic signals (recent regressions, uncertainty in (mu), contradiction rate, time pressure, repeated test failures).",
  :evidence
  [{:id "e-f2a87cbe-794e-4f83-b465-600c988887f4",
    :via :text,
    :query
    "policy-precision-commitment-temperature softmax abstain discriminability G-spread",
    :excerpt
    "Maintain a commitment temperature that modulates how strongly the policy commits to low-G choices."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/posterior-variance-as-epistemic-value",
   :to "aif/predictive-entropy-as-ambiguity",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/posterior-variance-as-epistemic-value"
   "aif/predictive-entropy-as-ambiguity"],
  :cited
  "Relation to `predictive-entropy-as-ambiguity`: THAT pattern is the genuine ambiguity (+) term (forward observation-distribution entropy, an irreducible-noise cost). THIS is the EIG (−) term (reducible model uncertainty). Distinct quantities, distinct signs — never collapse them into one leg.",
  :evidence
  [{:id "e-a4e4674e-d764-417a-a432-f93f2e2f86c2",
    :via :text,
    :query
    "posterior-variance-as-epistemic-value predictive-entropy-as-ambiguity distinct signs collapse",
    :excerpt
    "Distinct quantities, distinct signs — never collapse them into one leg."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/predictive-coding-belief-update",
   :to "futon-theory/four-types",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/predictive-coding-belief-update" "futon-theory/four-types"],
  :cited
  "The mean rule realises predictive coding (Friston et al.); the variance rule keeps belief uncertainty in a meaningful regime where ambiguity terms in EFE stay informative.",
  :evidence
  [{:id "e-03b5a03b-e30b-4d78-89f8-1fd726fcb6d1",
    :via :tag,
    :query
    "exact export occurrence: aif/predictive-coding-belief-update",
    :excerpt
    "| 3 | `aif/predictive-coding-belief-update` | The four turns | R/E/P/L maps onto predictive-coding belief-update mechanics |\n| 4 | `futon-theory/four-types` | Four-types cycle | Canonical pheno / geno / exo / xeno from which the bit-shift evo"}],
  :rung 1,
  :state :proposed}
 {:edge
  {:from "aif/predictive-entropy-as-ambiguity",
   :to "aif/shared-kernel-predictive-forward-model",
   :kind :why},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/predictive-entropy-as-ambiguity"
   "aif/shared-kernel-predictive-forward-model"],
  :cited
  "Verify your forward model returns predicted variance per observation channel. If not, the entropy isn't computable.",
  :evidence
  [{:id "e-d9ee0a42-9a13-4eae-aab4-53947b4cb33f",
    :via :text,
    :query
    "predictive-entropy-as-ambiguity forward model predicted variance per channel entropy computable",
    :excerpt
    "Verify your forward model returns predicted variance per observation channel. If not, the entropy isn't computable."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/scheduled-observer-entrypoint",
   :to "aif/interoceptive-tripwires",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/scheduled-observer-entrypoint" "aif/interoceptive-tripwires"],
  :cited
  "Add the liveness assertion to the tick harness: `zero-delta-over-N` → flag, with the run count surfaced (never silently green).",
  :evidence
  [{:id "e-4c79ce10-17fa-42de-923d-416814201f32",
    :via :text,
    :query
    "scheduled-observer-entrypoint liveness assertion zero-delta-over-N no-op trap",
    :excerpt
    "Add the liveness assertion to the tick harness: `zero-delta-over-N` → flag, with the run count surfaced (never silently green)."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/shared-kernel-predictive-forward-model",
   :to "aif/temporal-depth-beyond-greedy",
   :kind :how},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/shared-kernel-predictive-forward-model"
   "aif/temporal-depth-beyond-greedy"],
  :cited
  "Add a second arity for the forward-model seam: `(kernel state ext seed opts candidate-actions)` — supplied actions skip live action selection.",
  :evidence
  [{:id "e-0e34f09f-6619-4899-80d3-44efd4527c66",
    :via :text,
    :query
    "shared-kernel bit-equivalence kernel wrapper pure deterministic second arity",
    :excerpt
    "The predictive forward model then becomes a thin probabilistic wrapper around the same kernel: call kernel with candidate, add Gaussian noise (or other principled uncertainty representation), produce next-state distribution."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/status-gated-belief-update",
   :to "aif/free-energy-as-tick-scalar",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/status-gated-belief-update" "aif/free-energy-as-tick-scalar"],
  :cited "The trace record emits :mu for all instances every tick.",
  :evidence
  [{:id "e-6e323c36-fabd-44ea-84b4-4c0f83a485f9",
    :via :text,
    :query
    "status-gated-belief-update engagement status observation availability truncated freezes",
    :excerpt
    "The trace record emits :mu for all instances every tick."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/structure-learning-by-model-reduction",
   :to "aif/currency-before-merge",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/structure-learning-by-model-reduction"
   "aif/currency-before-merge"],
  :cited
  "Respect `currency-before-merge`: a merge that collapses two concepts is a STRUCTURAL edit, not merely a count sum — it must honour any accumulation-currency invariants downstream.",
  :evidence
  [{:id "e-5cd76000-beac-4607-ae73-a5b2a4a0a085",
    :via :text,
    :query
    "structure-learning-by-model-reduction Bayesian model reduction Dirichlet Bayes factor",
    :excerpt
    "Respect `currency-before-merge`: a merge that collapses two concepts is a STRUCTURAL edit, not merely a count sum — it must honour any accumulation-currency invariants downstream."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/structured-observation-vector",
   :to "aif/evidence-precision-registry",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/structured-observation-vector"
   "aif/evidence-precision-registry"],
  :cited
  "A stable observation vector is the prerequisite for meaningful precision control and policy scoring.",
  :evidence
  [{:id "e-264d1c43-e694-4488-b475-dd46f542aab1",
    :via :text,
    :query
    "structured-observation-vector typed normalized feature map precision control policy scoring",
    :excerpt
    "A stable observation vector is the prerequisite for meaningful precision control and policy scoring."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/temporal-depth-beyond-greedy",
   :to "aif/hierarchical-and-temporal-depth",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/temporal-depth-beyond-greedy"
   "aif/hierarchical-and-temporal-depth"],
  :cited
  "Extend G(π) evaluation to a horizon H > 1 with a temporal discount, AND introduce a two-timescale hierarchy: a slow (strategic) loop that updates priors/preferences and a fast (tactical) loop that acts against them.",
  :evidence
  [{:id "e-0e2cda23-9d80-4cff-ac57-a96372582495",
    :via :text,
    :query
    "temporal-depth-beyond-greedy rollout horizon fast slow two-timescale hierarchy",
    :excerpt
    "introduce a two-timescale hierarchy: a slow (strategic) loop that updates priors/preferences and a fast (tactical) loop that acts against them"}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/term-to-channel-traceability",
   :to "aif/expected-free-energy-scorecard",
   :kind :how},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/term-to-channel-traceability"
   "aif/expected-free-energy-scorecard"],
  :cited
  "Require every G term contribution to declare the observation keys it consumed, the precision channels applied, and the intermediate values used to compute that term, and persist this mapping alongside the score.",
  :evidence
  [{:id "e-e8e638c6-f673-4c56-b0e1-ae0f267464bc",
    :via :text,
    :query
    "term-to-channel-traceability dominant-channels provenance term-id final-contribution",
    :excerpt
    "Require every G term contribution to declare the observation keys it consumed, the precision channels applied, and the intermediate values used to compute that term, and persist this mapping alongside the score."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/two-layer-calibration",
   :to "aif/no-self-certification",
   :kind :why},
  :by "zai-2",
  :at "2026-08-30",
  :read ["aif/two-layer-calibration" "aif/no-self-certification"],
  :cited
  "The split was forced by discovering the calibration was self-certifying (`[[aif/no-self-certification]]`).",
  :evidence
  [{:id "e-09e5d48a-26a4-4d7c-84cf-9a36482afe1c",
    :via :text,
    :query
    "two-layer-calibration L1 G-vs-G dynamics L2 witnessed outcome prior equals value",
    :excerpt
    "The split was forced by discovering the calibration was self-certifying (`[[aif/no-self-certification]]`)."}],
  :rung 2,
  :state :proposed}
 {:edge
  {:from "aif/valuation-reads-the-paperwork",
   :to "aif/evidence-precision-registry",
   :kind :see-also},
  :by "zai-2",
  :at "2026-08-30",
  :read
  ["aif/valuation-reads-the-paperwork"
   "aif/evidence-precision-registry"],
  :cited
  "Persist, next to every score, the identity of the text it conditioned on (content hash or verbatim snapshot + date).",
  :evidence
  [{:id "e-e44ee550-b236-4497-b765-e3b74aed4540",
    :via :text,
    :query
    "valuation-reads-the-paperwork content hash or verbatim snapshot date scoring",
    :excerpt
    "Persist, next to every score, the identity of the text it conditioned on (content hash or verbatim snapshot + date)."}],
  :rung 2,
  :state :proposed}]
