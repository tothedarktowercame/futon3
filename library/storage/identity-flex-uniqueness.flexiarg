@flexiarg storage/identity-flex-uniqueness
@title Identity Flexibility vs Uniqueness
@sigils [🐚/之]
@keywords identity, uuid, external-id, uniqueness
@audience storage maintainers, futon1a builders
@tone formal-analytic
@factor Keen investigation (dhammavicaya)
@references [stack-coherence/evidence-ledger storage/identity-uniqueness]
@status [status[proposed] evidence[holes/missions/M-futon1a-evidence.md#L96] evidence[holes/missions/M-futon1a-evidence.md#L134]]
@why no problem-corpus source matched (@holds-at absent; holds-token and id-text searches over every library/problems node -- legacy 6, L5 dossier 20 incl. TRACE, L5 record/ruling 6 -- found no match). Negative claim per the committed check futon2 holes labs library-loop runs l6_no_source_check.py, receipt runs/L6-no-source-check.edn. Rationale not invented (L7 rationale-backfill, zai-1, 2026-09-05)
@how own mechanism: if you want flexible external IDs but also unique identity, coerce to UUIDs and reject duplicate external mappings before write (source: this pattern's ! conclusion line; L7 rationale-backfill, zai-1, 2026-09-05)

! conclusion: 
  if you want flexible external IDs but also unique identity, coerce to UUIDs and reject duplicate external mappings before write

  + context: futon1 enforced UUID IDs and external-id uniqueness to prevent duplicates.

  + IF:
    you want flexible external identifiers for integration

  + HOWEVER:
    you also need a single identity per entity

  + THEN:
    accept external IDs only through a unique mapping and require UUIDs as the stored identity

  + BECAUSE:
    ambiguous externals created duplicate entities and long-term drift

  + NEXT-STEPS:
    next[Define the external-id namespace rules and conflict handling.]
    next[Add tests that reject duplicate external-id usage across types.]
