@flexiarg storage/persistence-speed-mirroring
@title Persistence-Speed Mirroring
@sigils [🍵/文]
@keywords persistence, speed, datascript, xtdb, mirroring
@audience storage maintainers, futon1a builders
@tone formal-analytic
@factor Keen investigation (dhammavicaya)
@references [stack-coherence/evidence-ledger storage/reproducible-mirroring]
@status [status[proposed] evidence[holes/features/futon1.devmap#L1] evidence[holes/missions/M-futon1a-evidence.md#L106] evidence[holes/missions/M-futon1a-evidence.md#L138]]
@why no problem-corpus source matched (@holds-at absent; holds-token and id-text searches over every library/problems node -- legacy 6, L5 dossier 20 incl. TRACE, L5 record/ruling 6 -- found no match). Negative claim per the committed check futon2 holes labs library-loop runs l6_no_source_check.py, receipt runs/L7-no-source-check.edn. Rationale not invented (L7 rationale-backfill, zai-1, 2026-09-05)
@how own mechanism: if you want durability but also fast access, mirror XTDB into Datascript and enforce reproducible alignment as a hard contract (source: this pattern's ! conclusion line; L7 rationale-backfill, zai-1, 2026-09-05)

! conclusion: 
  if you want durability but also fast access, mirror XTDB into Datascript and enforce reproducible alignment as a hard contract

  + context: futon1 repeatedly fixed XTDB mirroring to prevent drift and metadata loss.

  + IF:
    you want the database to be persistent

  + HOWEVER:
    you also want access to be fast

  + THEN:
    mirror XTDB into Datascript with explicit mirroring rules and tests

  + BECAUSE:
    mirroring without enforcement creates drift across restarts and environments

  + NEXT-STEPS:
    next[Define the mirror invariants and verify them on every restart cycle.]
    next[Add tests that assert metadata parity between XTDB and Datascript.]
