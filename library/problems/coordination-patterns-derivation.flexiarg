@flexiarg problems/coordination-patterns-derivation
@title Problem node: Agent coordination needs domain-specific patterns derived from futon-theory
@keywords problem-node, section-grain, coordination
@audience futon stack operators, library authors
@tone plain
@style pattern
@why section-doc authority: futon3 library/coordination/INDEX.md, opening paragraph (quoted below; no library pattern grounds this node)
@how no mechanism claimed at problem grain; the section's own patterns are the mechanisms

! conclusion: This is a problem-stating node at section grain for the coordination section, from the section's own committed documentation: Domain-specific patterns for the futon3 agent coordination pipeline, derived from futon-theory via the derivation xenotype.

  + context: Minted by library-loop row L18 v3, grounding members of the fixed served-refused worklist that the section documentation names. Source: futon3 library/coordination/INDEX.md, opening paragraph.

  + IF:
    Work happens in or consumes the coordination section's patterns.

  + HOWEVER:
    The section's own documentation states the problem: "Domain-specific patterns for the futon3 agent coordination pipeline, derived from futon-theory via the derivation xenotype (IDENTIFY -> MAP -> DERIVE -> VERIFY -> INSTANTIATE)" (source: futon3 library/coordination/INDEX.md, opening paragraph)

  + THEN:
    This node is the landing point for @why edges from the coordination worklist members (mechanical pass, row L18 v3); per-pattern questions route to the section documentation, not to invention here.

  + BECAUSE:
    A section whose documentation states one shared problem carries one problem node, not per-pattern copies (the L11/L15/L18 cost finding).

    + evidence: futon3 library/coordination/INDEX.md, opening paragraph; the L18 v3 complete search receipt runs/L18-remainder.edn.
