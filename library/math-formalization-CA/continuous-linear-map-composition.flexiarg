@flexiarg math-formalization-CA/continuous-linear-map-composition
@title Continuous Linear Map Composition Properties
@sigils [🔗/合]
@keywords ContinuousLinearMap, composition, operator, bounded, apply
@audience lean-provers, functional-analysis-students
@tone heuristic
@difficulty routine
@subjects applied
@instantiates math-strategy/compose-independent-lemmas
@examples m98J03
@why no problem-corpus source matched (@holds-at absent; holds-token and id-text searches over every node in [problems futon-theory] found no match). Negative claim per the committed check futon2 holes labs library-loop runs l6_no_source_check.py, receipt runs/L8-no-source-check.edn. Rationale not invented (L8 rationale-backfill, zai-1, 2026-09-05)
@how own mechanism: Prove properties of composed bounded operators via ContinuousLinearMap API. (source: this pattern's ! conclusion line; L8 rationale-backfill, zai-1, 2026-09-05)

! conclusion: 
  Prove properties of composed bounded operators via ContinuousLinearMap API.

  + IF: Goal involves composition of bounded linear operators.

  + HOWEVER: Operator composition in Lean is Module.End.mul, not function composition.

  + THEN: Use ContinuousLinearMap.comp_apply to unfold, then simp.
    + LEAN:
      api[ContinuousLinearMap.comp_apply]
      tactic[simp [ContinuousLinearMap.comp_apply]]

  + BECAUSE: The bundled operator API needs explicit unfolding before standard tactics work.
