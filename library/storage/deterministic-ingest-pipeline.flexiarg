@flexiarg storage/deterministic-ingest-pipeline
@title Deterministic Ingest Pipeline
@sigils [📁/义]
@keywords nlp, ingest, tokenise, tag, chunk, recognise, deterministic
@audience futon1a builders, ingest maintainers
@tone formal-analytic
@factor Keen investigation (dhammavicaya)
@references [stack-coherence/evidence-ledger]
@status [status[proposed] evidence[holes/features/futon1.devmap#L40] evidence[holes/features/futon1.devmap#L89]]
@why no problem-corpus source matched (@holds-at absent; holds-token and id-text searches over every library/problems node -- legacy 6, L5 dossier 20 incl. TRACE, L5 record/ruling 6 -- found no match). Negative claim per the committed check futon2 holes labs library-loop runs l6_no_source_check.py, receipt runs/L7-no-source-check.edn. Rationale not invented (L7 rationale-backfill, zai-1, 2026-09-05)
@how own mechanism: provide deterministic ingestion (tokenise → tag → chunk → recognise) so graph construction is reproducible (source: this pattern's ! conclusion line; L7 rationale-backfill, zai-1, 2026-09-05)

! conclusion: 
  provide deterministic ingestion (tokenise → tag → chunk → recognise) so graph construction is reproducible

  + context: futon1’s devmap defines a deterministic NLP interface as the foundation for ingest.

  + IF:
    ingest depends on text processing and recognition stages

  + HOWEVER:
    non-deterministic NLP yields irreproducible graph states

  + THEN:
    formalize the pipeline stages and require deterministic outputs in tests

  + BECAUSE:
    reproducible ingest is required for a deterministic storage substrate

  + NEXT-STEPS:
    next[Document the pipeline as a public API with input/output shapes.]
    next[Add fixtures that must pass identically across environments.]
