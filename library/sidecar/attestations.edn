[{:edge
  {:from "sidecar/explicit-promotion-to-facts",
   :to "sidecar/validation-enforcement-gate",
   :kind :how},
  :rung 1,
  :read
  ["sidecar/explicit-promotion-to-facts"
   "sidecar/fact-lifecycle-event-types"],
  :evidence
  [{:id "e-8931a4eb-6509-428a-9d11-db2aa4ef552f",
    :via :tag,
    :query
    "exact live occurrence: sidecar/explicit-promotion-to-facts",
    :excerpt
    "memory-retrieval promotion-gate experimental-isolation lifecycle challenge"}],
  :refused-reason
  "warrant is a tag-list mention (f11-scribe memory record); needs a reader to say whether a tag co-occurrence states use",
  :state :proposed,
  :corpus
  {:store "http://127.0.0.1:7073",
   :basis {:count 191738, :max-at "2026-08-30T19:35:18.668154294Z"},
   :reflection-rule
   {:version 2,
    :worker-seats ["zai-1" "zai-2"],
    :worker-author :evidence-author-in-worker-seats,
    :spider-agent "codex-20",
    :agency-job
    :invoke-complete-or-job-envelope-naming-worker-or-spider-agent,
    :self-text :spider-self-text-prompt-markers}},
  :cited
  "Require a promote action that passes the validation gate and appends a promotion record.",
  :reviewed-by "claude-15",
  :at "2026-08-30",
  :reviewed-at "2026-08-31",
  :by "zai-2",
  :warrant-refused
  {:by "library-build-loop", :at "2026-09-01", :note "second read for worklist item L3 (futon3/holes/labs/library-contract/worklist.edn). Reader is the Claude worker driven by library-build-loop.sh; it is not a registered Agency seat, so the loop's own name is used rather than borrowing claude-13, who was named in the hold but did not do this read. Read the whole of e-8931a4eb at http://127.0.0.1:7073/api/alpha/evidence/e-8931a4eb-6509-428a-9d11-db2aa4ef552f, not only the tag-list excerpt the spider recorded. The record carries ONE endpoint, not two: :evidence/subject is sidecar/explicit-promotion-to-facts and the :evidence/body names no other pattern id, so it can say the source pattern is live but cannot say a relation to the target holds. On this edge specifically: the tag :promotion-gate names the promotion review that refused a deposited memory, which is the SOURCE pattern's own subject, and the body's remedy is a query-time eligibility predicate (\"intersect results with frozen eligible ids and required reviewed state\"), whereas library/sidecar/validation-enforcement-gate.flexiarg:19 defines a write-time check of required fields, enum constraints and store boundaries. Different act, same word. A tag co-occurrence is not a statement of use. Warrant refused, EDGE NOT refused: the relation is still asserted by the source file's own THEN clause at library/sidecar/explicit-promotion-to-facts.flexiarg:21 (the :cited string), and the directive at library/sidecar/explicit-promotion-to-facts.flexiarg:8 stays, unwarranted, until some reading carries it. What would carry it: a record whose body names both endpoints, or names a promote path and the write-time validator it goes through."},
  :review-note
  "held for second read (claude-13): warrant is a tag-list mention"}
 {:edge
  {:from "sidecar/explicit-promotion-to-facts",
   :to "sidecar/fact-lifecycle-event-types",
   :kind :see-also},
  :rung 1,
  :read
  ["sidecar/explicit-promotion-to-facts"
   "sidecar/fact-lifecycle-event-types"],
  :evidence
  [{:id "e-8931a4eb-6509-428a-9d11-db2aa4ef552f",
    :via :tag,
    :query
    "exact live occurrence: sidecar/explicit-promotion-to-facts",
    :excerpt
    "memory-retrieval promotion-gate experimental-isolation lifecycle challenge"}],
  :refused-reason
  "warrant is a tag-list mention (f11-scribe memory record); needs a reader to say whether a tag co-occurrence states use",
  :state :proposed,
  :corpus
  {:store "http://127.0.0.1:7073",
   :basis {:count 191738, :max-at "2026-08-30T19:35:18.668154294Z"},
   :reflection-rule
   {:version 2,
    :worker-seats ["zai-1" "zai-2"],
    :worker-author :evidence-author-in-worker-seats,
    :spider-agent "codex-20",
    :agency-job
    :invoke-complete-or-job-envelope-naming-worker-or-spider-agent,
    :self-text :spider-self-text-prompt-markers}},
  :cited "Define promotion log records with rationale and decided_by.",
  :reviewed-by "claude-15",
  :at "2026-08-30",
  :reviewed-at "2026-08-31",
  :by "zai-2",
  :warrant-refused
  {:by "library-build-loop", :at "2026-09-01", :note "second read for worklist item L3 (futon3/holes/labs/library-contract/worklist.edn). Reader is the Claude worker driven by library-build-loop.sh; it is not a registered Agency seat, so the loop's own name is used rather than borrowing claude-13, who was named in the hold but did not do this read. Read the whole of e-8931a4eb at http://127.0.0.1:7073/api/alpha/evidence/e-8931a4eb-6509-428a-9d11-db2aa4ef552f, not only the tag-list excerpt the spider recorded. The record carries ONE endpoint, not two: :evidence/subject is sidecar/explicit-promotion-to-facts and the :evidence/body names no other pattern id, so it can say the source pattern is live but cannot say a relation to the target holds. On this edge specifically: the tag :lifecycle and the record's phrase \"the same memory lifecycle as eligibility\" mean the APM memory-proposal lifecycle (deposited, proposed, review-refused) inside frame 11, not the sidecar fact lifecycle event enum (fact, warrant, retired) that library/sidecar/fact-lifecycle-event-types.flexiarg:8 and :19 define; the record names no lifecycle event type and no rationale or actor metadata attached to one. A tag co-occurrence is not a statement of use. Warrant refused, EDGE NOT refused: the :cited NEXT-STEPS bullet at library/sidecar/explicit-promotion-to-facts.flexiarg:31 still asserts it and the directive at library/sidecar/explicit-promotion-to-facts.flexiarg:9 stays, unwarranted. What would carry it: a record naming a promotion log record together with a fact/warrant/retired lifecycle event."},
  :review-note
  "held for second read (claude-13): warrant is a tag-list mention"}]
