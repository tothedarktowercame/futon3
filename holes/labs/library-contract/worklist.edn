{:schema :wm/worklist-v1,
 :title
 "library-contract: populate the pattern library's rationale layer so cascades can be formed from it",
 :ledger-of
 "futon2/holes/problems/P-organise-the-library.md and P-validated-R5.md; standard futon3/README-flexiarg.md §5a",
 :method
 "futon2/holes/TN-validation-method.md -- same discipline as wm-contract: one row per invocation, author != reviewer, decisions recorded with arms and run where runnable",
 :classes #{:W :D :J :C :E :V},
 :statuses #{:needs-joe :open :done :done-unreviewed :blocked},
 :items
 [{:statement
   "Vocabulary gap found at the wave-2 gate: the lint's :refused means the EDGE leaves the pattern file; refusing a WARRANT is a different act (:proposed plus a review note). One word for two acts (P-organise-the-library.md, WAVE 2 GATED 12:10Z).",
   :evidence
   {:sha "a3bff43",
    :schema
    "library/.spider/attestation-schema.edn -- :warrant-refused {:by :at :note} beside :state :proposed; :state :refused unchanged",
    :lint
    ["checks/library_graph_lint.clj:169 valid-warrant-refusal?"
     "checks/library_graph_lint.clj:195 refusal-act-failures (:warrant-refusal-requires-proposed-state)"
     "checks/library_graph_lint.clj:180 non-keyword attestation key is malformed"
     "summary counts :edge-refusals and :warrant-refusals separately"],
    :tests
    ["test/library_graph_lint_test.clj:159 edge-refusal-and-warrant-refusal-are-different-acts (4 planted cases: warrant-refusal keeps the directive and passes; edge-refusal with the directive still present fails :refused-edge-still-present and without it passes; both acts on one record fails; a warrant-refusal without a note is malformed)"
     "test/library_graph_lint_test.clj:185 non-keyword-attestation-key-is-malformed"],
    :reclassification
    "library/.spider/refusal-reclassification-2026-09-01.edn -- all 5 prior :refused records are edge-refusals by act (no surviving directive in any of the 5 source patterns); grounds quoted per record; aif rows at :607 and :646 give a warrant-shaped ground, recorded not revised",
    :repair
    "library/writing-coherence/attestations.edn:95 row :reason held unescaped quotes around \"meta-lede\", which split the EDN string and left `meta-lede` as a symbol key; escaped, and the linter now rejects the class",
    :not-touched
    "library/sidecar/attestations.edn rows 0 and 1 (:refused-reason, :state :proposed) left for L3's second read",
    :gates
    {:clj-kondo
     "exit 0, errors 0 warnings 0 (the one :info at library_graph_lint.clj:234 pre-dates this change)",
     :check-parens "exit 0, OK",
     :library-graph-lint-test
     "exit 0, 6 tests / 26 assertions / 0 failures",
     :spider-runner-test
     "exit 0, 3 tests / 11 assertions / 0 failures",
     :section-lint
     "aif pass? true, :edge-refusals 4; writing-coherence 4 failures and sidecar 2 failures are :evidence-excerpt-mismatch from the pinned fixture holding aif's 56 records only -- identical counts measured on the pre-change linter, so not a regression"}},
   :acceptance
   "checks/library_graph_lint.clj and the attestation schema distinguish edge-refusal (edge leaves the file) from warrant-refusal (edge stays :proposed, warrant recorded as rejected with a note). A test exercises both acts on planted records. Existing 5 :refused records re-classified with pointers, or shown to all be true edge-refusals. clj-kondo, check-parens, and test/library_graph_lint_test.clj green.",
   :review
   "Read a3bff43 in full and confirmed its author differs from the reviewer. Reproduced clj-kondo exit 0 (0 errors, 0 warnings), check-parens exit 0 (OK), library_graph_lint_test exit 0 (6 tests, 26 assertions), spider_runner_test exit 0 (3 tests, 11 assertions), and the C88-pinned aif section lint exit 0 (1247 files; edges :why 86, :how 27, :see-also 136; 4 edge-refusals, 0 warrant-refusals, 0 failures). Confirmed all 5 reclassified refused edges have no surviving directive in their source pattern. No .flexiarg file was changed by the evidence commit, so it authored no spider @why, post-hoc edge, or similarity-inferred edge.",
   :status :done,
   :id :L1,
   :class :C,
   :reviewed-by "codex",
   :reviewed-at "2026-09-01T19:21:47Z",
   :covers-key :none,
   :owner :any}
  {:statement
   "Reflection-class rule v2 misses an author describing their own references at authoring time: wave 2 rejected one warrant on exactly this (the pattern's author describing its own references). A v3 candidate is named in the record but not written.",
   :review-finding
   "checks/library_graph_lint.clj:278 authoring-turn? does not establish that the evidence record's author authored the named file; it only finds any authoring verb within 120 characters of any .flexiarg path. Reproducer: an evidence body containing `Alice wrote library/aif/example.flexiarg; I independently reviewed how it uses the cited pattern.` returns true, so rung-two-warrant? can discard genuine external corroboration. Add a counter-fixture where the record author describes somebody else's authored pattern file, make v3 leave it non-reflection, and then recompute the C88 audit. Review gates otherwise reproduced: clj-kondo exit 0 (0 errors, 0 warnings), check-parens exit 0 (OK), library_graph_lint_test exit 0 (7 tests, 35 assertions), spider_runner_test exit 0 (5 tests, 23 assertions), system-coherence pinned section lint exit 0 (1247 files; :why 86, :how 27, :see-also 136; 0 failures), and worklist_check exit 0. Evidence commit changes no .flexiarg files, so it adds no spider-authored @why, post-hoc, or similarity-inferred edges.",
   :evidence
   {:cache-pin
    "evidence-cache-path now takes the rule version from the corpus pin, not the constant, so bumping to v3 does not orphan a v2-pinned index: system-coherence's basis 191738 still resolves to /tmp/futon3-spider-evidence-occurrences-v3-191738-d8f61810a34eb73a.edn and lints pass? true",
    :not-touched
    "library/system-coherence/attestations.edn -- the wave-2 record's :review-note still says \"v3 candidate\"; rewording it is a wave-2 record edit no acceptance asked for",
    :tests
    ["test/spider_runner_test.clj:81 authoring-turn-is-reflection-under-v3-and-not-under-v2 -- asserts all three v2 clauses are false on the rejected record and v3 is true, and that v3 leaves the counter-fixture alone"
     "test/spider_runner_test.clj:97 rung-two-warrant-refuses-an-authoring-turn (stubbed fetch, no live store)"
     "test/library_graph_lint_test.clj:238 wave-1-attestations-re-audited-under-reflection-rule-v3 -- recomputes the audit file's numbers from the C88-pinned fixture"],
    :rule
    ["checks/library_graph_lint.clj:12 reflection-rule-version 3"
     "checks/library_graph_lint.clj:278 authoring-turn? -- an authoring verb within 120 chars of a *.flexiarg path"
     "checks/library_graph_lint.clj:300 reflection-record? adds the class"
     "checks/library_graph_lint.clj:312 reflection-rule -- :authoring-turn and :authoring-verb-window ride in the cache header beside :basis, where v2's clauses live"],
    :gates
    {:clj-kondo
     "exit 0, errors 0 warnings 0 over the four touched files (the one :info at library_graph_lint.clj:247 spider-self-text? pre-dates this change -- reproduced on HEAD's copy)",
     :check-parens
     "exit 0, OK (emacs -Q --batch -l futon4/dev/check-parens.el, arxana-check-parens-cli)",
     :library-graph-lint-test
     "exit 0, 7 tests / 35 assertions / 0 failures",
     :spider-runner-test
     "exit 0, 5 tests / 23 assertions / 0 failures",
     :section-lint
     "system-coherence pass? true, 0 failures, under the v2-pinned cache (no live rebuild)"},
    :wave-1-re-audit
    {:records-audited 56,
     :basis
     "C88 pin -- test/fixtures/library-graph/evidence-records.edn, 56 records, sha 15effe91…",
     :changed-verdicts 0,
     :note
     "the one authoring turn (e-e8e638c6) is a zai-2 spider turn quoting the same claude-5 announcement, already reflection under v2 by its author; the expected 0 holds",
     :newly-reflection 0,
     :record "library/aif/attestations-v3-reflection-audit.edn",
     :attestations-audited 62,
     :authoring-turns 1,
     :reflection-under-v2 49,
     :reflection-under-v3 49},
    :fixtures
    ["test/fixtures/library-graph/reflection-v3-fixtures.edn :authoring-turn -- the wave-2 rejected case (system-coherence e-7a50b862, claude-5 announcing the file it had just written and listing its references)"
     "test/fixtures/library-graph/reflection-v3-fixtures.edn :external-description -- the counter-fixture, abridged from wave-1 aif record e-f5a7638c: says \"flexiarg\" and \"authored\", names five patterns, announces no pattern file"],
    :rung-2-gate
    ["checks/spider_runner.clj:210 rung-two-warrant? -- rung 2 had no reflection check at all, which is how the rejected record reached the owner; at least one cited record must be non-reflection"
     "checks/spider_runner.clj:252 the throw (:reflection-only-rung-two) in validate-output"
     "checks/spider_runner.clj:160 the seat prompt states the rule"],
    :sha "299f47b"},
   :acceptance
   "Rule v3 stated in the cache header beside the basis (where v2 lives), with the wave-2 rejected case as a fixture that v3 catches and v2 misses; a counter-fixture v3 must NOT catch (a genuine external description). Test in the spider/lint suite. Wave-1 attestations re-audited under v3 with the count of changed verdicts reported (expected 0 per the wave-1 audit; state the number).",
   :review
   "Read 299f47b and ccf2bf8 in full and confirmed both are authored by Joseph Corneli, not the reviewer. Reproduced clj-kondo exit 0 (0 errors, 0 warnings), check-parens exit 0 (OK), library_graph_lint_test exit 0 (7 tests, 36 assertions, including the C88 audit: 56 records, 62 attestations, 49 reflection under v2, 1 authoring turn, 49 reflection under v3, 0 changed verdicts), spider_runner_test exit 0 (6 tests, 33 assertions), system-coherence pinned section lint exit 0 (1247 files; :why 86, :how 27, :see-also 136; 0 failures), and worklist_check exit 0. Confirmed the repaired attribution rule leaves the third-party-authoring fixture and the reviewer's Alice reproducer non-reflection while retaining impersonal, first-person, and subject-equals-author cases. Neither evidence commit changes a .flexiarg file, so it adds no spider-authored @why, post-hoc edge, or similarity-inferred edge.",
   :status :done,
   :id :L2,
   :class :C,
   :reviewed-by "codex",
   :reviewed-at "2026-09-01T19:47:31Z",
   :covers-key :none,
   :review-finding-answered
   {:sha "ccf2bf8",
    :fix
    ["checks/library_graph_lint.clj:301 authoring-verb-attributed-to-author? -- a verb counts when its clause is impersonal (a preposition follows and no agent is named), first-person, or has the record's author as subject; an active clause naming another agent does not count. A subject the rule cannot read falls through to counting, so an unreadable clause refuses a warrant rather than admitting one."
     "checks/library_graph_lint.clj:325 authoring-turn? filters the verb spans through it"
     "checks/library_graph_lint.clj:297 record-author, lifted out of reflection-record? so both read the author the same way"
     "checks/library_graph_lint.clj:26-37 authoring-subject-tail, first-person-subject, named-agent-subject, authoring-complement"
     "checks/library_graph_lint.clj:363 reflection-rule gains :authoring-attribution; :authoring-turn is now :author-attributed-authoring-verb-within-window-of-a-flexiarg-path"
     "checks/spider_runner.clj:160 the seat prompt says a report of somebody ELSE's authoring does warrant"],
    :counter-fixture
    "test/fixtures/library-graph/reflection-v3-fixtures.edn :third-party-authoring -- emacs-7c6ac3768ecf9d2149cd9909e6224870, an operator turn joe wrote 2026-08-25 relaying what codex-15 and invoke-1787649777853-1018-f0693af4 had built, three .flexiarg paths beside the verbs. A real record from the live store, not a constructed one; abridged, elision marked.",
    :tests
    ["test/spider_runner_test.clj:121 an-authoring-turn-is-the-record-authors-own-authoring -- asserts the FLIP, not just the verdict: authoring-turn-by-proximity? (the pre-fix predicate, carried verbatim at test/spider_runner_test.clj:87) is true on the counter-fixture and lint/authoring-turn? is false; the wave-2 impersonal case stays true under both; the reviewer's Alice reproducer is false; first-person and subject-equals-author stay true"
     "test/spider_runner_test.clj:157 rung-two-warrant? accepts the third-party record on its own"
     "test/library_graph_lint_test.clj:272 the re-audit falsifier also asserts the counter-fixture is not an authoring turn"],
    :re-audit
    "library/aif/attestations-v3-reflection-audit.edn recomputed under the fixed predicate over the same C88-pinned 56 records: 56 records, 62 attestations, 49 reflection under v2, 1 authoring turn, 49 under v3, 0 changed verdicts -- every number unchanged. The one wave-1 record carrying a path (e-e8e638c6) states its announcement impersonally (`Pattern authored at <path>`), so attribution does not reach it.",
    :rule-version
    "Stays 3; recorded with its cost in the audit file's :stale-caches. No corpus outside this ledger item was pinned against the pre-fix predicate. Caches written before ccf2bf8 are keyed v3 and carry pre-fix flags; the fix only ever drops verbs, so those caches mark a superset as reflection -- they can refuse a warrant they should have allowed, never admit one they should have refused.",
    :gates
    {:clj-kondo
     "exit 0, errors 0 warnings 0 over the four touched Clojure files (exit captured to /tmp/L2gates/kondo.exit and tested; the one :info now at library_graph_lint.clj:266 is the same pre-existing spider-self-text? coercion, moved down by the insertions)",
     :check-parens
     "exit 0, OK -- emacs -Q --batch -l /home/joe/code/futon4/dev/check-parens.el --eval (arxana-check-parens-cli) -- <four files>. The `--` separator is required: without it the CLI reports `No target files` and exits 2, which is not a parens failure",
     :library-graph-lint-test
     "exit 0, 7 tests / 36 assertions / 0 failures",
     :spider-runner-test
     "exit 0, 6 tests / 33 assertions / 0 failures",
     :section-lint
     "system-coherence pass? true, 0 failures; aif against the C88-pinned fixture pass? true, 0 failures, 4 edge-refusals -- the same counts L1 recorded, so the rule change moved no section verdict"},
    :not-touched
    "No .flexiarg file changed, so this adds no spider-authored @why, no post-hoc edge, and no similarity-inferred edge."},
   :owner :any}
  {:statement
   "Two sidecar attestation holds from wave 2 (a tag-list warrant) are queued for a second read and nobody has read them.",
   :evidence
   {:not-touched
    ["No .flexiarg file changed, so this authors no @why, no post-hoc edge, and no similarity-inferred edge (P-validated-R5 law O2 respected)."
     "The stale :refused-reason key on both rows (L1's evidence flagged it) is left as-is: renaming it to L1's vocabulary is a schema migration no L3 acceptance asked for. Worth its own row."],
    :holds
    ["library/sidecar/attestations.edn:0 -- edge {:from sidecar/explicit-promotion-to-facts :to sidecar/validation-enforcement-gate :kind :how}, :cited the THEN clause at library/sidecar/explicit-promotion-to-facts.flexiarg:21, directive at :8"
     "library/sidecar/attestations.edn:1 -- edge {:from sidecar/explicit-promotion-to-facts :to sidecar/fact-lifecycle-event-types :kind :see-also}, :cited the NEXT-STEPS bullet at library/sidecar/explicit-promotion-to-facts.flexiarg:31, directive at :9"],
    :gates
    {:exit-codes
     "captured to /tmp/L3gates/{parens,kondo,lintest,spidertest}.exit and tested with a numeric comparison; no gate status read through a pipe",
     :check-parens
     "exit 0, OK -- emacs -Q --batch -l /home/joe/code/futon4/dev/check-parens.el --eval (arxana-check-parens-cli) -- library/sidecar/attestations.edn (the only file this item edited). The -- separator is required, per L2's note",
     :clj-kondo
     "exit 0, errors 0 warnings 0 over checks/library_graph_lint.clj, checks/spider_runner.clj, test/library_graph_lint_test.clj, test/spider_runner_test.clj. No Clojure was touched by this item; the run establishes the gate is still green. The one :info at library_graph_lint.clj:266 is the same pre-existing spider-self-text? coercion L2 recorded",
     :library-graph-lint-test
     "exit 0, 7 tests / 36 assertions / 0 failures, under the C88 pin",
     :spider-runner-test
     "exit 0, 6 tests / 33 assertions / 0 failures",
     :section-lint
     "sidecar run TWICE -- once against git show HEAD:library/sidecar/attestations.edn and once against the edited file -- both exit 0, pass? true, 0 failures, 1247 files. Sole summary delta :warrant-refusals 0 -> 2; :edge-refusals stays 0 and the section's edges-by-kind stays {:why 0 :how 1 :see-also 1}, which is what a warrant-refusal should look like: the directives did not leave the file. The corpus basis 191738 resolved to the pinned cache /tmp/futon3-spider-evidence-occurrences-v3-191738-d8f61810a34eb73a.edn, so no live crawl"},
    :recorded
    "Each row gains :warrant-refused {:by \"library-build-loop\" :at \"2026-09-01\" :note ...} and keeps :state :proposed, so both directives stay in the pattern file, unwarranted. Each note also states what evidence WOULD carry the edge. The prior :review-note (claude-15's hold) is left verbatim as the historical record.",
    :reader
    "\"library-build-loop\" -- the Claude worker driven by holes/labs/library-contract/library-build-loop.sh. It is not on the Agency roster (GET /api/alpha/agents), and claude-13, the seat the hold names, is on the roster but did not do this read, so borrowing that id would misattribute. Flagged for the reviewer: if the ledger wants a roster id here, say which and it can be rewritten.",
    :verdict
    "BOTH warrant-refused (L1's vocabulary, landed at a3bff43); NEITHER edge-refused. The record carries ONE endpoint, not two: its subject is the source pattern and its body names no other pattern id, so it cannot say a relation to either target holds. It is fair rung-1 evidence that the SOURCE pattern is live -- its body's \"A proposal is not a fact merely because its evidence body exists\" restates that pattern's conclusion -- which is why the tag hit is not spurious and the answer is warrant-refusal rather than edge-refusal.",
    :per-edge
    ["row 0 (:how -> validation-enforcement-gate): the tag :promotion-gate names the promotion review that refused a deposited memory, i.e. the SOURCE pattern's own subject, not the target; and the body's remedy is a query-time eligibility predicate (intersect search results with frozen eligible ids and required reviewed state), whereas library/sidecar/validation-enforcement-gate.flexiarg:19 defines a write-time check of required fields, enum constraints and store boundaries. Different act, same word."
     "row 1 (:see-also -> fact-lifecycle-event-types): the tag :lifecycle and the body's \"the same memory lifecycle as eligibility\" mean the APM memory-proposal lifecycle inside frame 11 (deposited / proposed / review-refused), not the fact/warrant/retired event enum that library/sidecar/fact-lifecycle-event-types.flexiarg:8 and :19 define; the record names no lifecycle event type and no rationale or actor metadata on one."],
    :evidence-record
    "e-8931a4eb-6509-428a-9d11-db2aa4ef552f, cited by BOTH holds, :via :tag. Not in the C88 pin (that fixture is aif's 56 records); read in full from http://127.0.0.1:7073/api/alpha/evidence/e-8931a4eb-6509-428a-9d11-db2aa4ef552f rather than from the recorded excerpt, which was the tag list alone. An f11-scribe :memory record, :evidence/at 2026-08-18, session frame-11-t01A05-scribe-promote, :evidence/subject {:ref/type :pattern :ref/id \"sidecar/explicit-promotion-to-facts\"}, tags [:memory :memory/assert :memory-retrieval :promotion-gate :experimental-isolation :lifecycle :challenge].",
    :sha "076f69d"},
   :acceptance
   "Each hold read against its evidence excerpt and either attested, sent back with a note, or warrant-refused (using L1's vocabulary if landed, a review note otherwise). The read recorded in the section's attestations.edn with reader and date. Pointers to both records.",
   :review
   "Read 076f69d in full and confirmed it is authored by Joseph Corneli, not the reviewer. Fetched and read the complete e-8931a4eb record: its subject is sidecar/explicit-promotion-to-facts and its body names neither target, so it supports the source but cannot warrant either relation. Confirmed the source file still carries both directives and that the notes distinguish the query-time eligibility predicate from the write-time validation gate and the memory-proposal lifecycle from fact/warrant/retired events. Reproduced clj-kondo exit 0 (0 errors, 0 warnings), check-parens exit 0 (OK), library_graph_lint_test exit 0 (7 tests, 36 assertions), spider_runner_test exit 0 (6 tests, 33 assertions), and corpus-pinned sidecar section lint exit 0 (1247 files; section edges :why 0, :how 1, :see-also 1; 2 warrant-refusals, 0 edge-refusals, 0 failures). The evidence commit changes no .flexiarg file, so it adds no spider-authored @why, @why-posthoc edge, or similarity-inferred edge.",
   :status :done,
   :id :L3,
   :class :V,
   :reviewed-by "codex",
   :reviewed-at "2026-09-01T19:54:42Z",
   :covers-key :none,
   :owner :any}
  {:statement
   "The one-edge price test, the stated next step of NOTE-the-chain-layer-exists-and-is-empty.md:105-112: nothing yet says whether a derived check over an @how edge is cheap or expensive, and that price decides whether Q(o|pi) is reachable by populating and deriving.",
   :evidence
   {:second-finding
    "The edge is attested and the check says NOT ATTESTED, and neither refutes the other: the rung-1 attestation :cited the source pattern's own cross-link line (flexiarg:151-153), which is a SHOULD, while the check asks whether the method is applied at the site. Recorded, not acted on -- no attestation row was edited, since no L4 acceptance asked for that. Whether the schema needs a way to say an edge records an aspiration is worth its own row.",
    :edge
    "peripherals/progress-heartbeat-distinct-from-cycle-completion @how peripherals/canonical-typed-event-vs-side-channel -- ATTESTED at library/peripherals/attestations.edn:0, :state [:attested-by \"claude-15\"], rung 1. Chosen from the nine attested @how edges in the library (aif 6, peripherals 2, ukrns 1) because its source pattern's own evidence paragraph (progress-heartbeat-distinct-from-cycle-completion.flexiarg:107-113) names a real incident in real code, so there is a site to run a check AGAINST. Most of the other eight name none; that selection cost is counted in the price.",
    :not-touched
    ["No .flexiarg file changed, so this authors no @why, no @why-posthoc, and no similarity-inferred edge (P-validated-R5 law O2)."
     "No attestations.edn row changed. The edge keeps :state [:attested-by \"claude-15\"]."
     "checks/how_witness_snatch.clj left as it is; the kondo config makes it lint clean without editing it."],
    :check
    ["checks/how_witness_heartbeat.clj -- 171 lines, same relational shape as checks/how_witness_snatch.clj, shipped with BOTH a mirror (:145 -- the one real typed event at the site, asked what it carries out; must be empty) and a positive control (:138 -- the relation with the method-instance conjunct dropped; must be non-empty, or an empty witness would only mean the relation cannot see)"
     "checks/how_witness_heartbeat.clj:104-140 the core.logic relation; :57-102 the 19 fact rows with their file:line pointers; :1-55 the ns docstring, which carries the four inventions so they travel with the code"
     "checks/README.md -- row added to the witness table, and the run command"],
    :gates
    {:exit-codes
     "captured to /tmp/L4gates/{kondo,parens,lintest,spidertest,run}.exit and tested with a numeric loop that exits 1 on any non-zero; no gate status read through a pipe",
     :clj-kondo
     "exit 0, 0 errors 0 warnings over checks/how_witness_heartbeat.clj, checks/how_witness_snatch.clj, checks/how_kernel_snatch.clj",
     :check-parens
     "exit 0, OK -- emacs -Q --batch -l /home/joe/code/futon4/dev/check-parens.el --eval (arxana-check-parens-cli) -- checks/how_witness_heartbeat.clj (and separately .clj-kondo/config.edn). The `--` separator is required, per L2's note",
     :library-graph-lint-test
     "exit 0, 7 tests / 36 assertions / 0 failures, under the C88 pin (1247 files; :why 86, :how 27, :see-also 136) -- same numbers L3 recorded",
     :spider-runner-test
     "exit 0, 6 tests / 33 assertions / 0 failures",
     :witness-run
     "exit 0; the check is not wired into a test suite, matching how the two snatch witnesses ship -- it is registered in checks/README.md with its run command instead",
     :section-lint
     "not run: no wave slice and no .flexiarg or attestations.edn edit, so there is no section for it to have moved"},
    :tooling
    "Added .clj-kondo/config.edn (:lint-as fresh/run*/project as clojure.core/fn) because clj-kondo does not know core.logic binding forms: without it every logic variable reads as an unresolved symbol, and checks/how_witness_snatch.clj was carrying four such errors unnoticed. .gitignore narrowed from `.clj-kondo/` to `.clj-kondo/*` plus `!.clj-kondo/config.edn` so the config is tracked and the cache is not. Verified the clj-kondo output on the four gated files (library_graph_lint.clj, spider_runner.clj and their tests) is BYTE-IDENTICAL before and after adding the config -- it only adds knowledge, so it can remove an error and never add one.",
    :note
    "p4ng/empirics-futon/NOTE-the-one-edge-price.md, beside NOTE-the-chain-layer-exists-and-is-empty.md, committed p4ng 2551f81",
    :result
    "NOT ATTESTED. Witness empty: 0 of 5 source obligations are carried out by the method at the site. Positive control non-empty: 3 ARE discharged ([:s1 :last-progress-at] futon3c multi.clj:889, [:s3 :watchdog-silence] process_watchdog.clj:136, [:s4 :last-subtask] multi.clj:888) -- by state fields on ONE polled status map (multi.clj:1678), which is the side channel the method pattern names, plus a second liveness stamp on the CYDER registry (cyder.clj:145). Mirror silent. Facts pinned at futon3c ffb7e7bc.",
    :verdict
    "EXPENSIVE, and specifically: the code is cheap and the check is expensive. 3m12s of reading produced the 19 rows; under a second of core.logic consumed them. The cost per edge is a reading cost, not a compute cost, so it does not fall with more edges the way a compute cost would. What that implies for Q(o|pi) is NOT settled here -- Q needs many edges and this is one.",
    :price
    {:wall
     "5m47s total: 3m12s selecting the edge and reading the six files before a line was written, 2m35s writing/running/registering. The run itself is 0.85-0.89 s warm over three runs, 1.68 s cold.",
     :lines
     "171 total -- 55 ns+docstring, 46 fact rows and pointers, 37 relation+witness+controls, 30 -main",
     :fact-rows
     "19: 5 source obligations and 5 method obligations off the two flexiargs, 6 site mechanisms each a futon3c file:line, 3 discharge adjudications",
     :files-read 6,
     :invented
     ["I1 obligations -- neither pattern marks which THEN steps are checkable; reading the five numbered steps as the obligations is a decision made in the check"
      "I2 grounding -- nothing in the edge, in attestations.edn:0, or in either flexiarg names a file, a function, or a system; the site came from a prose evidence paragraph no parser reads"
      "I3 a meaning for 'carries out' -- README-flexiarg.md:171 says '@how' names 'the methods by which this pattern is carried out' and stops; reading it as EVERY obligation discharged by an instance of the method is one of at least four readings, and they do not agree on these facts"
      "I4 the site adjudication -- every discharge row is a human read of a source file; core.logic joins the rows, it does not produce them"]},
    :sha "futon3 2f35b62; note in p4ng 2551f81"},
   :acceptance
   "Take ONE attested @how edge. Write the check (core.logic rule or unit test) that says whether the named method actually carries out the pattern. Record what it cost: wall time, lines, and what had to be invented that the edge did not supply. Verdict stated as cheap or expensive WITH the numbers, in a note beside the NOTE. No generalisation beyond the one edge.",
   :review
   "Read futon3 2f35b62 and p4ng 2551f81 in full and confirmed both are authored by Joseph Corneli, not the reviewer. Confirmed the attested peripherals @how edge, both five-step obligation lists, all six futon3c ffb7e7bc source pointers, the 171-line check, and the note's accounting of 19 fact rows, 6 files, 4 inventions, and 5m47s wall time. Reproduced clj-kondo exit 0 (0 errors, 0 warnings), check-parens exit 0 (OK), library_graph_lint_test exit 0 (7 tests, 36 assertions; C88 pin 1247 files and edges :why 86, :how 27, :see-also 136), spider_runner_test exit 0 (6 tests, 33 assertions), witness run exit 0 (0 of 5 obligations, positive control 3, mirror empty), and worklist_check exit 0. No section lint applies because this is not a wave slice and neither evidence commit changes a .flexiarg or attestations.edn file. Therefore the work authors no spider @why, @why-posthoc edge, or similarity-inferred edge.",
   :status :done,
   :id :L4,
   :class :E,
   :reviewed-by "codex",
   :reviewed-at "2026-09-01T20:09:05Z",
   :covers-key :none,
   :owner :any}
  {:statement
   "Wave 3: the 0%-tagged families -- or3, nomad, mmca, memory, math-formalization-CA, math-formalization-CV, math-informal-CT, math-informal-CA -- rung 2 only, per P-organise-the-library's wave plan. One section per invocation.",
   :loop-mode :one-slice-per-invocation,
   :evidence
   {:sha "346dbaf",
    :wave-3-complete
    "library/.spider/ledger.edn -- 8 entries with :wave 3: or3 (8436467), nomad (4542fa1), mmca (7c97a6a), memory (ced9838), math-formalization-CA (c4bb49f), math-formalization-CV (bee7d0b), math-informal-CT (5899f62), math-informal-CA (346dbaf)",
    :edges
    "5 edges written across the whole wave, all @see-also in nomad (slice 2, 4542fa1); 0 @why proposed or authored by the spider in any slice, per LD0",
    :absences
    "76 typed absences, each carrying its :corpus pin: 17 or3, 3 nomad, 8 mmca, 5 memory, 23 math-formalization-CA, 7 math-formalization-CV, 7 math-informal-CT, 6 math-informal-CA",
    :library-wide-edges
    "unchanged from slice 2 onward at :edges-by-kind {:why 86 :how 27 :see-also 141}; the wave's only movement was @see-also 136 -> 141 in nomad",
    :open-defects-for-their-own-rows
    ["checks/spider_runner.clj:280-283 -- an absence is validated only for a matching :pattern and a non-blank :note. Four symptoms across slices 5-8: a false factual ground, five unshown searches, nine unresolvable citations, then 22 of 31 plus a garbled id."
     "checks/library_graph_lint.clj:8 -- edge-kinds omits @instantiates (31 lines) and @references (550 lines), so neither is counted nor target-resolved; two @instantiates targets do not resolve and the lint still reports :unresolved-targets 0."
     "checks/library_graph_lint.clj:388-392 -- :co-mention is computed against the CURRENT pattern-id set, so records about retiring (slice 7) or renaming (slice 8) patterns read clean."
     "checks/library_graph_lint.clj:382-402 -- no dedupe by record id; 6.8% of hit entries are duplicates, so wave-3 'hits' figures are entry counts."
     "checks/library_graph_lint.clj:107 -- evidence-cache-path hardcodes the literal \"-v3-\", so a cache filename is not a version claim."
     "checks/spider_fleet.clj:139 -- a :pinned-evidence-cache-is-unreadable throw that can never fire."
     "checks/spider_runner.clj:184 -- a parse failure loses the seat's text while a validation failure keeps it."
     "The @references / @instantiates vocabulary question itself: out of scope by design, a kind the lint counts separately, or a source of candidates for attested @see-also."],
    :reviewer-entry-points
    ["git show 346dbaf -- the run artifacts and the ledger entry"
     "library/.spider/ledger.edn, 15th :sections element -- the slice's findings with their file:line pointers"
     "/tmp/L5s8/*.exit -- the twelve-plus captured gate exit codes"
     "/tmp/L5s8/ca-{before,after,after2,after3}.edn -- the four byte-identical section lint reports"]},
   :acceptance
   "Per slice: run the spider for ONE listed section against the pinned evidence basis (C88 pin discipline), gate with library_graph_lint, record the section's before/after edges-by-kind and attestation states in library/.spider/ledger.edn, commit. Append the section and sha to :progress. The row is done when all eight sections have a wave-3 ledger entry. No @why authored by the spider: propose only (LD0).",
   :status :done,
   :reviewed-by "codex",
   :reviewed-at "2026-09-01T23:13:15Z",
   :review
   "Read all eight evidence diffs (8436467, 4542fa1, 7c97a6a, ced9838, c4bb49f, bee7d0b, 5899f62, 346dbaf; 9,640 diff lines) and confirmed they are authored by Joseph Corneli, not the reviewer. Reproduced library_graph_lint_test exit 0 (10 tests, 59 assertions; C88 pin 1247 files and library-wide edges :why 86, :why-posthoc 0, :how 27, :see-also 141), spider_runner_test exit 0 (6 tests, 33 assertions), and all eight C88-pinned wave-section lint runs exit 0 with 0 failures, 0 unresolved targets, 0 why cycles, 0 warrant refusals, and 0 edge refusals. Section edge counts reproduced as or3 0/0/0/0, nomad 0/0/0/5, mmca 0/0/0/0, memory 0/0/0/0, math-formalization-CA 6/0/0/16, math-formalization-CV 6/0/0/9, math-informal-CT 0/0/0/0, math-informal-CA 0/0/0/0 in :why/:why-posthoc/:how/:see-also order. The evidence diffs add exactly five directives, all nomad @see-also backed by proposed rung-2 attestations; they add no @why, @why-posthoc, or similarity-inferred edge. Confirmed the ledger has all eight wave-3 sections and records 76 absences. worklist_check exits 0.",
   :id :L5,
   :class :W,
   :covers-key :none,
   :progress
   [{:not-touched
     ["No .flexiarg file changed (the section produced no edge), so this slice authors no @why, no @why-posthoc, and no similarity-inferred edge (P-validated-R5 law O2)."
      "checks/spider_fleet.clj:139 is left as it is: clj-kondo reports `Condition always true` there, and it is right -- (assoc (read-edn path nil) :cache-path p) is a map even when the pinned cache file is absent, so the :pinned-evidence-cache-is-unreadable throw below it can never fire and a missing pin would run the fleet on an empty index. This slice used spider_runner.clj directly, whose delay does throw (checks/spider_runner.clj:120), so the defect did not touch this run. Reported, not fixed: it is not in this row's acceptance and deserves its own row."],
     :gates
     {:exit-codes
      "captured to /tmp/L5gates/{before,after,kondo,parens,lintest,spidertest}.exit and tested with a string comparison; no gate status read through a pipe",
      :section-lint
      "checks/library_graph_lint.clj over or3, run TWICE -- once before the run finished writing (exit 0) and once after (exit 0). Both pass? true, 0 failures, 1247 files, :edges-by-kind {:why 86 :how 27 :see-also 136}, 0 unresolved-targets, 0 why-cycles, 0 warrant-refusals, 0 edge-refusals. Reports at /tmp/L5gates/or3-{before,after}.edn",
      :clj-kondo
      "exit 0, 0 errors 0 warnings over checks/library_graph_lint.clj, checks/spider_runner.clj, test/library_graph_lint_test.clj, test/spider_runner_test.clj. No Clojure was touched by this slice; the run establishes the gate is still green. The one :info at library_graph_lint.clj:266 is the same pre-existing spider-self-text? coercion L2 recorded",
      :check-parens
      "exit 0, OK -- emacs -Q --batch -l /home/joe/code/futon4/dev/check-parens.el --eval (arxana-check-parens-cli) -- library/.spider/ledger.edn library/or3/attestations.edn library/or3/proposals.edn library/or3/.spider/{state,absences,seat-failures}.edn. The -- separator is required, per L2's note",
      :library-graph-lint-test
      "exit 0, 7 tests / 36 assertions / 0 failures, under the C88 pin",
      :spider-runner-test
      "exit 0, 6 tests / 33 assertions / 0 failures"},
     :section "or3",
     :basis {:count 197217, :max-at "2026-08-31T16:39:25.560756552Z"},
     :records
     ["library/or3/.spider/absences.edn -- 17 records, each with its :corpus pin and the rung-2 queries actually run"
      "library/or3/attestations.edn -- [] (no edge was warranted, so no attestation record exists)"
      "library/or3/proposals.edn -- []"
      "library/or3/.spider/state.edn -- :phase :complete, :turn 17, :finished-at 2026-09-01T20:31:44Z"
      "library/or3/.spider/gates/turn-000.edn .. turn-017.edn -- the per-turn linter reports"],
     :result
     "17 of 17 patterns processed in 17 turns, 0 seat failures; 17 typed absences, 0 edges, 0 proposals. Section before/after unchanged at :edges-by-kind {:why 0 :how 0 :see-also 0}, fraction-organised 0.0.",
     :pin-defect
     "The cache this slice was pinned to was built under :reflection-rule {:version 2 :worker-seats [] :spider-agent nil} -- before rule v3 (299f47b, ccf2bf8) and naming no seat, so its cached :reflection flags neither implement v3 nor mark zai-3/codex-20 authorship. Note the filename is not the tell: checks/library_graph_lint.clj:107 hardcodes the literal \"-v3-\" in evidence-cache-path and carries the rule version only inside the sha, so a v2 cache is named v3. The defect could not have admitted an edge in or3 -- clean-hit? tests :listing/:self-text/:co-mention and ignores :reflection entirely, and :clean is 0 for all 17, and a stricter rule can only demote hits further. Rung 2 is unaffected: spider_runner.clj:222 rung-two-warrant? calls lint/reflection-record? against the live record under the current code. The next slice should build its cache with the seat named and under v3.",
     :slice 1,
     :at "2026-09-01",
     :finding
     "No rung-1 warrant exists anywhere in or3, independent of what any seat did. 5 of 17 patterns have an exact-occurrence hit (co-design-the-call 3, count-every-card-back 2, plural-value-accounting 5, reproducibility-as-a-teaching-habit 1, teach-the-process-not-the-facility 1) and every one is :listing true -- a futon3a embeddings retrieval ranking, which P-validated-R5 law O2 refuses as a relation and checks/library_graph_lint.clj clean-hit? refuses as a warrant. clean = 0 and clean-non-reflection = 0 for all 17. Measured from the pinned cache with lint/clean-hit? and lint/clean-non-reflection-hit?; table at /tmp/L5gates/or3-coverage.txt and in the runner state's :rung-one-coverage {:patterns 17 :any 5 :clean 0 :clean-non-reflection 0}. So the wave plan's \"rung 2 only\" is not a policy choice for this section, it is the only rung available.",
     :ledger-entry
     "library/.spider/ledger.edn:188 -- {:wave 3 :section \"or3\"}, the 8th element of :sections",
     :sha "8436467",
     :seat "zai-3"}
    {:gates
     {:exit-codes
      "captured to /tmp/L5s2/{before,after,kondo,parens,lintest,spidertest}.exit and tested with a string comparison; no gate status read through a pipe",
      :section-lint
      "checks/library_graph_lint.clj over nomad, run before the spider and again after -- both exit 0, both pass? true, 0 failures, 1247 files, 0 unresolved-targets, 0 why-cycles, 0 warrant-refusals, 0 edge-refusals. Sole delta is the section's :see-also 0 -> 5 and the library total 136 -> 141. Reports at /tmp/L5s2/nomad-{before,after}.edn",
      :clj-kondo
      "exit 0, 0 errors 0 warnings over checks/library_graph_lint.clj, checks/spider_runner.clj, test/library_graph_lint_test.clj, test/spider_runner_test.clj. No Clojure was touched by this slice. The one :info at library_graph_lint.clj:266 is the same pre-existing spider-self-text? coercion L2 recorded",
      :check-parens
      "exit 0, OK -- emacs -Q --batch -l /home/joe/code/futon4/dev/check-parens.el --eval (arxana-check-parens-cli) -- library/.spider/ledger.edn library/nomad/attestations.edn library/nomad/proposals.edn library/nomad/.spider/{state,absences,seat-failures}.edn; re-run on the ledger after the append, also exit 0",
      :library-graph-lint-test
      "exit 0, 7 tests / 36 assertions / 0 failures, under the C88 pin",
      :spider-runner-test
      "exit 0, 6 tests / 33 assertions / 0 failures"},
     :section "nomad",
     :not-fixed
     ["library_graph_lint.clj:107 still hardcodes the literal \"-v3-\" in evidence-cache-path and carries the rule version only inside the sha, so a cache filename is not a version claim. or3 reported it; this slice worked around it by building a real v3 cache rather than trusting a name. The naming defect itself wants its own row."
      "checks/spider_fleet.clj:139 -- the :pinned-evidence-cache-is-unreadable throw that can never fire, reported by or3 and still unfixed. Not in this row's acceptance; this slice used spider_runner.clj, whose delay does throw (checks/spider_runner.clj:120)."],
     :basis {:count 206483, :max-at "2026-09-01T20:35:40.731036691Z"},
     :records
     ["library/nomad/attestations.edn -- 5 records, all :kind :see-also, all :rung 2, all :state :proposed, all :by zai-3, each with its :corpus pin"
      "library/nomad/.spider/absences.edn -- 3 records (letter-from-the-future, licensed-ground, self-certifying-artifact) naming the rung-2 queries actually run"
      "library/nomad/proposals.edn -- []"
      "library/nomad/.spider/state.edn -- :phase :complete, :turn 8, :finished-at 2026-09-01T21:09:47Z"
      "library/nomad/.spider/gates/turn-000.edn .. turn-008.edn -- the per-turn linter reports"
      "5 directive lines added: author-the-yardstick.flexiarg:8, choose-the-grid.flexiarg:8, live-yardstick.flexiarg:8, owner-of-the-problem.flexiarg:8, skip-the-flood.flexiarg:8"],
     :pin-acted-on
     "This slice ACTS ON the defect or3 recorded rather than restating it. or3 ran against a cache built under :reflection-rule {:version 2 :worker-seats [] :spider-agent nil}. This slice built a fresh index (lint/ensure-live-evidence-index! \"library\" #{\"zai-3\"} \"codex-20\") whose :reflection-rule is {:version 3 :worker-seats [\"zai-3\"] :spider-agent \"codex-20\"}, so the cached :reflection flags implement rule v3 and name this run's seat. The crawl read 206,483 records at 1000/page in ~20 min and indexed 1092 patterns; cache at /tmp/futon3-spider-evidence-occurrences-v3-206483-70eeae7145d2c7a6.edn, pinned via SPIDER_EVIDENCE_CACHE for both the run and the after-lint.",
     :result
     "8 of 8 patterns processed in 8 turns, 0 retries, 0 malformed-output receipts, 0 seat failures. First wave-3 section to produce edges: 5 @see-also directives written and attested :proposed, 3 typed absences, 0 proposals, 0 @why. Section goes :edges-by-kind {:why 0 :how 0 :see-also 0} -> {:why 0 :how 0 :see-also 5}; library-wide @see-also 136 -> 141 with :why 86 and :how 27 unchanged. fraction-organised stays 0.0 because it counts outgoing @why only and the spider may not author @why -- the section gained peer links, not authority links, which is the most this lane is allowed to produce.",
     :slice 2,
     :at "2026-09-01",
     :finding
     "The 5 edges rest on exactly two evidence records, and they are not equally strong. e-3d5c18cc-3d05-4a3c-b395-1c361b62a805 STATES a relation -- \"the fast-money patterns are Owner-Of-The-Problem and Live-Yardstick (pre-allocated budgets, named holders), not Author-The-Yardstick (accrual)\" groups two patterns by a shared property and contrasts a third, which is what @see-also means. e-05d08802-4523-4ec2-ba96-5eaa41c18b35 reports only CO-ATTRIBUTION -- \"the held-out control -> choose-the-grid + skip-the-flood\" says one spot-check was attributed to both patterns, not that they stand in a relation. choose-the-grid <-> skip-the-flood rest on that record ALONE and are the two an owner review should read first; author-the-yardstick <-> live-yardstick cite both records; owner-of-the-problem -> live-yardstick cites the stating one. Not a law O2 violation -- co-attribution in an agent-written report is not embedding similarity -- and it is why all five sit at :proposed, not :attested. Both records are claude-4 invoke-complete envelopes from session 8b297865 dated 2026-07-27, five weeks before the spider programme existed, so neither is reflection under rule v3: author not in worker-seats, envelope names neither zai-3 nor codex-20, no spider prompt markers, no .flexiarg path for the authoring-turn test. Verified by fetching both from GET /api/alpha/evidence/<id>.",
     :ledger-entry
     "library/.spider/ledger.edn -- {:wave 3 :section \"nomad\"}, the 9th element of :sections; verified the append changed nothing else (old :sections equals (butlast new :sections), and (dissoc ledger :sections) is identical) after pprint rewrapped the file",
     :absence-note
     "licensed-ground is the instructive absence: its own body says \"Guard on nomad/owner-of-the-problem\" and also names self-certifying-artifact and author-the-yardstick, and the spider still recorded an absence. A pattern asserting its own relation is the author's text, not external corroboration, so it does not warrant a directive line -- the same standard that refused or3's 17.",
     :sha "4542fa1",
     :seat "zai-3",
     :rung-one-finding
     "As in or3, no rung-1 warrant exists anywhere in nomad, before any seat reads anything. 5 of 8 patterns have an exact-occurrence hit (author-the-yardstick 4, choose-the-grid 7, live-yardstick 1, owner-of-the-problem 7, self-certifying-artifact 7) and EVERY hit on every one carries :co-mention true, so clean-hit? fails throughout: clean = 0 and clean-non-reflection = 0 for all 8. The other 3 have no index entry at all. Table at /tmp/L5s2/nomad-coverage.txt, computed with lint/clean-hit? and lint/clean-non-reflection-hit? against the pinned cache, matching the runner's :rung-one-coverage {:patterns 8 :any 5 :clean 0 :clean-non-reflection 0}. So the wave plan's \"rung 2 only\" is again a fact about the corpus, not a policy choice."}
    {:not-touched
     ["No .flexiarg file changed (the section produced no edge), so this slice authors no @why, no @why-posthoc, and no similarity-inferred edge (P-validated-R5 law O2)."
      "checks/library_graph_lint.clj:107 still hardcodes the literal \"-v3-\" in evidence-cache-path, so a cache filename is not a version claim. Reported by or3, worked around by nomad and again here (the rule was read out of the cache body, not its name)."
      "checks/spider_fleet.clj:139 -- the :pinned-evidence-cache-is-unreadable throw that can never fire, reported by or3 and nomad, still unfixed. This slice used spider_runner.clj directly, whose delay does throw (checks/spider_runner.clj:120-122)."
      "The other dirty paths in this shared checkout are not this slice's: resources/sigils/patterns-index.tsv was modified at 2026-09-01T05:37:37Z, ~16 h before this run, and the three untracked library/math-formalization/*.flexiarg files and holes/labs/library-contract/runs/build-loop.log belong to other work. Staged explicit paths only; no commit -a."],
     :apparatus-defect
     "checks/spider_runner.clj cannot be run as `bb checks/spider_runner.clj` from the futon3 root: there is no bb.edn, so its (:require [checks.library-graph-lint]) fails with FileNotFoundException at spider_runner.clj:3 before anything runs. The first attempt of this slice died there and wrote no state. It needs `bb --classpath . checks/spider_runner.clj ...` (the nomad ledger entry records `bb -cp .`). Reported, not fixed: adding a bb.edn to futon3 is a repo-wide change outside this row's acceptance.",
     :gates
     {:exit-codes
      "captured to /tmp/L5s3/{before,after,run,kondo,parens,lintest,spidertest,ledgerread}.exit and tested by a shell loop that reads each file and exits 1 on any non-zero; no gate status read through a pipe. All eight are 0.",
      :section-lint
      "checks/library_graph_lint.clj over mmca, run before the spider and again after -- both exit 0, both pass? true, 0 failures, 1247 files, 0 unresolved-targets, 0 why-cycles, 0 warrant-refusals, 0 edge-refusals. Before and after are IDENTICAL on every counted field including library-wide {:why 86 :how 27 :see-also 141}, which is the expected result for a section that produced no edge. Reports at /tmp/L5s3/mmca-{before,after}.edn",
      :runner
      "exit 0 (/tmp/L5s3/run.exit); state.edn :phase :complete, :turn 8, 8 of 8 completed",
      :clj-kondo
      "exit 0, 0 errors 0 warnings over checks/library_graph_lint.clj, checks/spider_runner.clj, test/library_graph_lint_test.clj, test/spider_runner_test.clj. No Clojure was touched by this slice; the run establishes the gate is still green. The one :info at library_graph_lint.clj:266 is the same pre-existing spider-self-text? coercion L2 recorded",
      :check-parens
      "exit 0, OK -- emacs -Q --batch -l /home/joe/code/futon4/dev/check-parens.el --eval (arxana-check-parens-cli) -- library/.spider/ledger.edn library/mmca/attestations.edn library/mmca/proposals.edn library/mmca/.spider/{state,absences,seat-failures}.edn, run after the ledger append. The -- separator is required, per L2's note",
      :library-graph-lint-test
      "exit 0, 7 tests / 36 assertions / 0 failures, under the C88 pin (1247 files; the pin now reads :see-also 141, which is nomad's 5 edges from slice 2, already committed)",
      :spider-runner-test
      "exit 0, 6 tests / 33 assertions / 0 failures",
      :ledger-reread
      "exit 0 -- library/.spider/ledger.edn re-reads as EDN after the append; :sections is 10 long, ending [... or3 nomad mmca]"},
     :section "mmca",
     :pin-choice
     "Reused the v3 cache slice 2 built (/tmp/futon3-spider-evidence-occurrences-v3-206483-70eeae7145d2c7a6.edn, :reflection-rule {:version 3 :worker-seats [\"zai-3\"] :spider-agent \"codex-20\"}, built-at 2026-09-01T20:58:38Z) rather than crawling again. Same seat and same rule as this run, so its cached :reflection flags are the ones this run needs, and pinning two slices to one basis is what C88 pin discipline asks for; it also keeps slice 2's own whistle envelopes out of the index, since the basis predates them. Cost avoided: the ~20-minute 206k-record crawl, against a futon1b JVM sitting at 3122/4096 MB heap with 0 of 2 permits free at the start of this slice. The v3 rule does visible work here for the first time in wave 3: 6 hits across screen-the-knob-before-the-controller and verified-but-idle-link carry :self-text true :reflection true, which the v2 cache or3 ran against would have left unflagged. It moves no verdict -- clean-hit? ignores :reflection and :clean is 0 for all 8 anyway.",
     :basis {:count 206483, :max-at "2026-09-01T20:35:40.731036691Z"},
     :records
     ["library/mmca/.spider/absences.edn -- 8 records, each with its :corpus pin and the three rung-2 text-search queries actually run for that pattern"
      "library/mmca/attestations.edn -- [] (no edge was warranted, so no attestation record exists)"
      "library/mmca/proposals.edn -- []"
      "library/mmca/.spider/seat-failures.edn -- []"
      "library/mmca/.spider/state.edn -- :phase :complete, :turn 8, :finished-at 2026-09-01T21:22:43Z, 8 of 8 :completed"
      "library/mmca/.spider/gates/turn-000.edn .. turn-008.edn -- the per-turn linter reports"
      "library/mmca/.spider/receipts/ -- 24 receipts (intent/completion/applied for 8 turns); 0 malformed-* receipts, which is the evidence of 0 retries"
      "No .flexiarg file changed"],
     :result
     "8 of 8 patterns processed in 8 turns, 0 retries, 0 malformed-output receipts, 0 seat failures, 7m06s wall (21:15:37Z -> 21:22:43Z). Every turn ended in a typed absence: 8 absences, 0 edges, 0 proposals, 0 attestations. Section before/after unchanged at :edges-by-kind {:why 0 :how 0 :see-also 0}, fraction-organised 0.0; library-wide unchanged at {:why 86 :how 27 :see-also 141}. Like or3 and unlike nomad, mmca produced no edge at all.",
     :slice 3,
     :at "2026-09-01",
     :record-defect
     "The absence note for mmca/verified-but-idle-link says \"all five runner-supplied hits are flagged\" and lists e-299454c4, e-278d6ae0, e-3f705183, e-badbbb6d, e-20880cb9. The runner supplied FOUR: e-299454c4-3903-4177-a796-55eed4f27cf7, e-3f705183-d8d9-4162-9ef0-149e41052d32, e-badbbb6d-eb27-4ee9-a3f9-099a98d124cb (:listing true :co-mention true) and e-20880cb9-1c87-4c7b-947b-aa517cad2bbf (:self-text true :reflection true) -- read straight out of the pinned index, script at /tmp/L5s3/vil.clj. e-278d6ae0-ecd3-418f-bac2-aa05227c6f10 is a real record but is indexed under peripherals/hot-reload-as-default-fix-path, memory/licence-to-fail and memory/verify-in-the-serving-process, not under this pattern; the seat's own note names peripherals/hot-reload-as-default-fix-path in the same sentence, so it appears to have folded a co-ranked id it read INSIDE one of its four hits into the runner-supplied set. Left as the seat wrote it and corrected here rather than revised, following L1's practice for the reclassified refusals. The absence stands either way: the extra id is also a :listing true ranking and the four real hits give clean = 0. GENERAL DEFECT: checks/spider_runner.clj:280-283 validates an absence only for (= pattern (:pattern absence)) and a non-blank :note, so evidence ids asserted in an absence note are never checked against the rung-1 set or the live store, unlike an edge's evidence ids at checks/spider_runner.clj:262-278. An absence is free text the runner takes on trust. Reported, not fixed: not in this row's acceptance; wants its own row.",
     :finding
     "No rung-1 warrant exists anywhere in mmca, independent of what any seat did -- the third section in a row. 7 of 8 patterns have at least one exact-occurrence hit (eye-before-metric 2, instrument-horizon-must-match-system 2, persistence-over-appearance 1, realization-variance-is-not-a-regime 3, screen-the-knob-before-the-controller 7, threshold-shaped-events 1, verified-but-idle-link 4); perceivable-target has no index entry at all. EVERY hit on every pattern carries :co-mention true, so clean-hit? fails throughout and clean = 0, clean-non-reflection = 0 for all 8. Most are also :listing true (futon3a embeddings rankings, refused as relations by P-validated-R5 law O2). Two are psr_search memory-pull-offers (e-pull-offer-2d47ff92 on screen-the-knob-before-the-controller, e-pull-offer-bddb9f53 on threshold-shaped-events) -- a Lean/Mathlib retrieval that surfaced the pattern among unrelated ids, which states no relation either. Measured from the pinned cache with lint/clean-hit? and lint/clean-non-reflection-hit?; table at /tmp/L5s3/mmca-coverage.txt, matching the runner's :rung-one-coverage {:patterns 8 :any 7 :clean 0 :clean-non-reflection 0}.",
     :ledger-entry
     "library/.spider/ledger.edn -- {:wave 3 :section \"mmca\"}, the 10th element of :sections. The append was checked in-process before writing: (vec (:sections old)) equals (vec (butlast (:sections new))) and (dissoc old :sections) equals (dissoc new :sections), so pprint rewrapping the file changed nothing else.",
     :sha "7c97a6a",
     :seat "zai-3"}
    {:not-touched
     ["No .flexiarg file changed (the section produced no edge), so this slice authors no @why, no @why-posthoc, and no similarity-inferred edge (P-validated-R5 law O2)."
      "checks/library_graph_lint.clj:107 still hardcodes the literal \"-v3-\" in evidence-cache-path, so a cache filename is not a version claim. Reported by or3; read the rule out of the cache body again here."
      "checks/spider_fleet.clj:139 -- the :pinned-evidence-cache-is-unreadable throw that can never fire, reported by or3, nomad and mmca, still unfixed. This slice used spider_runner.clj, whose delay does throw (checks/spider_runner.clj:120-122)."
      "The other dirty paths in this shared checkout are not this slice's: resources/sigils/patterns-index.tsv, the three untracked library/math-formalization/*.flexiarg files, and holes/labs/library-contract/runs/build-loop.log belong to other work. Staged explicit paths only; no commit -a."],
     :apparatus-defect
     "Extends slice 3's. futon3 has no bb.edn, so nothing that (:require [checks.library-graph-lint]) runs as a bare `bb <file>`. Slice 3 recorded this for checks/spider_runner.clj; this slice found the TEST suites fail the same way -- `bb test/library_graph_lint_test.clj` and `bb test/spider_runner_test.clj` both exit 1 with FileNotFoundException at line 3. Both pass under `bb --classpath .`. Reported, not fixed: adding a bb.edn is a repo-wide change outside this row's acceptance.",
     :gates
     {:exit-codes
      "captured to /tmp/L5s4/{before,after,after2,run,kondo,parens,parens2,lintest,spidertest}.exit and tested by a shell loop that reads each file and flags any non-zero; no gate status read through a pipe. All nine are 0.",
      :section-lint
      "checks/library_graph_lint.clj over memory, run before the spider and again after -- both exit 0, both pass? true, 0 failures, 1247 files, 0 unresolved-targets, 0 why-cycles, 0 warrant-refusals, 0 edge-refusals. The two reports are byte-identical (diff empty), the expected result for a zero-edge section; library-wide {:why 86 :how 27 :see-also 141} unchanged. A third run after the ledger append also exit 0. Reports at /tmp/L5s4/memory-{before,after,after2}.edn",
      :runner
      "exit 0 (/tmp/L5s4/run.exit); state.edn :phase :complete, :turn 5, 5 of 5 completed",
      :clj-kondo
      "exit 0, 0 errors 0 warnings over checks/library_graph_lint.clj, checks/spider_runner.clj, test/library_graph_lint_test.clj, test/spider_runner_test.clj. No Clojure was touched by this slice; the run establishes the gate is still green. The one :info at library_graph_lint.clj:266 is the pre-existing spider-self-text? coercion L2 recorded",
      :check-parens
      "exit 0, OK -- emacs -Q --batch -l /home/joe/code/futon4/dev/check-parens.el --eval (arxana-check-parens-cli) -- library/.spider/ledger.edn library/memory/attestations.edn library/memory/proposals.edn library/memory/.spider/{state,absences,seat-failures}.edn; re-run on the ledger after the append, also exit 0. The -- separator is required, per L2's note",
      :library-graph-lint-test
      "exit 0, 7 tests / 36 assertions / 0 failures, under the C88 pin",
      :spider-runner-test
      "exit 0, 6 tests / 33 assertions / 0 failures"},
     :section "memory",
     :pin-choice
     "Third slice on the v3 cache slice 2 built (/tmp/futon3-spider-evidence-occurrences-v3-206483-70eeae7145d2c7a6.edn, :reflection-rule {:version 3 :worker-seats [\"zai-3\"] :spider-agent \"codex-20\"}), for slice 3's reasons: same seat, same rule, one basis across slices is what C88 pin discipline asks, and the basis predates slices 2 and 3 so their own whistle envelopes stay out of the index. No crawl this slice.",
     :basis {:count 206483, :max-at "2026-09-01T20:35:40.731036691Z"},
     :records
     ["library/memory/.spider/absences.edn -- 5 records, each with its :corpus pin and the three rung-2 text-search queries actually run for that pattern"
      "library/memory/attestations.edn -- [] (no edge was warranted, so no attestation record exists)"
      "library/memory/proposals.edn -- []"
      "library/memory/.spider/seat-failures.edn -- []"
      "library/memory/.spider/state.edn -- :phase :complete, :turn 5, :finished-at 2026-09-01T21:34:18Z, 5 of 5 :completed"
      "library/memory/.spider/gates/turn-000.edn .. turn-005.edn -- the per-turn linter reports"
      "library/memory/.spider/receipts/ -- 15 receipts (intent/completion/applied for 5 turns); 0 malformed-* receipts, which is the evidence of 0 retries"
      "No .flexiarg file changed"],
     :result
     "5 of 5 patterns processed in 5 turns, 0 retries, 0 malformed-output receipts, 0 seat failures, 4m51s wall (21:29:27Z -> 21:34:18Z). Every turn ended in a typed absence: 5 absences, 0 edges, 0 proposals, 0 attestations. Section before/after unchanged at :edges-by-kind {:why 0 :how 0 :see-also 0}, fraction-organised 0.0; library-wide unchanged at {:why 86 :how 27 :see-also 141}. Like or3 and mmca, and unlike nomad, memory produced no edge.",
     :slice 4,
     :at "2026-09-01",
     :references-finding
     "The most substantive thing this slice turned up, and it is not about memory. All 5 absences give a SECOND ground beside the warrant test: \"the pattern's own @references to X and Y are already-present author directives and cannot be repeated\". That ground does not hold. @references is not one of README-flexiarg §5a's three pattern-to-pattern edge kinds; checks/library_graph_lint.clj never parses it (the string occurs in that file only in two comments, :14 and :329); it contributes nothing to :edges-by-kind or to fraction-organised. The proof it is unchecked is in this section: library/memory/name-a-reusable-artifact.flexiarg:8 references memory/stratify-before-you-contrast, which exists nowhere under library/ (`find library -name stratify-before-you-contrast.flexiarg` is empty), and the lint still reports :unresolved-targets 0. 494 of the 1247 pattern files carry an @references line, so this is library-wide. It does NOT move this section's outcome -- each absence also rests on the warrant test (clean = 0, no rung-2 record stated a relation), which is independent and sufficient -- but an editor declining to write @see-also because @references already names the neighbour is deferring to a line the graph cannot see. Wants its own row: either @references enters the §5a table and the linter, or the spider prompt stops treating it as an author directive. NOT opened here: adding a row is outside this row's acceptance.",
     :finding
     "memory is the first wave-3 section that is heavily indexed, and the extra traffic buys nothing. All 5 of 5 patterns have exact-occurrence hits, at two orders of magnitude above the previous three sections: licence-to-fail 367, name-a-reusable-artifact 39, no-tightening-while-held 503, surface-the-closed-set 74, verify-in-the-serving-process 492 -- against or3's 1-5, nomad's 1-7 and mmca's 1-7. clean = 0 and clean-non-reflection = 0 for every one: every hit carries at least one of :listing, :self-text, :co-mention. Most are embeddings retrieval rankings (:listing true :co-mention true), which P-validated-R5 law O2 refuses as a relation; the flag-false remainder are bare co-mentions, which state no relation either. So the section with by far the most corpus traffic in wave 3 has exactly as much rung-1 warrant as the sections with almost none. Measured with lint/clean-hit? and lint/clean-non-reflection-hit? against the pinned cache; table at /tmp/L5s4/memory-coverage.txt, matching the runner's :rung-one-coverage {:patterns 5 :any 5 :clean 0 :clean-non-reflection 0}. Four sections in, \"rung 2 only\" has not once been a policy choice.",
     :ledger-entry
     "library/.spider/ledger.edn -- {:wave 3 :section \"memory\"}, the 11th element of :sections. The append was checked in-process before writing: (vec (:sections old)) equals (vec (butlast (:sections new))) and (dissoc old :sections) equals (dissoc new :sections), and the file was re-read and compared to the value written, so pprint rewrapping changed nothing else.",
     :record-defects
     ["library/memory/.spider/absences.edn, no-tightening-while-held: the note cites e-ce932d5ca as one of \"this spider fleet's own reflections\". No record with that prefix is in the pinned index under ANY pattern; GET http://127.0.0.1:7073/api/alpha/evidence/e-ce932d5ca returns {:error \"not found\"}; a text-search for ce932d5c returns 0 results. The id names nothing. The other two in the same list, e-bc96ec70 and e-957fdc3b, are real and are :self-text true :reflection true as described."
      "library/memory/.spider/absences.edn, verify-in-the-serving-process: the note lists e-1f276035 among that pattern's flag-false rung-1 hits. e-1f276035-c12f-4851-9106-383f293cbb17 is a real record, but it is indexed under memory/surface-the-closed-set and 8 other patterns and NOT under verify-in-the-serving-process -- the same fold-in of a co-ranked sibling id slice 3 found for mmca/verified-but-idle-link."
      "library/memory/.spider/absences.edn, verify-in-the-serving-process: the same sentence calls e-db21ad13, e-92b32180, e-df3d942f and e-671ceccc \"this-fleet reflections\". All four carry :reflection false under rule v3 in the pinned cache. They are disqualified anyway, by :co-mention true."
      "Verdict unaffected in all three: clean = 0 across all 5 patterns whatever these ids are, so the absences stand. Left as the seat wrote them and corrected in the ledger rather than revised, following L1's practice for the reclassified refusals and slice 3's for mmca. Cross-check script at /tmp/L5s4/check-ids.clj, output /tmp/L5s4/id-crosscheck.txt. Second slice in a row to hit the general defect at checks/spider_runner.clj:280-283: an absence is validated only for a matching :pattern and a non-blank :note, so evidence ids asserted in an absence note are never checked against the rung-1 set or the live store, unlike an edge's ids at checks/spider_runner.clj:262-278."],
     :sha "ced9838",
     :seat "zai-3"}
    {:rung-one-rule-finding
     "checks/spider_runner.clj rung-one-warrant? was changed by 36a9c63 (2026-08-30 19:22Z) from (and (false? (:listing hit)) (false? (:self-text hit))) to lint/clean-non-reflection-hit?, which also requires (false? (:co-mention hit)). Before it a co-mentioning record could warrant a rung-1 edge; after it the warranting record can never name the edge's TARGET, so a rung-1 edge attests external use of the SOURCE with the relation supplied by the seat's reading. Re-tested all 18 rung-1 attestations in the library against the current rule and the pinned index (/tmp/L5s5/rung1-recheck.txt, script /tmp/L5s5/r1.clj): 8 pass, 10 fail. Of the 10, 4 are already :refused, 3 are :proposed, and 3 are ATTESTED by claude-15 -- aif/evidence-precision-registry -> exotic/full-lift-registry and the reciprocal pair aif/candidate-pattern-action-space <-> aif/evidence-precision-registry, the latter two both resting on e-ec575c01-804f-4b54-84d1-f5dbf042c03b, which is :co-mention true. Nothing re-checks an attested edge when the warrant rule changes under it. Reported, not acted on: re-opening attested edges is an owner decision, not this row's acceptance.",
     :stale-evidence-finding
     "The first wave-3 absence that is wrong on its stated ground. library/math-formalization-CA/.spider/absences.edn, measure-integration-api: the note declines an edge to math-strategy/missing-dependency-protocol because re-proposing it 'would repeat an existing directive, which is barred', citing evidence that the pattern's flexiarg line 8 is '@why math-strategy/missing-dependency-protocol'. It is not: library/math-formalization-CA/measure-integration-api.flexiarg:8 is '@why math-informal/reduce-to-known-result', and the string missing-dependency-protocol does not occur in the file. The three cited records are genuine rung-1 hits under this pattern (emacs-8088b05af210c2eb6b1c285b7eb50596, emacs-85871a6cd8972feb60207f03aa35aa00, emacs-741bf66440ca4e673b3e31d97c98520d, all :co-mention true) and their excerpt states the claim verbatim -- and they were TRUE WHEN WRITTEN: 53ef3cd created the file with that @why and 1b75c1f (2026-08-26, 'H5a: repoint three API patterns' @why ...', authorised by Joe) changed it. So the seat asserted a stale but once-accurate claim as a present-tense fact and declined an edge because the target was 'already present' when it had been deliberately REMOVED. The conclusion is right and the reason is false. General defect: the evidence basis is pinned (C88) but the library is not. checks/spider_runner.clj:258-261 guards an EDGE by requiring its :cited sentence to occur in the current pattern file, but nothing checks an evidence EXCERPT's claims about directives against the current file, and an absence is unvalidated entirely (checks/spider_runner.clj:280-283). Wants its own row.",
     :not-touched
     ["No .flexiarg file changed (the section produced no edge), so this slice authors no @why, no @why-posthoc, and no similarity-inferred edge (P-validated-R5 law O2)."
      "The 16 @instantiates lines are left exactly as their authors wrote them; proposing the §5a migration is recorded, not performed."
      "The 3 attested rung-1 edges that fail today's warrant rule are left at :attested; re-opening them is an owner decision."
      "resources/sigils/patterns-index.tsv and the three untracked library/math-formalization/*.flexiarg files were already dirty in this shared checkout when the slice began and were NOT staged; the commit names explicit paths only."],
     :apparatus-defect
     "A parse failure loses the seat's text; a validation failure keeps it -- and this run produced one of each. checks/spider_runner.clj:184 builds :value with (edn/read-string response) eagerly inside invoke-seat!, so a non-EDN reply throws before the map holding :raw/:wire is returned; invoke-valid! catches it as :invoke-error (:334-336) and builds {:error ...} with no :raw and no :wire (:337-338), unlike the validation branch at :339-343 which keeps both. The receipt at :347-350 therefore records :response nil :wire nil and the retry prompt at :353-357 quotes nothing back to the seat. Observed: turn-009-malformed-1.edn (lp-norm-comparison, 'Invalid leading character: @') has :response nil; turn-020-malformed-1.edn (series-evaluation-api, missing checkpoint restatement) has both populated. Reported, not fixed -- changing the runner mid-wave is outside this row's acceptance.",
     :gates
     {:exit-codes
      "captured to /tmp/L5s5/{before,after,after2,kondo,parens,parens2,lintest,spidertest,run}.exit and read from the files; no gate status read through a pipe",
      :section-lint
      "checks/library_graph_lint.clj over math-formalization-CA, run before the spider (exit 0), after it (exit 0), and again after the ledger append (exit 0). All three pass? true, 0 failures, 1247 files, 0 unresolved-targets, 0 why-cycles, 0 warrant-refusals, 0 edge-refusals, :edges-by-kind {:why 86 :how 27 :see-also 141}. The before and after reports are byte-identical (diff exit 0, empty output). Reports at /tmp/L5s5/ca-{before,after,after2}.edn",
      :clj-kondo
      "exit 0, 0 errors 0 warnings over checks/library_graph_lint.clj, checks/spider_runner.clj, test/library_graph_lint_test.clj, test/spider_runner_test.clj. No Clojure was touched by this slice. The one :info at library_graph_lint.clj:266 is the pre-existing spider-self-text? coercion L2 recorded.",
      :check-parens
      "exit 0, OK -- emacs -Q --batch -l /home/joe/code/futon4/dev/check-parens.el --eval (arxana-check-parens-cli) -- over the four gated Clojure files, and again over library/.spider/ledger.edn and the section's EDN records after the append. Apparatus note: invoking the file as `emacs --batch --script check-parens.el <files>` exits 0 while printing nothing and checking nothing -- only the documented --eval form is the real gate.",
      :library-graph-lint-test
      "exit 0, 7 tests / 36 assertions / 0 failures, under the C88 pin",
      :spider-runner-test
      "exit 0, 6 tests / 33 assertions / 0 failures"},
     :section "math-formalization-CA",
     :basis {:count 206483, :max-at "2026-09-01T20:35:40.731036691Z"},
     :instantiates-finding
     "The wave plan calls this a '0%-tagged' family, but it is the only wave-3 section that already carries edges ({:why 6 :how 0 :see-also 16}, fraction-organised 0.26 against 0.0 for the other four). 16 of its 23 patterns carry an @instantiates line, which README-flexiarg.md:224 lists as a DEPRECATED SPELLING of @why, 'same meaning'. The two partition the section exactly -- 6 patterns have @why and no @instantiates, 16 the reverse, 1 (transfer-derivatives-via-eventuallyeq) neither, none both. 16 of the library's 31 @instantiates lines are here. checks/library_graph_lint.clj never parses the directive, so it is neither counted nor target-checked: math-informal-CA/conjugate-reduce-to-model-case, the @instantiates target of schwarz-disk-automorphism-formula and surj-via-oriented-root-preimage, names no file in library/, and the section still reports :unresolved-targets 0. Were the §5a migration performed, 22 of 23 patterns would have an outgoing @why and fraction-organised would read 0.96 rather than 0.26, with two dangling targets to resolve and no new authorship. Recorded, not acted on: migrating 16 author-written edges is an authorship decision the spider lane may not make (decisions.edn :spider-editorial-standing).",
     :records
     ["library/math-formalization-CA/.spider/absences.edn -- 23 records, each with its :corpus pin and the rung-2 text-search queries actually run for that pattern"
      "library/math-formalization-CA/attestations.edn -- [] (no edge was warranted, so no attestation record exists)"
      "library/math-formalization-CA/proposals.edn -- []"
      "library/math-formalization-CA/.spider/seat-failures.edn -- []"
      "library/math-formalization-CA/.spider/state.edn -- :phase :complete, :turn 23, :finished-at 2026-09-01T22:04:48Z, 23 of 23 :completed"
      "library/math-formalization-CA/.spider/gates/turn-000.edn .. turn-023.edn -- the per-turn linter reports"
      "library/math-formalization-CA/.spider/checkpoints/checkpoint-001.edn -- the turn-20 checkpoint"
      "library/math-formalization-CA/.spider/receipts/ -- 71 receipts, including 2 malformed-1 receipts (turns 9 and 20), both recovered on attempt 2"
      "No .flexiarg file changed"],
     :result
     "23 of 23 patterns processed in 23 turns, 0 seat failures, 22m30s (21:42:18Z -> 22:04:48Z). Every turn ended in a typed absence: 23 absences, 0 edges, 0 proposals, 0 attestations. Section before/after unchanged at :edges-by-kind {:why 6 :how 0 :see-also 16}, fraction-organised 0.2609; library-wide unchanged at {:why 86 :how 27 :see-also 141}; the before and after lint reports are byte-identical. Two malformed-output receipts, the first retries in wave 3, both recovered on attempt 2.",
     :slice 5,
     :at "2026-09-01",
     :absence-ids-checked
     "Cross-checked every evidence id cited in all 23 absence notes against the pinned index (/tmp/L5s5/id-crosscheck.txt; scripts /tmp/L5s5/checkids2.clj and rung-side.clj, anchored so hyphenated prose like 'unfold-the-definition' is not misread as an id -- an unanchored first pass invented ids such as e-definition and e-machine). 136 ids cited: 58 are runner-supplied rung-1 hits for their own pattern, 13 are indexed under a different pattern, 65 are not in the rung-1 index. Unlike slices 3 and 4 NONE of this is a fold-in defect: all 13 appear in the rung-2 half of their note, checked positionally against each note's 'Rung 2' marker, which returned zero candidates. First wave-3 slice whose absence notes cite ids correctly on both rungs.",
     :finding
     "math-formalization-CA is the first wave-3 section where rung-1 warrant EXISTS -- :clean 11 and :clean-non-reflection 11 of 23 patterns, against clean = 0 for every pattern of or3, nomad, mmca and memory. 21 of 23 have some hit (measure-integration-api 179, schwarz-disk-automorphism-formula 75, surj-via-oriented-root-preimage 65, complex-arg-of-cpow-root 56, series-evaluation-api 52; hilbert-projection-properties and measure-restrict-simplify have no index entry at all). So for the first time in wave 3 the seat could have written a rung-1 edge, and wrote none. The reason it gives pattern after pattern is structural: :co-mention is (> (count patterns) 1) at checks/library_graph_lint.clj:392, so a hit is clean exactly when the record mentions no SECOND library pattern -- and a record naming no second pattern states no relation to one. Table at /tmp/L5s5/ca-coverage.txt, matching the runner's :rung-one-coverage {:patterns 23 :any 21 :clean 11 :clean-non-reflection 11}.",
     :checkpoint-note
     "The turn-20 checkpoint passed its format check and missed its purpose: checkpoint-001.edn restates the SPIDER'S OWN PROCEDURE rather than what the section is about, which is what checks/spider_runner.clj:139 asks for. No measure theory, integration or complex analysis appears in it; the only section-specific token is the trailing 'The current item is math-formalization-CA/series-evaluation-api.' checkpoint! (checks/spider_runner.clj:322-330) compares a restatement only to the previous one for equality, and at n=1 there is none -- a drift check that fires on repetition cannot fire on a first restatement that was never about the section.",
     :ledger-entry
     "library/.spider/ledger.edn -- {:wave 3 :section \"math-formalization-CA\"}, the 12th element of :sections. The append was checked in-process before writing: (vec (:sections old)) equals (vec (butlast (:sections new))), (dissoc old :sections) equals (dissoc new :sections), and the file was re-read and compared to the value written, so pprint rewrapping changed nothing else.",
     :sha "c4bb49f",
     :seat "zai-3"}
    {:not-touched
     ["No .flexiarg file changed (the section produced no edge), so this slice authors no @why, no @why-posthoc, and no similarity-inferred edge (P-validated-R5 law O2)."
      "The doubled @why/@see-also on frontier-bound-from-arc-hypotheses is left exactly as its author wrote it; only recorded."
      "The one @instantiates line is left as written; proposing the §5a migration is recorded, not performed."
      "checks/library_graph_lint.clj:107 still hardcodes the literal \"-v3-\" in evidence-cache-path, so a cache filename is not a version claim. Reported by or3; this slice again read the rule out of the cache body, not its name."
      "checks/spider_fleet.clj:139 -- the :pinned-evidence-cache-is-unreadable throw that can never fire, reported by every wave-3 slice so far, still unfixed. This slice used spider_runner.clj, whose delay does throw (checks/spider_runner.clj:120-122)."
      "checks/spider_runner.clj:184 -- a parse failure loses the seat's text while a validation failure keeps it (slice 5's :apparatus-defect). Not exercised here: 0 malformed-output receipts."
      "The other dirty paths in this shared checkout are not this slice's: holes/labs/library-contract/runs/build-loop.log, resources/sigils/patterns-index.tsv, and the three untracked library/math-formalization/*.flexiarg files belong to other work. Staged 35 explicit paths only; no commit -a."],
     :gates
     {:exit-codes
      "captured to /tmp/L5s6/{before,after,after2,run,kondo,parens,parens-records,parens-ledger,lintest,spidertest,append}.exit and tested by a shell loop that reads each file and exits 1 on any non-zero; no gate status read through a pipe. All eleven are 0.",
      :section-lint
      "checks/library_graph_lint.clj over math-formalization-CV, run before the spider (exit 0), after it (exit 0), and again after the ledger append (exit 0). All three pass? true, 0 failures, 1247 files, 0 unresolved-targets, 0 why-cycles, 0 warrant-refusals, 0 edge-refusals, library-wide :edges-by-kind {:why 86 :how 27 :see-also 141}. All three reports are byte-identical (diff exit 0, empty output), which is what a zero-edge section should produce. Reports at /tmp/L5s6/cv-{before,after,after2}.edn",
      :runner
      "exit 0 (/tmp/L5s6/run.exit); state.edn :phase :complete, :turn 7, 7 of 7 completed",
      :clj-kondo
      "exit 0, 0 errors 0 warnings over checks/library_graph_lint.clj, checks/spider_runner.clj, test/library_graph_lint_test.clj, test/spider_runner_test.clj. No Clojure was touched by this slice. The one :info at library_graph_lint.clj:266 is the pre-existing spider-self-text? coercion L2 recorded.",
      :check-parens
      "exit 0, OK -- emacs -Q --batch -l /home/joe/code/futon4/dev/check-parens.el --eval (arxana-check-parens-cli) -- over the four gated Clojure files, over the section's EDN records (attestations, proposals, state, absences, seat-failures), and again over library/.spider/ledger.edn after the append. The -- separator is required, per L2's note.",
      :library-graph-lint-test
      "exit 0, 7 tests / 36 assertions / 0 failures, under the C88 pin (1247 files; :why 86 :how 27 :see-also 141)",
      :spider-runner-test
      "exit 0, 6 tests / 33 assertions / 0 failures"},
     :section "math-formalization-CV",
     :instantiates-note
     "Follow-up to slice 5's finding on a section small enough to state exactly. math-formalization-CV carries 1 of the library's 31 @instantiates lines (complex-polynomial-bound:9 -> math-informal-CA/estimate-by-bounding) and that pattern is the section's only one with no @why, which is exactly why fraction-organised reads 6/7 = 0.857 rather than 1.0. Unlike math-formalization-CA's two dangling @instantiates targets, this one resolves: library/math-informal-CA/estimate-by-bounding.flexiarg exists. So the §5a migration would be a pure one-line respelling here with no target to repair. Recorded, not performed -- respelling an author's causal directive is an authorship decision.",
     :basis {:count 206483, :max-at "2026-09-01T20:35:40.731036691Z"},
     :records
     ["library/math-formalization-CV/.spider/absences.edn -- 7 records, each with its :corpus pin"
      "library/math-formalization-CV/attestations.edn -- [] (no edge was warranted, so no attestation record exists)"
      "library/math-formalization-CV/proposals.edn -- []"
      "library/math-formalization-CV/.spider/seat-failures.edn -- []"
      "library/math-formalization-CV/.spider/state.edn -- :phase :complete, :turn 7, :finished-at 2026-09-01T22:18:06Z, 7 of 7 :completed"
      "library/math-formalization-CV/.spider/gates/turn-000.edn .. turn-007.edn -- the per-turn linter reports"
      "library/math-formalization-CV/.spider/receipts/ -- 21 receipts (intent/completion/applied for 7 turns); 0 malformed-* receipts"
      "No checkpoint directory: checkpoint! fires at turn 20 and this section is 7 patterns long"
      "No .flexiarg file changed"],
     :result
     "7 of 7 patterns processed in 7 turns, 0 seat failures, 0 retries, 0 malformed-output receipts, 5m26s (22:12:40Z -> 22:18:06Z). Every turn ended in a typed absence: 7 absences, 0 edges, 0 proposals, 0 attestations. Section before/after unchanged at :edges-by-kind {:why 6 :how 0 :see-also 9}, fraction-organised 0.8571; library-wide unchanged at {:why 86 :how 27 :see-also 141}; the before and after lint reports are byte-identical. The fastest and cleanest wave-3 run so far by every apparatus measure, and the one whose absence notes are least well grounded.",
     :slice 6,
     :at "2026-09-01",
     :rung-two-is-not-pinned-finding
     "The general defect behind that, and the thing a reviewer should weigh hardest. C88 pins rung 1 and does not pin rung 2. rung-one-hits reads the frozen cache (basis :max-at 2026-09-01T20:35:40Z, before this run's 22:12:40Z start, so nothing this run wrote is in it); rung 2 is a live GET against futon1b, into which this run's own :turn-start envelopes are written as it proceeds, so the turn-1 prompt is searchable by turn 2. Measured over the 15 reconstructed queries (/tmp/L5s6/selfpollution.txt, script /tmp/L5s6/selfpoll.clj): 62 results returned, 16 authored by zai-3 with :evidence/at inside the run window [22:12:40Z, 22:18:06Z]; two queries returned exactly one result and it was one of those. At limit=5 that is recall spent on the searcher's own text, worsening as the run goes on. This is NOT a hole in the warrant rule -- rung-two-warrant? (checks/spider_runner.clj:210-221) applies lint/reflection-record? and would refuse such an edge at :252-254. It is a hole in recall. Fix direction, for its own row: filter rung-2 results by the pinned basis :max-at, or apply reflection-record? at retrieval rather than only at validation.",
     :absence-ids-checked
     "Every directive claim the seven notes make about a current pattern file was checked against the file and all are TRUE (lines 8-9 of each pattern; 9-10 for complex-polynomial-bound's @instantiates/@examples). So this slice reproduces none of slice 5's stale-directive defect -- the notes are accurate about the library and unsupported about the search.",
     :finding
     "Five of the seven absence notes record no rung-2 search. Turns 1 and 2 name the three queries each ran against the text-search endpoint, as every wave-3 slice before this one did; turns 3 through 7 replace that with 'Rung 2: no bounded keyword query could surface a warrant the supplied evidence structure excludes' and the claim that the only library targets visible anywhere in the evidence are that pattern's existing directives. The claim is false as written: reconstructing the queries by the recipe turns 1-2 used and running them (/tmp/L5s6/rung2probe.sh, output /tmp/L5s6/rung2-probe.txt) surfaces records naming 29, 10, 20, 13 and 32 distinct ids that resolve to real library/<id>.flexiarg files, most not directives on that pattern. The seat's CONCLUSION survives -- the records those ids come from are this run's own turn-start prompts and 190 KB fleet transcripts that enumerate ids without stating a relation, so none is a warrant -- so this is a right conclusion on a false ground, the same shape as slice 5's stale-evidence finding from a different cause: not stale evidence, but an absence reporting a search it does not show having run. checks/spider_runner.clj:280-283 validates an absence only for a matching :pattern and a non-blank :note, so nothing catches it.",
     :ledger-entry
     "library/.spider/ledger.edn -- {:wave 3 :section \"math-formalization-CV\"}, the 13th element of :sections. The append was checked in-process before writing: (vec (:sections old)) equals (vec (butlast (:sections new))), (dissoc old :sections) equals (dissoc new :sections), and the file was re-read and compared to the value written. One count in the entry prose (holomorphic-disk-api resolving ids) was corrected 31 -> 32 after re-counting the probe output; the ledger re-reads as EDN and check-parens passes on it.",
     :sha "bee7d0b",
     :duplicate-kind-finding
     "library/math-formalization-CV/frontier-bound-from-arc-hypotheses.flexiarg names the same target under two directive kinds -- line 8 '@why math-formalization-CV/holomorphic-disk-api' and line 9 '@see-also math-formalization-CV/holomorphic-disk-api'. Under README-flexiarg §5a those say incompatible things about one target: @why claims it as the authority this pattern rests on, @see-also is 'a peer technique; no claim of authority in either direction'. Scanned all 1247 .flexiarg files for a target under two of @why/@how/@see-also (/tmp/L5s6/dupkind.clj, output /tmp/L5s6/dupkind.txt): this is the only instance in the library. checks/library_graph_lint.clj does not check for it and counts both, so the section's :see-also 9 includes the redundant one and the lint still reports 0 failures. The seat noticed the pair and correctly declined to act on it. Recorded, not acted on: both lines are author-written and removing either is an authorship decision over an @why (decisions.edn :spider-editorial-standing). Wants its own row, with the lint check that would have caught it.",
     :seat "zai-3",
     :rung-one-finding
     "Second wave-3 section where rung-1 warrant exists, and the first where EVERY pattern has an index entry: :any 7 of 7, against 21 of 23 for math-formalization-CA, 5 of 17 for or3, 5 of 8 for nomad. :clean 4 and :clean-non-reflection 4 of 7 (derivative-bounds-api 10 hits / 3 clean, entire-affine-by-divided-difference 8/3, entire-and-singularity-api 70/8, holomorphic-disk-api 97/19; the other three have hits but none clean). So for the second time in wave 3 the seat could have written a rung-1 edge and wrote none, for the reason slice 5 recorded: :co-mention is (> (count patterns) 1) at checks/library_graph_lint.clj:392, so a hit is clean exactly when the record names no second library pattern, and such a record states no relation to one. Table at /tmp/L5s6/cv-coverage.txt; matches the runner's :rung-one-coverage {:patterns 7 :any 7 :clean 4 :clean-non-reflection 4}. Also the first wave-3 section where the v3 reflection rule moves a CLEAN count rather than only flagging already-excluded hits: entire-and-singularity-api is clean 8 but clean-non-reflection 7, holomorphic-disk-api 19 but 18. In mmca the flag fired and changed nothing because :clean was 0."}
    {:not-touched
     ["No .flexiarg file changed (the section produced no edge), so this slice authors no @why, no @why-posthoc, and no similarity-inferred edge (P-validated-R5 law O2)."
      "The @references / @factor / @family directives are left exactly as their authors wrote them; only counted and reported."
      "checks/library_graph_lint.clj:107 still hardcodes the literal \"-v3-\" in evidence-cache-path, so a cache filename is not a version claim. Reported by or3; this slice again read the rule out of the cache body, not its name."
      "checks/spider_fleet.clj:139 -- the :pinned-evidence-cache-is-unreadable throw that can never fire, reported by every wave-3 slice, still unfixed. This slice used spider_runner.clj, whose delay does throw (checks/spider_runner.clj:120-122)."
      "checks/spider_runner.clj:184 -- a parse failure loses the seat's text while a validation failure keeps it (slice 5's :apparatus-defect). Not exercised here: 0 malformed-output receipts."
      "The two new defects this slice found (no dedupe by record id at library_graph_lint.clj:382-402; :co-mention computed against the current pattern set at :388-392) are REPORTED, not fixed: neither is in this row's acceptance and each deserves its own row."
      "The other dirty paths in this shared checkout are not this slice's: holes/labs/library-contract/runs/build-loop.log, resources/sigils/patterns-index.tsv, and the three untracked library/math-formalization/*.flexiarg files belong to other work. Staged 35 explicit paths only; no commit -a."],
     :remaining "1 of 8 sections left: math-informal-CA.",
     :gates
     {:exit-codes
      "captured to /tmp/L5s7/{before,after,after2,run,kondo,parens,parens-records,parens-ledger,lintest,spidertest,append}.exit and tested by a shell loop that reads each file and exits non-zero on any non-zero. All eleven are 0. No gate status read through a pipe.",
      :section-lint
      "checks/library_graph_lint.clj over math-informal-CT, run before the spider (exit 0), after it (exit 0), and again after the ledger append (exit 0). All three pass? true, 0 failures, 1247 files, 0 unresolved-targets, 0 why-cycles, 0 warrant-refusals, 0 edge-refusals, library-wide :edges-by-kind {:why 86 :how 27 :see-also 141}. All three reports byte-identical (diff exit 0, empty output), as a zero-edge section should produce. Reports at /tmp/L5s7/ct-{before,after,after2}.edn",
      :runner
      "exit 0 (/tmp/L5s7/run.exit); state.edn :phase :complete, :turn 7, 7 of 7 completed",
      :clj-kondo
      "exit 0, 0 errors 0 warnings over checks/library_graph_lint.clj, checks/spider_runner.clj, test/library_graph_lint_test.clj, test/spider_runner_test.clj. No Clojure was touched by this slice. The one :info at library_graph_lint.clj:266 is the pre-existing spider-self-text? coercion L2 recorded.",
      :check-parens
      "exit 0, OK -- emacs -Q --batch -l /home/joe/code/futon4/dev/check-parens.el --eval (arxana-check-parens-cli) -- over the four gated Clojure files, over the section's EDN records (attestations, proposals, absences, seat-failures, state), and again over library/.spider/ledger.edn after the append. The -- separator is required, per L2's note.",
      :library-graph-lint-test
      "exit 0, 7 tests / 36 assertions / 0 failures, under the C88 pin (1247 files; :why 86 :how 27 :see-also 141)",
      :spider-runner-test
      "exit 0, 6 tests / 33 assertions / 0 failures"},
     :section "math-informal-CT",
     :clean-hit-finding
     "Why this section's one clean rung-1 hit is clean, and why that is a defect. :clean 1 and :clean-non-reflection 1 of 7, both from verify-universal-property's emacs-5db8b15ad191eec138201bb9184ee70f (/tmp/L5s7/ct-coverage.txt, /tmp/L5s7/hitids.txt; matches the runner's :rung-one-coverage {:patterns 7 :any 5 :clean 1 :clean-non-reflection 1}). The record (/tmp/L5s7/clean-record.edn) is a claude-2 emacs-claude-repl turn of 2026-08-13 running an orphan sweep: `math-informal/verify-universal-property: HTTP 200, orphan', `math-strategy/clarification-meta: HTTP 200, orphan', `math-informal-CT/verify-universal-property: HTTP 200, correct replacement' -- all three verbatim in the record. It names three library ids, but :co-mention is (> (count patterns) 1) over (filter ids ...) where ids is the CURRENT pattern set (library_graph_lint.clj:388-392), and the first two were retired by the reorganisation the record describes. path-hits is 1, co-mention false, hit reads clean. Generalised: a record about retiring patterns is systematically un-flagged, because the filter that would flag it runs against the library the retirement produced. Third distinct route to a clean-looking hit that warrants nothing. The seat refused it and named the right ground -- fleet infrastructure bookkeeping, no second library pattern as a mathematical relation -- the best-grounded refusal of wave 3 so far.",
     :basis {:count 206483, :max-at "2026-09-01T20:35:40.731036691Z"},
     :vocabulary-finding
     "The finding of this slice, and the one that touches the wave plan's premise. math-informal-CT reads 0.0 not because its patterns state no relations but because the relations they state are spelled @references, which README-flexiarg §5a's table does not list and checks/library_graph_lint.clj:8 does not count. Six of the seven patterns carry a line-9 `@references [a b c]`; all 18 targets resolve to real library files (checked one by one). verify-universal-property has no @references and is also the only one of the seven with no @family. Library-wide (/tmp/L5s7/refscan3.txt): 550 of 1247 files carry an @references line, 488 in bracketed-list form holding 1649 targets, of which 1218 (377 distinct) resolve to a library pattern and 431 do not (repo paths, retired p4ng ids, prose tokens). So 1218 stated pattern-to-pattern references go uncounted against 254 counted edges. They are NOT edges -- @references states no direction and no authority, which is what §5a's three kinds are for -- but `0%-tagged family' describes the spelling here, not the graph, and the spider was sent to find relations for patterns that already assert them in an uncounted vocabulary. Wants its own row: decide whether @references is out of scope by design, a fourth kind the lint counts separately, or a source of candidates for attested @see-also. Not acted on: converting one to @why is an authorship decision (decisions.edn :spider-editorial-standing) and converting one to @see-also without external warrant is an inferred edge (P-validated-R5 law O2).",
     :records
     ["library/math-informal-CT/.spider/absences.edn -- 7 records, each with its :corpus pin"
      "library/math-informal-CT/attestations.edn -- [] (no edge was warranted)"
      "library/math-informal-CT/proposals.edn -- []"
      "library/math-informal-CT/.spider/seat-failures.edn -- []"
      "library/math-informal-CT/.spider/state.edn -- :phase :complete, :turn 7, :finished-at 2026-09-01T22:33:57Z, 7 of 7 :completed"
      "library/math-informal-CT/.spider/gates/turn-000.edn .. turn-007.edn -- the per-turn linter reports"
      "library/math-informal-CT/.spider/receipts/ -- 21 receipts (intent/completion/applied for 7 turns); 0 malformed-* receipts"
      "No checkpoint directory: checkpoint! fires at turn 20 and this section is 7 patterns long"
      "No .flexiarg file changed"],
     :duplicate-index-hits-finding
     "New apparatus defect, found while checking the seat's hit counts, affecting every wave-3 coverage number reported so far. The pinned index stores a pattern's hits as a vector with no dedupe by record id: over 1092 indexed patterns, 662 have a repeated id; 74811 hit entries against 69706 distinct-per-pattern, 5105 duplicates, 6.8% (/tmp/L5s7/dupes.txt). Here transpose-across-an-adjunction shows 15 entries over 11 distinct ids; the other six patterns have none, which is why the seat wrote `~14'. Mechanism: page-evidence (library_graph_lint.clj:70-88) throws on a repeated CURSOR but add-record-occurrences (:382-402) conj's unconditionally, so a record returned on two overlapping pages is indexed twice. Not a live-write artifact: 1317 distinct ids are duplicated and all twelve sampled have an :evidence/at hours to weeks BEFORE the crawl basis (2026-08-30 and earlier against a 2026-09-01T20:35:40Z basis), so they were already in the store when the crawl began. Warrants unaffected -- a duplicated dirty hit is still dirty, and this section's one clean hit appears once -- but the `hits' figures in the or3, nomad, mmca, memory, math-formalization-CA and -CV entries are entry counts, not distinct-record counts. Wants its own row.",
     :unresolvable-citation-finding
     "Third symptom of the hole slices 5 and 6 found, and the easiest to close. 17 distinct evidence ids are cited across the seven notes (/tmp/L5s7/ids.txt); GET /api/alpha/evidence/<id> returns 200 for 8 and 404 for 9 (/tmp/L5s7/idcheck.txt). None invented: each 404 truncates a real id to its first hex group (e-pull-offer-1726f09b for ...-9157-4f51-8f0c-777a141f40ad, e-75c13c35 for ...-5dce-4afd-8f68-3fa79b43e405, and seven more). An edge could not do this -- evidence-valid? (checks/spider_runner.clj:222-238) requires the id to fetch and to contain the normalized excerpt -- but an absence is validated only for a matching :pattern and a non-blank :note (:280-283), so its citations are never dereferenced. Fix direction, for its own row: extract e-*/emacs-* tokens from an absence note and require each to resolve.",
     :rung-two-self-pollution-confirmed
     "Slice 6 inferred rung-2 self-pollution by reconstructing queries afterwards; this slice has it from the seat and it checks out. Five notes report meeting `this spider's own prompt' and name the id; all five fetch as :evidence/author \"zai-3\" with :evidence/at inside the run window -- 22:28:47, 22:29:41, 22:30:38, 22:31:41, 22:32:33 against a run of 22:27:44 to 22:33:57 (/tmp/L5s7/selfpoll.txt). e-e9f3d9bf-81d6-4e3c-8ab2-68fc6ce44bad is cited by three different patterns' notes: the turn-3 prompt kept surfacing for turns 4 and 5. C88 pins rung 1 and not rung 2, so the live text-search spends its limit=5 on the run's own turn-start envelopes. Costs recall, not correctness: rung-two-warrant? (checks/spider_runner.clj:210-221) applies reflection-record?.",
     :result
     "7 of 7 patterns processed in 7 turns, 0 seat failures, 0 retries, 0 malformed-output receipts, 6m13s (22:27:44Z -> 22:33:57Z). Every turn ended in a typed absence: 7 absences, 0 edges, 0 proposals, 0 attestations. Section before/after unchanged at :edges-by-kind {:why 0 :how 0 :see-also 0}, fraction-organised 0.0 -- the first wave-3 section that starts AND ends with no counted edge; library-wide unchanged at {:why 86 :how 27 :see-also 141}; the before and after lint reports are byte-identical.",
     :slice 7,
     :at "2026-09-01",
     :absence-ids-checked
     "Six of the seven notes name the rung-2 queries they ran, with the query strings quoted; the seventh (verify-universal-property) reuses slice 6's `no bounded keyword query could surface a warrant the supplied evidence structure excludes' and names none -- slice 6's defect recurs at 1 of 7 rather than 5 of 7. Every rung-1 hit count the notes give matches the pinned cache (2, 5, 0, 1, 0, 15-as-`~14', 2). Every @references list quoted matches the file, with one misquote: chase-the-diagram's note writes math-informal/construct-an-auxiliary-object where line 9 reads math-informal/construct-auxiliary-object -- the id it names does not exist, the one it meant does. None of slice 5's stale-directive defect.",
     :ledger-entry
     "library/.spider/ledger.edn -- {:wave 3 :section \"math-informal-CT\"}, the 14th element of :sections. The append was checked in-process before writing: (vec (:sections old)) equals (vec (butlast (:sections new))), (dissoc old :sections) equals (dissoc new :sections), the appended entry equals (last (:sections new)), and the file was re-read and compared to the value written. check-parens passes on the result.",
     :sha "5899f62",
     :seat "zai-3"}
    {:phantom-target-finding
     "Slice 5 recorded that math-informal-CA/conjugate-reduce-to-model-case, the @instantiates target of math-formalization-CA/schwarz-disk-automorphism-formula:9 and surj-via-oriented-root-preimage:9, names no file in library/. Stronger: it has never existed in this repository. git log --all --diff-filter=A -- '*conjugate-reduce-to-model-case*' is empty, and git ls-tree -r --name-only over 400 commits of git rev-list --all finds no path matching /conjugate/. The section lint still reports :unresolved-targets 0, because @instantiates is absent from checks/library_graph_lint.clj:8, so it is neither counted nor target-resolved. Respelled @why -- the migration README-flexiarg.md:224 records as 'same meaning' -- the linter would fail both as :dangling-target. 2 of the library's 31 @instantiates targets do not resolve; the other 29 do, checked one by one. The uncounted vocabulary is not merely uncounted, it is unchecked. Wants its own row alongside slices 5 and 6: the respelling is an authorship decision, but target-resolving @instantiates in the linter is not.",
     :not-touched
     ["No .flexiarg file changed (the section produced no edge), so this slice authors no @why, no @why-posthoc, and no similarity-inferred edge (P-validated-R5 law O2)."
      "The @instantiates and @references directives on other sections' files are counted and reported only. Respelling one as @why -- README-flexiarg.md:224's stated migration -- is an authorship decision the spider/editor lane may not make (decisions.edn :spider-editorial-standing), and the two phantom-target lines are left for the row that takes the migration."
      "The two seat record defects are corrected in the ledger, not edited in the absence records (L1 'recorded not revised')."
      "checks/library_graph_lint.clj:8, :107, :382-402 and :388-392, checks/spider_runner.clj:184 and :280-283, and checks/spider_fleet.clj:139 are all reported and none fixed: no Clojure is in this row's acceptance and each defect deserves its own row."
      "The other dirty paths in this shared checkout are not this slice's: holes/labs/library-contract/runs/build-loop.log, resources/sigils/patterns-index.tsv, and the three untracked library/math-formalization/*.flexiarg files. 31 explicit paths staged; no commit -a."],
     :duplicate-index-hits-confirmed
     "Slice 7's :duplicate-index-hits-finding reproduces at the smallest scale that shows it cleanly: math-informal-CA/estimate-by-bounding has 4 hit entries over 2 distinct ids, with e-c112f339-ad0e-4745-8821-0f6152b21711 appearing three times and emacs-e7c4dec7afcd98a1791d0014bf53be4f once. The seat noticed and wrote 'e-c112f339 (supplied three times)'. The other five patterns have no duplicates. Mechanism as slice 7 diagnosed (checks/library_graph_lint.clj:382-402 conj's unconditionally). No effect on this slice's verdicts: e-c112f339 carries :co-mention true whether counted once or three times.",
     :slice-7-count-correction
     "A correction to slice 7's :vocabulary-finding, measured on an unchanged tree (no .flexiarg has been modified since 5899f62). Slice 7 records '550 of 1247 files carry an @references line'. 550 is the count of @references LINES: grep -rh '^@references' library --include='*.flexiarg' | wc -l gives 550, grep -rl gives 494, and exactly 56 files carry two such lines (56 x 1 extra = the difference). The correct statement is 494 of 1247 files, 550 lines. Slice 7's other figures reproduce closely but not exactly under my parse -- 489 bracketed lines (slice 7: 488), 1650 targets (1649), 1233 resolving / 379 distinct (1218 / 377), 417 not resolving (431); those depend on how the bracket body is tokenised, so they are recorded as a second measurement, not a correction. Only the files-vs-lines conflation is a definite mis-statement, and it does not change slice 7's conclusion, which rests on the ratio of uncounted references to counted edges.",
     :gates
     {:exit-codes
      "captured to /tmp/L5s8/{before,after,after2,after3,run,kondo,parens,parens-records,parens-ledger2,lintest,spidertest,append,lintdiff,lintdiff2,lintdiff3}.exit and tested by a shell loop that reads each file; all are 0. No gate status read through a pipe.",
      :section-lint
      "checks/library_graph_lint.clj over math-informal-CA, run before the spider (exit 0), after it (exit 0), after the ledger append (exit 0) and after the ledger correction (exit 0). All four pass? true, 0 failures, 1247 files, 0 unresolved-targets, 0 why-cycles, 0 warrant-refusals, 0 edge-refusals, library-wide :edges-by-kind {:why 86 :how 27 :see-also 141}, section {:why 0 :how 0 :see-also 0} fraction-organised 0.0. All four reports byte-identical to the before report (diff exit 0, empty output). Reports at /tmp/L5s8/ca-{before,after,after2,after3}.edn",
      :runner
      "exit 0 (/tmp/L5s8/run.exit); state.edn :phase :complete, :turn 6, 6 of 6 completed",
      :clj-kondo
      "exit 0, 0 errors 0 warnings over checks/library_graph_lint.clj, checks/spider_runner.clj, test/library_graph_lint_test.clj, test/spider_runner_test.clj. No Clojure was touched by this slice. The one :info at library_graph_lint.clj:266 is the pre-existing spider-self-text? coercion L2 recorded.",
      :check-parens
      "exit 0, OK -- emacs -Q --batch -l /home/joe/code/futon4/dev/check-parens.el --eval (arxana-check-parens-cli) -- over the four gated Clojure files, over the section's EDN records (attestations, proposals, absences, seat-failures, state), and over library/.spider/ledger.edn after the append and again after the correction. The -- separator is required, per L2's note.",
      :library-graph-lint-test
      "exit 0, 7 tests / 36 assertions / 0 failures, under the C88 pin (1247 files; :why 86 :how 27 :see-also 141)",
      :spider-runner-test
      "exit 0, 6 tests / 33 assertions / 0 failures"},
     :section "math-informal-CA",
     :clean-hit-finding
     "The section's one clean rung-1 hit (:clean 1, :clean-non-reflection 1 of 6, from estimate-by-bounding's emacs-e7c4dec7afcd98a1791d0014bf53be4f; matches the runner's :rung-one-coverage {:patterns 6 :any 3 :clean 1 :clean-non-reflection 1}) is clean because the record names ONE pattern twice. It is a claude-2 emacs-claude-repl turn on the store failing to ingest the math split (/tmp/L5s8/clean-record.json), printing 'math-informal/estimate-by-bounding in store: True <- stale' beside 'math-informal-CA/estimate-by-bounding in store: False <- never ingested' -- the same file before and after its rename in 0678457. :co-mention is (> (count patterns) 1) over (filter ids ...) with ids the CURRENT pattern-id set (checks/library_graph_lint.clj:388-392), and library/math-informal/estimate-by-bounding.flexiarg no longer exists, so only one id counts. Slice 7 found the retirement case; this is the rename case, which cannot warrant an edge even in principle, since both ids denote one pattern. The seat refused it on the right substantive ground.",
     :basis {:count 206483, :max-at "2026-09-01T20:35:40.731036691Z"},
     :records
     ["library/math-informal-CA/.spider/absences.edn -- 6 records, each with its :corpus pin"
      "library/math-informal-CA/attestations.edn -- [] (no edge was warranted)"
      "library/math-informal-CA/proposals.edn -- []"
      "library/math-informal-CA/.spider/seat-failures.edn -- []"
      "library/math-informal-CA/.spider/state.edn -- :phase :complete, :turn 6, :finished-at 2026-09-01T22:49:18Z, 6 of 6 :completed"
      "library/math-informal-CA/.spider/gates/turn-000.edn .. turn-006.edn -- the per-turn linter reports"
      "library/math-informal-CA/.spider/receipts/ -- 18 receipts (intent/completion/applied for 6 turns); 0 malformed-* receipts"
      "No checkpoint directory: checkpoint! fires at turn 20 and this section is 6 patterns long"
      "No .flexiarg file changed"],
     :unresolvable-citation-finding
     "Slice 7's finding at a higher rate and in a new shape. 31 distinct e-*/emacs-* ids are cited across the six notes (/tmp/L5s8/ids.txt); GET /api/alpha/evidence/<id> returns 200 for 9 and 404 for 22 (/tmp/L5s8/idcheck.txt) -- 71% against slice 7's 53%. None invented: 9 are prefixes of full ids in the pinned cache, 12 resolve by prefix against the live store (/tmp/L5s8/idprefix.txt), and the 22nd is a shape slice 7 did not see -- e-cdb08ff7-bd43-4a31-aa15-838aa8563 is full-UUID-shaped with a 9-hex final group where the real record's (e-cdb08ff7-bd43-4a31-aa15-381275aa8563) is 12, so it is a garbled group and not a truncation from the right. That distinction bears on the fix: slice 7's proposed check (extract the tokens and require each to RESOLVE) catches it; a looser prefix-match check would not. The seat reports the defect from its own side -- pass-to-a-subsequence's note says 'two further query-2 ids did not resolve via GET and could not be verified'. Root cause unchanged: checks/spider_runner.clj:280-283 validates an absence only for a matching :pattern and a non-blank :note, while an edge's evidence must fetch and contain the excerpt (:222-238). Fourth symptom, one missing check.",
     :result
     "6 of 6 patterns processed in 6 turns, 0 seat failures, 0 retries, 0 malformed-output receipts, 5m42s (22:43:36Z -> 22:49:18Z). Every turn ended in a typed absence: 6 absences, 0 edges, 0 proposals, 0 attestations. Section before/after unchanged at :edges-by-kind {:why 0 :how 0 :see-also 0}, fraction-organised 0.0; library-wide unchanged at {:why 86 :how 27 :see-also 141}; the before and after lint reports are byte-identical. This is the last of the row's eight sections.",
     :slice 8,
     :at "2026-09-01",
     :row-complete
     "This closes the row: all eight sections named in :statement now have a wave-3 ledger entry (or3, nomad, mmca, memory, math-formalization-CA, math-formalization-CV, math-informal-CT, math-informal-CA), verified by filtering library/.spider/ledger.edn :sections on :wave 3. Across the wave the spider wrote 5 edges in total -- all @see-also, all in nomad (slice 2) -- against 76 typed absences, and proposed no @why (LD0 honoured throughout).",
     :absence-ids-checked
     "All six notes name the rung-2 queries they ran against http://127.0.0.1:7073/api/alpha/evidence/text-search (limit=5, hydrate=true), two each, with the query strings quoted -- so neither slice 6's unshown-search defect (5 of 7 there, 1 of 7 in slice 7) nor slice 5's stale-directive defect recurs here. Every rung-1 hit count is right against the pinned cache except pass-to-a-subsequence's (see :record-defects): epsilon-of-room 'the single ... hit' = 1; estimate-by-bounding 'supplied three times' = 3 of 4 entries; monotone-approximation, optimise-a-free-parameter, show-both-inequalities 'empty ([])' = 0. All six independently observe that the pattern 'carries only a @factor directive, which cannot be repeated' -- the seat reached this slice's finding from the file side.",
     :finding
     "The finding of this slice, and it splits a term the wave plan uses as one. math-informal-CT (slice 7) reads fraction-organised 0.0 because its relations are spelled @references, uncounted. math-informal-CA reads 0.0 because its patterns state no relation in ANY spelling: all six carry exactly @flexiarg @title @sigils @keywords @audience @tone @factor and nothing else (grep -ho '^@[a-z-]*' library/math-informal-CA/*.flexiarg | sort | uniq -c gives 6 of each and no other line). The split did not strip them -- git show 0678457^:library/math-informal/<id>.flexiarg for all six shows the same seven directives. Library-wide the bare shape is the majority: 133 of 1247 files carry an edge kind, 494 carry @references, 12 carry @family, 640 carry none. Per wave-3 section, bare/total: or3 17/17, math-formalization-CA 16/23, math-informal-CA 6/6, nomad 3/8, math-informal-CT 1/7, math-formalization-CV 1/7, mmca 0/8, memory 0/5. Ledger entry :no-outgoing-directive-finding.",
     :inbound-degree-finding
     "The corollary that makes the 0.0 misleading the other way. fraction-organised counts OUTGOING @why only (checks/library_graph_lint.clj:505-511), and a section's :edges-by-kind filters on (str/starts-with? (:from %) prefix) at :505. Measured inbound, 11 directive lines on other files name a math-informal-CA id, in three vocabularies: 1 @why (math-formalization-CA/uniform-continuity-boundedness:7 -> estimate-by-bounding, which the linter DOES count on the source side), 7 @instantiates (math-formalization-CA/{lp-norm-comparison,layer-cake-crossover-split,complex-arg-of-cpow-root,metric-cauchy-convergence}:9 and math-formalization-CV/complex-polynomial-bound:9 to estimate-by-bounding, plus 2 to a phantom), 3 @references (math-strategy/property-of-object-result:8, math-informal-NA/hybrid-certification:8, math-informal/structural-inclusion:9). estimate-by-bounding takes 8 of the 11 and is the library's most-named @instantiates target; epsilon-of-room, optimise-a-free-parameter and show-both-inequalities have zero inbound of any kind. Recorded, not acted on: reversing an inbound directive onto a CA file would be authoring a relation (decisions.edn :spider-editorial-standing, P-validated-R5 law O2).",
     :ledger-entry
     "library/.spider/ledger.edn -- {:wave 3 :section \"math-informal-CA\"}, the 15th element of :sections and the 8th wave-3 entry. The append was checked in-process before writing (/tmp/L5s8/append.bb): (vec (:sections old)) equals (vec (butlast (:sections new))), (dissoc old :sections) equals (dissoc new :sections), the appended entry equals (last (:sections new)), and the file was re-read and compared to the value written. check-parens passes on the result. One number in that entry was then corrected before the commit was amended: the wave-3 absence total read 78 and is 76 (17+3+8+5+23+7+7+6, summed from the eight entries' :after :absences), so the entry now carries the per-section breakdown and the summation source.",
     :record-defects
     ["pass-to-a-subsequence's note opens 'none of the four supplied hits' and names three (e-e21c0351, e-717bff58, e-901ed4e2). The pinned index holds 3 entries over 3 distinct ids for that pattern, so 'four' is wrong. Verdict unaffected: all three carry :listing true and :co-mention true. Recorded, not revised in place (L1 practice)."
      "estimate-by-bounding's note attributes its inbound-sibling list to 'earlier surveys of this same fleet' rather than the pinned evidence, and it is wrong on both edges: uniform-continuity-boundedness:7 spells its relation @why, not @instantiates, and is therefore the one inbound edge the linter already counts; and the list omits math-formalization-CV/complex-polynomial-bound:9, which slice 6 recorded. The conclusion survives -- each relation does already sit on the sibling's own file -- but the ground is recalled, not measured. Recorded, not revised."],
     :sha "346dbaf",
     :seat "zai-3"}],
   :owner :any}
  {:id :L7 :class :C :status :done-unreviewed :owner :any
   :covers-key :none
   :evidence
   {:sha "0b9a1a4"
    :defect-reproduced
    "Mutation run (the fixed file with only -main's guard reverted to the old assoc-then-nil-test): checks/spider_fleet.clj -main with --evidence-cache pointing at a nonexistent path did NOT throw; it printed `basis nil` and the coverage row `| aif | 33 | 0 | 0 | 0 |` and wrote library/.spider/fleet-2026-09-02.edn with :evidence-basis nil and :rung-one-coverage {:patterns 33 :any 0 :clean 0 :clean-non-reflection 0}. That report is saved at /tmp/L7gates/mutant-empty-index-report.edn and was deleted from the repo; it is the row's claim shown happening, not inferred. Output /tmp/L7gates/mutant2.out, exit 1 in /tmp/L7gates/mutant2.exit, 2 failures both in the new test."
    :fix
    ["checks/spider_fleet.clj:24-45 load-pinned-cache -- refuses BEFORE :cache-path is attached, which is where the old code could not: it separates :missing (fs/exists? false), :unparseable (edn/read-string throws, message carried in :read-error) and :no-index (parses but (:index cache) is not a map, the shape that would have handed every worker a nil index). The docstring states why a post-assoc nil test cannot fire."
     "checks/spider_fleet.clj:156-159 -- -main calls load-pinned-cache; the `_ (when-not cache (throw ...))` binding that clj-kondo flagged is deleted, not moved. The live branch (lint/ensure-live-evidence-index!) always returns a map, so the guard was dead on both arms."]
    :tests
    ["test/spider_runner_test.clj:161 the-fleet-refuses-a-missing-pin-before-it-starts-a-fleet -- drives fleet/-main itself (the fleet path the acceptance names, not just the helper) with a nonexistent pin and asserts the throw carries \"pinned evidence cache is missing\"; asserts the fleet report file is untouched across the call, so the refusal is proven to precede refresh! and the seat futures; and records the defect witness inline, (map? (assoc (fleet/read-edn missing nil) :cache-path missing)) => true. Budget 0 so that a future regression reports the failure instead of shelling out to a real seat."
     "test/spider_runner_test.clj:186 a-pin-that-carries-no-index-is-refused-and-a-readable-one-passes -- truncated EDN throws with :reason :unparseable, an index-less map throws with :reason :no-index, and the CONTROL (a readable pin written to a temp file) loads, keeps its :index, and reports :cache-path = the pin, so (or (:cache-path cache) (recompute)) still resolves to the pin the operator named."]
    :gates
    {:clj-kondo "exit 0, 0 errors 0 warnings over checks/spider_fleet.clj, checks/spider_runner.clj, checks/library_graph_lint.clj, test/spider_runner_test.clj, test/library_graph_lint_test.clj. The :139 Condition-always-true warning this row is about is gone (baseline before the fix: 1 warning at 139:23). The one remaining :info is the pre-existing spider-self-text? boolean coercion at library_graph_lint.clj:287, outside this diff. /tmp/L7gates/kondo.{out,exit}"
     :check-parens "exit 0, OK (emacs -Q --batch -l futon4/dev/check-parens.el --eval (arxana-check-parens-cli) -- --no-defaults, over both touched files; the args must follow a bare -- or the CLI reports \"No target files\" and exits 2). /tmp/L7gates/parens.{out,exit}"
     :spider-runner-test "exit 0, 8 tests / 43 assertions / 0 failures (was 6 / 33). /tmp/L7gates/spidertest.{out,exit}"
     :library-graph-lint-test "exit 0, 10 tests / 59 assertions / 0 failures, unchanged. Library census identical to LD1's record: 1247 files, :edges-by-kind {:why 86 :why-posthoc 0 :how 27 :see-also 141}. /tmp/L7gates/lintest.{out,exit}"
     :exit-codes "Every gate wrote $? to /tmp/L7gates/<gate>.exit immediately after the command and each file was compared as a string; no gate status was read through a pipe."}
    :not-in-scope
    "No .flexiarg file and no attestation record was touched, so this item authors no @why, @why-posthoc, @how, @see-also or similarity-inferred edge (P-validated-R5 law O2, decisions.edn :spider-editorial-standing). No section lint is recorded because no library section changed: this is a class C code row, not a wave slice. checks/spider_runner.clj:120's own throw is unchanged -- it was already correct, as the row states; the fleet failing earlier means the runner's throw is now a second line of defence rather than the only one."}
   :statement "spider_fleet.clj:139: clj-kondo's `Condition always true` is right -- (assoc (read-edn path nil) :cache-path p) is a map even when the pinned cache file is absent, so the :pinned-evidence-cache-is-unreadable throw can never fire and a missing pin would run a fleet on an empty index. Recorded by L5 slice 1; spider_runner.clj:120's own throw works."
   :acceptance "The fleet path refuses a missing or unreadable pinned cache loudly (test proves it: point it at a nonexistent pin and assert the throw). A control proves a readable pin still passes. clj-kondo 0/0, check-parens, spider tests green."}
  {:id :L8 :class :C :status :open :owner :any
   :covers-key :none
   :statement "The evidence-cache filename says v3 while its content says :reflection-rule {:version 2} -- library_graph_lint.clj:107 hardcodes the literal -v3- and carries the version only in the sha (L5 slice 1, reviewer-check item 2). A reader trusting the filename misreads which reflection rule filtered the cache."
   :acceptance "The cache content carries the rule version authoritatively and the lint asserts filename tag == content version, failing loudly on mismatch. The existing v3-named/v2-content cache is either regenerated or renamed with the reason recorded. Tests cover the mismatch case."}
  {:id :L9 :class :W :status :open :owner :any
   :covers-key :none
   :loop-mode :one-slice-per-invocation
   :statement "Authoring stage for the rationale layer, using LD1's mechanism: wave 3 proved the 0%-families harvest nothing (76 absences, 0 edges), and README-flexiarg §5a's corollary says the rationale layer can only be authored. Pilot sections, one per invocation: war-room (the section §5a's own post-hoc discussion is about: rulings that make sense of patterns retrospectively; 28 patterns, 17 in live use, zero corroborated relations), then aif (33 patterns, best-attested section, feeds the WM candidate space). INTERIM CHOICES, Joe may redirect: pilot = these two sections; the attestation bar for a post-hoc edge is the same evidence-tag machinery spider edges use."
   :progress []
   :acceptance "Per slice: read each pattern in ONE section together with its evidence tags and the section's recorded absences; where a retrospective reading has a statable warrant, write the inline @why-posthoc edge AND a :proposed attestation record carrying the warrant text and its evidence pointer. No bare edges: an @why-posthoc without a warrant record is a lint failure. Never rewrite an author's own @why. Target quality over count; a section slice that finds nothing statable records that finding. Section lint before/after, spider tests, worklist_check. Row done when both pilot sections have a slice entry and the proposed edges are queued for attestation review."}
  {:id :L10 :class :E :status :open :owner :any
   :covers-key :none
   :loop-mode :one-slice-per-invocation
   :statement "Generalize L4's price test: nine attested @how edges exist (aif 6, peripherals 2, ukrns 1); L4 built the derived check for one at 5m47s. Eight remain. L4 also found most of the other eight name no concrete incident in their evidence paragraphs -- so the expected finding for several slices is :no-site-to-check-against, which is itself the measurement."
   :progress []
   :acceptance "Per slice, ONE edge: either write the derived check (L4's witness pattern as template: check runs against the site the pattern's evidence names, positive control included) and record its cost like L4 did, or record :no-site-to-check-against with the evidence paragraph quoted. No invented sites (the L4 selection-cost lesson). Row done when all eight have a check or a recorded no-site finding, with the price table summarized in a note beside NOTE-the-chain-layer-exists-and-is-empty.md."}
  {:id :LD1,
   :class :D,
   :status :done,
   :owner :any,
   :covers-key :none,
   :reviewed-by "codex",
   :reviewed-at "2026-09-01T23:24:32Z",
   :review
   "Read e0689e0 in full and confirmed it is authored by Joseph Corneli, not the reviewer. Reproduced clj-kondo exit 0 (0 errors, 0 warnings), check-parens exit 0 (OK), library_graph_lint_test exit 0 (10 tests, 59 assertions; C88 pin 1247 files and library-wide edges :why 86, :why-posthoc 0, :how 27, :see-also 141), spider_runner_test exit 0 (6 tests, 33 assertions), and worklist_check exit 0. Confirmed @why-posthoc is parsed and counted separately, must resolve and earn attestation when new, does not enter the authored-authority cycle law or organised fraction, remains invisible to checks/playout_snatch.clj until opt-in, and is standard in the directive whitelist. The evidence commit changes no .flexiarg file and leaves checks/spider_runner.clj's permitted directives unchanged, so it authors no spider @why, @why-posthoc edge, or similarity-inferred edge. No wave-section lint applies to decision row LD1.",
   :evidence
   {:sha "e0689e0",
    :decision
    "holes/labs/library-contract/decisions.edn :posthoc-why-syntax -- three arms, each now carrying :parser-impact, :greppability and :cost separately, plus :interim, :why-interim, :changes-it and a :landed map of what this item put where. Two cost claims were measured, not asserted: playout_snatch.clj:237 (`re-matches #\"@why (.*)\"`) returns nil on `@why-posthoc war-room/wr-3` and \"war-room/wr-3\" on the authored line; and `grep '@why'` and `grep '@why\\b'` both match a @why-posthoc line (2 hits over a 2-line file) while `grep '@why '` matches 1 -- recorded as the adopted arm's own cost, a bare-@why grep over-counts.",
    :standard
    "README-flexiarg.md:198-216 -- the interim form `@why-posthoc <id> [<id> …]` in §5a with a pointer to decisions.edn :posthoc-why-syntax, the invisible-until-opt-in property, and what the lint does with it. Added as a marked paragraph after the 2026-08-23 post-hoc ruling, not as a fourth row of the §5a table, which is introduced as declaring two relations.",
    :whitelist
    "flexiarg-directives.edn :why-posthoc, :status :standard, :value :pattern-id-list -- §5c: a directive reaches the store only if it is :standard here, so the interim form had to be admitted to be ingestable and attestable. Its SYNTAX is what is interim.",
    :lint
    ["checks/library_graph_lint.clj:8-21 -- edge-kinds unchanged (the three §5a relations); new posthoc-why-kind and counted-edge-kinds"
     "checks/library_graph_lint.clj:148 -- the kind alternation now reads @why-posthoc first; before this it matched nothing and the directive was dropped in silence"
     "checks/library_graph_lint.clj:237 -- valid-attestation? admits :why-posthoc, so a post-hoc edge can earn the attestation §5a asks of it; without this the new-edge rule would make one an unfixable failure"
     "checks/library_graph_lint.clj:528-538 -- section :edges-by-kind reports :why-posthoc as its own key and adds :patterns-with-outgoing-why-posthoc; :fraction-organised and :patterns-with-outgoing-why still filter on :why alone"
     "checks/library_graph_lint.clj:608 -- library-wide :edges-by-kind likewise; :patterns-in-why-graph (line 610) stays authored-only"
     "checks/library_graph_lint.clj:174-182 -- cycle-failures documents why it does NOT admit post-hoc edges: acyclicity is a law about authority"],
    :tests
    ["test/library_graph_lint_test.clj:235 posthoc-why-is-parsed-and-counted-apart-from-authored-why -- the form is recognised (1 :why-posthoc edge, exit 0) and never summed into :why; a matched authored-@why fixture reaches :fraction-organised 0.5 where the post-hoc one stays 0.0"
     "test/library_graph_lint_test.clj:260 posthoc-why-earns-the-evidence-checks-but-not-the-authority-law -- dangling target and new-edge-without-attestation both fire on a post-hoc edge; a post-hoc/authored mixed loop passes with :why-cycles 0 while the all-authored loop still fails :why-cycle"
     "test/library_graph_lint_test.clj:297 posthoc-why-is-invisible-to-consumers-that-have-not-opted-in -- the playout_snatch @why regex reads zero post-hoc lines, and the whitelist carries :why-posthoc as :standard"
     "test/library_graph_lint_test.clj:210 -- the live-report partition assertion moved from edge-kinds to counted-edge-kinds and asserts the key is present at zero. The 2026-08-31 snapshot fixture is untouched: its census keys are still checked against edge-kinds, so the pinned measurement is not rewritten"],
   :gates
    {:clj-kondo
     "exit 0, errors 0 warnings 0 over checks/library_graph_lint.clj and test/library_graph_lint_test.clj. The one :info is the pre-existing spider-self-text? boolean coercion, now reported at library_graph_lint.clj:287 because this change adds 21 lines above it (recorded at :266 by earlier slices, :234 by L1); it is outside every hunk of this diff",
     :check-parens "exit 0, OK (emacs -Q --batch, arxana-check-parens-cli, --no-defaults over both files)",
     :library-graph-lint-test
     "exit 0, 10 tests / 59 assertions / 0 failures (was 7 / 36) under `bb -cp .` -- the invocation C88-pinned-library-evidence-index.md documents; futon3 has no bb.edn, so a bare `bb <file>` fails at the require",
     :spider-runner-test "exit 0, 6 tests / 33 assertions / 0 failures",
     :section-lint
     "aif section under the C88 pin: exit 0, pass? true, 1247 files, 0 failures, 0 unresolved targets, 4 edge-refusals, 0 warrant-refusals. Counts :why 86, :why-posthoc 0, :how 27, :see-also 141; section aif :why 6, :how 13, :see-also 45, :fraction-organised 0.1818. Not asserted from memory: the same lint built from `git show HEAD:checks/library_graph_lint.clj` and run as its own script gives a byte-identical report except for the two new zero-valued keys, so the authored counts are unchanged by this item and :see-also 141 (vs 136 in earlier records) is pre-existing library drift"},
    :not-in-scope
    "checks/spider_runner.clj:18 edge-kinds is unchanged, so the spider lane still may not propose a @why-posthoc edge -- widening the fleet's vocabulary is a separate decision under :spider-editorial-standing, and this item's acceptance names the lint only. checks/playout_snatch.clj is unchanged by design: it is the opt-in that has not been taken, and its silence is the property the adopted arm was chosen for. No .flexiarg file was touched, so this item authors no @why, no @why-posthoc and no similarity-inferred edge (P-validated-R5 law O2); the library carries zero post-hoc directives, and the key is reported at 0.",
    :proposes-nothing-to-joe
    "The interim adoption stands under the 2026-09-01 record-so-Joe-can-veto rule. A veto costs the four pointers above, not re-work: no edge anywhere is spelled @why-posthoc yet."},
   :statement
   "README-flexiarg §5a asks for an inline mark distinguishing a post-hoc @why (a trace, a hypothesis) from an author's own @why (a causal claim) and does not fix the syntax. Not determined by theory: a decision with arms, recorded in decisions.edn (interim arm :why-posthoc adopted so work can proceed; Joe can veto).",
   :acceptance
   "decisions.edn entry :posthoc-why-syntax carries the three arms with costs (parser impact, greppability, collision with existing @why consumers such as checks/playout_snatch.clj parse-why), the interim choice, and what evidence would change it. README-flexiarg §5a gains the interim form with a pointer to the decision. The lint recognises the interim form and counts it separately from authored @why. Tests."}
  {:id :LJ1,
   :class :J,
   :status :needs-joe,
   :owner :joe,
   :covers-key :none,
   :bar
   {:why-not-runnable
    "Confirm-vs-rewrite of a problem statement has no machine-behaviour difference: the statement is the criterion runs are judged against, so no recorded field separates the arms.",
    :a
    "The S1 problem field of P-validated-R5.md is marked [Joe: confirm/rewrite] by its own author; no source, code, or prior ruling determines what the operator means his problem statement to be.",
    :b
    "No experiment can decide a problem statement: it is the criterion runs are judged against, so the arms (confirm vs rewrite) have no machine-behaviour difference a recorded field could separate."},
   :statement
   "P-validated-R5 §1 confirm/rewrite. The gate before any §5 packet is dispatched: Joe confirms the S1 problem statement (G computed over single actions from a channel-range C; 'policy' a borrowed name for a cascade; a cascade in code a bag of pattern ids; nothing states what G over a cascade IS).",
   :acceptance
   "Joe says 'confirmed' or supplies the rewrite; the confirmation or new text lands in P-validated-R5.md §1 with a date; L6 unblocks."}
  {:id :L6,
   :class :C,
   :status :blocked,
   :owner :any,
   :depends-on [:LJ1],
   :covers-key :none,
   :statement
   "Kernel + compose on library/snatch first (P-validated-R5 §5 step 3, owner codex-22 by that record): the generic find/organise path, currently sorry in DarkTower/WarMachine/Holes.lean:264,308 and hardcoded to library/snatch in checks/playout_snatch.clj:228-260 and find_snatch.clj.",
   :acceptance
   "As P-validated-R5 §5 step 3 states it, against the pinned find-snatch.edn fixture; laws F1-F4/O1-O4 as narrowed by the 2026-08-31 scope amendment. Not started before LJ1."}]}
