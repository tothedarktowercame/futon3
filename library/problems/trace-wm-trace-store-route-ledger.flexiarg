@flexiarg problems/trace-wm-trace-store-route-ledger
@title Problem node TRACE: WM trace store / route ledger
@keywords problem-node, TRACE, dossier, wm-trace-store-route-ledger
@audience futon stack operators, library authors
@tone plain
@style pattern
@holds-at TRACE
@why dossier authority: PROBLEMS-assurance-band-batch6.md, TRACE, The problem it solves (no library pattern grounds this node; its authority is the recorded dossier)
@how no mechanism claimed at problem grain; what solving requires is the dossier's residual-problems list (source: futon2 holes labs wm-contract PROBLEMS-assurance-band-batch6.md, TRACE)

! conclusion: This is a problem-stating node for control-map node TRACE (WM trace store / route ledger): TRACE solves durable attribution of a WM tick to its recording route: the roster places **“WM trace store (route ledger)”** in ACT's assurance band and names the write basis (`control-stages.edn:44`; control-stage TRACE).

  + context: Minted by library-loop row L5 from the twenty-node problem dossiers; it exists so @why edges from mechanism patterns have a problem node to land on.

  + IF:
    A pattern's rationale is that it answers the problem this node states.

  + HOWEVER:
    The problem, verbatim from the dossier: TRACE solves durable attribution of a WM tick to its recording route: the roster places **“WM trace store (route ledger)”** in ACT's assurance band and names the write basis (`control-stages.edn:44`; control-stage TRACE). Runtime trace records preserve observations, beliefs, scores, ranked actions, decisions, mode, and optional version provenance (`src/futon2/aif/trace.clj:1-44`; runtime TRACE). Immediately before writing, the WM attaches `:TRACE` to `:wm/route` (`scripts/futon2/report/war_machine.clj:6750-6759`; runtime route ledger). TRACE has no catalogue pattern or contract criterion, and the equation registry classifies it as plumbing (`aif-equations.edn:472-475`; explicit missing-source facts). Its scoped problem is thus making the tick and its route inspectable after execution, not proving that all process stages occurred. (source: futon2 holes labs wm-contract PROBLEMS-assurance-band-batch6.md, TRACE, The problem it solves)

  + THEN:
    Treat this node as the landing point for @why edges from patterns that answer it; solving the problem itself is scoped by the dossier's residual-problems list, not by this node.

  + BECAUSE:
    The census (library-loop L1) found only six problem-stating nodes for 1256 patterns, so most @why edges had nothing to land on; minting the dossier problems as nodes is the recorded-corpus repair.
    + evidence: futon2 holes labs wm-contract PROBLEMS-assurance-band-batch6.md, section "## TRACE — WM trace store / route ledger", subsection "The problem it solves".
