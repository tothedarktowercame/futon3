@flexiarg math-formalization-CA/metric-cauchy-convergence
@title Cauchy Sequence Convergence in Metric Spaces
@sigils [🌀/収]
@keywords Cauchy, convergence, metric, tendsto, triangle-inequality, epsilon-delta
@audience lean-provers, analysis-students
@tone heuristic
@difficulty moderate
@subjects analysis topology
@instantiates math-informal-CA/estimate-by-bounding
@examples a97A04
@why no problem-corpus source matched (@holds-at absent; holds-token and id-text searches over every node in [problems futon-theory] found no match). Negative claim per the committed check futon2 holes labs library-loop runs l6_no_source_check.py, receipt runs/LL8-no-source-check.edn. Rationale not invented (L8 rationale-backfill, zai-1, 2026-09-05)
@how own mechanism: Prove convergence by showing Cauchy via Metric.cauchySeq_iff + triangle inequality chains. (source: this pattern's ! conclusion line; L8 rationale-backfill, zai-1, 2026-09-05)

! conclusion: 
  Prove convergence by showing Cauchy via Metric.cauchySeq_iff + triangle inequality chains.

  + IF: Goal is Tendsto or convergence of a sequence in a metric/normed space.

  + HOWEVER: Direct epsilon-delta may require long calc chains.

  + THEN: Use Metric.cauchySeq_iff to reduce to pairwise distance bounds, then calc + nlinarith.
    + LEAN:
      api[Metric.cauchySeq_iff] api[Metric.tendsto_atTop] api[Real.dist_eq]
      tactic[have → intro → calc → nlinarith → ring]

  + BECAUSE: Cauchy in a complete space gives convergence; the iff lemma handles the quantifiers.
