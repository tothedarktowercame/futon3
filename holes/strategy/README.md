# Strategy Devmaps

This directory holds devmaps for strategic handles that are:

1. Cross-repo rather than owned by one feature devmap
2. Actionable as closure programs, not only as abstract theses
3. Measurable by bindings, counters, or durable evidence

Current classification, derived from
`futon5a/data/alignment.edn :sorry-topology` and the `🌐6` rubric:

Scorecard:

- See [strategic-sorry-v1-scorecard.md](/home/joe/code/futon3/holes/strategy/strategic-sorry-v1-scorecard.md:1) for the current recommendation-grade scoring pass against the `SNv1` invariant.

Numbering rule for Strategic SORRY devmaps:

- `globeN-*` follows the current order of `:sorry-topology` in
  `futon5a/data/alignment.edn`, which is the source War Machine reads for
  Strategic SORRY nodes today.
- This is distinct from the stack-spine `🌐N` alias series; for example
  bare `globe6` still refers to the stack-level `🌐6` substrate handle, while
  `globe6-governance-interface` would refer to the sixth Strategic SORRY in
  the topology order.

| Handle | Status | Why |
|---|---|---|
| `🌐6` | full devmap | Cross-cutting recursive substrate with explicit closure predicates |
| `globe1-market-interface` / `SORRY-market-interface` | full devmap | The primary external-demand / incoming-line gap; cross-repo and counter-checkable |
| `globe2-mode-violation` / `SORRY-mode-violation` | full devmap | Critical constraint violation (`cargo-implies-depositing`) that needs a closure program, not just diagnosis |
| `globe3-peer-eval-artifact` / `SORRY-peer-eval-artifact` | lighter note | Real gap, but currently a bounded packaging/artifact problem rather than a full strategic substrate |
| `globe4-paragogy-revenue` / `SORRY-paragogy-revenue` | lighter note | Strategic, but current probes are still too fuzzy for a good closure program |
| `globe5-vsat-revenue` / `SORRY-vsat-revenue` | candidate later | Likely worthy of a full devmap once the current Eric path or another concrete route is sharper |
| `globe6-governance-interface` / `SORRY-governance-interface` | defer | Low urgency and not yet load-bearing |
| `globe7-novelty-floor` / `SORRY-novelty-floor` | diagnostic only | Better treated as a tick / symptom than as its own devmap |
| `globe8-policy-transition` / `SORRY-policy-transition` | full devmap | Cross-workstream policy/state transition with observable commit/evidence ratios |

Important distinction:

- `SORRY-mode-violation` is the constraint-level devmap: when cargo is present,
  the system must detect and clear the violation.
- `SORRY-policy-transition` is the regime-change devmap: how Joe actually moves
  from `:pi-scholar` toward `:pi-free-solo` via a balanced depositing policy.

Those two are coupled but not identical.
