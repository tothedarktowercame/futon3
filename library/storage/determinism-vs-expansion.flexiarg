@flexiarg storage/determinism-vs-expansion
@title Determinism vs Feature Expansion
@sigils [🌏/万]
@keywords determinism, features, expansion, gates
@audience storage maintainers, futon1a builders
@tone formal-analytic
@factor Keen investigation (dhammavicaya)
@references [stack-coherence/evidence-ledger storage/deterministic-substrate]
@status [status[proposed] evidence[holes/missions/M-futon1a-evidence.md#L126] evidence[holes/missions/M-futon1a-evidence.md#L142]]
@why no problem-corpus source matched (@holds-at absent; holds-token and id-text searches over every library/problems node -- legacy 6, L5 dossier 20 incl. TRACE, L5 record/ruling 6 -- found no match). Negative claim per the committed check futon2 holes labs library-loop runs l6_no_source_check.py, receipt runs/L6-no-source-check.edn. Rationale not invented (L7 rationale-backfill, zai-1, 2026-09-05)
@how own mechanism: if you want new features but also deterministic behavior, isolate expansions behind determinism gates and tests (source: this pattern's ! conclusion line; L7 rationale-backfill, zai-1, 2026-09-05)

! conclusion: 
  if you want new features but also deterministic behavior, isolate expansions behind determinism gates and tests

  + context: futon1 fixed determinism gate baselines while adding ingestion features.

  + IF:
    you want to add new ingestion or modeling features

  + HOWEVER:
    determinism must remain intact across environments

  + THEN:
    require determinism gates and regression tests for any feature expansion

  + BECAUSE:
    un-gated expansion breaks reproducibility and undermines the storage substrate

  + NEXT-STEPS:
    next[Define determinism gates for each ingest or model pipeline.]
    next[Add fixtures that must remain invariant across versions.]
